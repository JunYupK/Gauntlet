# Claude Code — 개인 설정은 커밋하지 않는다 (.claude/settings.json은 공유)
.claude/settings.local.json

# SDD 작업 공간 (스킬이 만드는 스크래치)
.superpowers/

.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
.idea/
*.iml
records/
web/node_modules/
web/.next/

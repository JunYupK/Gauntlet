# log.md — 결정 로그

> BRIEF.md가 **입력**이라면, 이 문서는 그 입력으로부터 무엇을 어떻게 결정했는지의 **기록**이다.
> BRIEF §8이 요구하는 "설계 판단과 그 사유", "브레인스토밍 대화 기록"이 여기에 쌓인다.
> 나중에 재구성하지 않는다. 결정이 나오는 즉시 append 한다.
> **기각된 대안과 그 이유도 지우지 않는다.** 무엇을 고르지 않았는지가 발표의 재료다.

---

## 세션 1 — 2026-08-19 · 브레인스토밍

`superpowers:brainstorming` 스킬로 진행. 경로 분류: **아키텍처(Architectural)**.
근거: 저장소에 BRIEF.md 하나뿐인 신규 프로젝트이고, 게임 엔진·루프 하네스·시각화·기록을 모두 새로 만든다. 수정할 기존 흐름이 없으므로 bounded가 될 수 없다.

---

### D1. 종목 — 트론 라이트사이클

**결정.** 정수 격자 위 1:1 트론 라이트사이클. 지나간 칸은 영구 벽. 먼저 부딪히는 쪽이 진다.

**근거**
- 규칙이 한 문장이라 BRIEF §2(비개발자 30초 이해)를 통과한다.
- R3 육안 식별이 가장 극적이다. 약한 봇은 5턴 만에 자멸하고, 강한 봇은 격자를 빼곡히 채우며 200턴을 버틴다. 두 화면을 나란히 놓으면 설명이 필요 없다.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 스네이크 배틀 | 먹이 스폰 위치가 승패에 큰 운 요소로 작용. 세대 간 개선이 실력인지 운인지 분리하려면 매치 수를 크게 늘려야 함 |
| 영역 점령(땅따먹기) | 점수가 연속적이라 그래프는 매끄럽지만 "죽는 순간"이라는 극적 장면이 없어 R3의 시각적 임팩트가 약함 |
| 물리 기반 대전(당구·로봇) | **후보에서 사전 배제.** 부동소수점 누적 오차와 타임스텝 의존성 때문에 R1(같은 조건 → 같은 결과) 보장 비용이 큼. 정수 격자 이산 턴제는 재현성이 공짜로 나온다 |

---

### D2. 턴 판정 규칙 — 순서 의존성 제거

**결정.** BRIEF §11-2("동시에 일어나는 일의 처리 순서를 얼버무리지 않는다")를 규칙 확정 시점에 처리했다.

턴 `t`의 벽 집합 `W(t)`를 **고정한 채로** 두 봇의 목표 좌표 `pA`, `pB`를 각각 계산한 뒤 동시에 판정한다.

- A 사망 ⟺ `pA`가 맵 밖 ∨ `pA ∈ W(t)` ∨ `pA == pB`
- B도 동일한 식 (A/B를 바꿔도 결과가 같다 = 선후공 이점 없음)
- 생존한 봇에 대해서만 `W(t+1) = W(t) ∪ {살아남은 머리 좌표}`
- 시작 칸은 0턴에 즉시 벽 → **후진은 별도 규칙 없이 자기 벽 충돌로 자연 사망**
- 한쪽만 사망 → 생존 승 / 양쪽 사망 → 무승부
- 매 턴 벽이 최소 1칸 늘어나므로 **최대 턴 ≤ 격자 칸 수** (무한 경기 불가, 종료 보장)
- 시작 위치는 시드로 생성하고, 시드마다 **좌우를 바꿔 2판** (시작 위치 비대칭 이점 제거)

**근거.** "A를 먼저 움직이고 B를 움직인다"는 순차 해석은 선공 이점을 만들어 공정성을 깬다. 벽 집합을 고정한 동시 판정은 A/B 대칭이므로 순서 자체가 존재하지 않는다.

---

### D3. R1의 함정 — 시간 기반 타임아웃 금지

**결정.** 봇에 시간 기반 타임아웃을 걸지 않는다. **성능은 관문에서만 판정하고, 대전은 시간에 전혀 의존하지 않는다.**

**근거.** 같은 봇이 어떤 실행에서는 통과하고 어떤 실행에서는 타임아웃되면 같은 조건에 다른 결과가 나온다. R1이 깨지고, R1이 깨지면 루프의 판정이 흔들려 C2가 성립하지 않는다.
승격 심사 때 "1수당 응답 시간" 관문(G6)을 넉넉한 마진으로 통과시키고, 일단 통과한 봇의 대전 결과는 순수하게 결정론으로 만든다.

BRIEF §11-3("재현 가능성을 먼저 요구한다")에 해당하는 지점. 알아서 나오지 않는 성질이었다.

---

### D4. 봇 실행 모델 — Java 클래스 + 빌드 시 컴파일

**결정.** 봇은 `Bot` 인터페이스 구현체 하나. 세대마다 `Gen07Bot.java` 같은 파일을 추가하고 Gradle이 컴파일한다. 루프는 "코드 작성 → gradle 관문 → 대전" 사이클.

**근거**
- 인프로세스라 빠르고 구성요소가 적다 → R4(발표 중 안정성)에 유리.
- 봇 코드가 Java 소스라 화면에 띄우기 좋다. **세대별 코드 diff 자체가 발표 재료**가 된다.
- BRIEF §9("봇은 사람이 읽을 수 있는 코드다")와 정합.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| JavaCompiler API 런타임 컴파일 | 재빌드 없이 봇 추가가 가능하고 발표 중 라이브 세대 생성도 되지만, 바로 그 라이브 의존이 R4가 경계하는 지점. 클래스로더·메모리 관리 복잡도도 붙음 |
| 별도 프로세스 + JSON stdin/stdout | 격리는 가장 강하지만 프로세스 생명주기·파이프 버퍼·OS 의존성이 생기고, 이 인프라 자체가 발표에서 보여줄 것이 아님에도 시간을 크게 먹음 |

---

### D5. 관측 모델 — 전지 시점 + 무상태

**결정.** 봇은 전체 격자와 양쪽 머리·방향·턴 번호를 보고, **인스턴스 필드를 가질 수 없다.**

```java
public interface Bot {
    String name();
    Direction move(GameView view);   // 상태 없음. 매 턴 view만 보고 결정
}

public record GameView(
    int width, int height,
    boolean[][] wall,        // 이동 전 벽 집합 W(t)
    Point myHead,  Direction myDir,
    Point oppHead, Direction oppDir,
    int turn
) {}
```

**근거**
- 무상태 순수 함수로 못 박으면 "같은 입력 → 같은 출력"이 인터페이스 수준에서 강제된다. **R1이 규율이 아니라 구조가 된다.**
- "인스턴스 필드 0개"는 리플렉션으로 **기계 판정된다**(R2, §11-1).
- 전지 시점이면 개선 계단이 길게 나온다:
  > 무작위 → 벽 회피 → 한 수 앞 예측 → flood fill 남은 공간 비교 → Voronoi 영역 다툼 → 관절점(articulation point) 회피 → 상대 수 읽기

  이 계단은 화면에서 설명 없이 보인다(R3).

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 전지 시점 + 상태 허용 | 경기 간 객체 오염으로 재현성이 깨질 경로가 생기고 그걸 기계로 막기 어렵다. §6이 코드 재작성 방식이라 누적 상태의 이득도 작다 |
| 제한 시야(반경 N칸) | flood fill·Voronoi 같은 전역 사고 단계가 통째로 사라져 개선 계단이 3~4칸에서 끊기고, 초기/최종 봇의 육안 차이도 줄어든다 |

---

### D6. 관문 목록 — 위생 관문 G1~G6

**결정.** 전부 코드가 O/X를 낸다(R2).

| | 관문 | 판정 방법 |
|---|---|---|
| G1 | 컴파일 성공 | gradle |
| G2 | 인스턴스 필드 0개 | 리플렉션 |
| G3 | 금지 API 미사용 (시드 없는 Random, 시계, Thread, 파일/네트워크, static 가변 필드) | 바이트코드 검사 |
| G4 | 합법 수 — 무작위 국면 N개에 예외 0회, 범위 밖 반환 0회 | 퍼징 |
| G5 | 결정론 — 같은 국면 2회 호출 일치, 같은 시드 경기 2회 리플레이 해시 일치 | 해시 비교 |
| G6 | 시간 예산 — 국면 N개 p99 응답 < X ms **(관문에서만 측정, 대전에는 미적용)** | 벤치 |

**함께 결정한 것 둘 — 관문과 R3의 긴장을 푸는 장치**

관문을 세게 잡을수록 R3이 손해를 본다. R3은 1세대 봇이 처참하게 약할 것을 요구하는데, "자멸하지 말 것" 같은 품질 관문을 걸면 1세대부터 이미 벽을 피한다. 개선 곡선의 가장 극적인 구간이 관문에 미리 먹힌다.

1. **Gen 0은 관문 대상이 아니다.** Gen 0은 사람이 심는 기준선(예: 직진만 하는 봇)이고 루프는 Gen 1부터 돈다. 관문은 *도전자*에게만 적용된다.
2. **품질 기준은 관문이 아니라 지표로 둔다.** 자멸률(안전한 칸이 있는데도 죽는 수를 둔 비율)은 반려 사유가 아니라 **세대별로 측정해 남기는 숫자**다. 관문이 곡선을 죽이지 않으면서 `자멸률 87% → 3%` 같은 그래프가 R3의 보조 증거로 공짜로 쌓인다(§8).

---

### D7. 실력 하한선 관문 — G7 회귀 방지 추가

**결정.** G1~G6에 더해, 고정된 베이스라인 봇 3종(직진봇/랜덤봇/벽회피봇)을 **전 시드 격파**해야 챔피언에 도전할 자격이 생긴다.

**근거.** 고정 상대라 변하지 않는 절대 좌표가 생긴다. 도전자가 운으로 챔피언을 이기고 승격해 공선이 뒤로 가는 사고를 막는다. 베이스라인이 약해서 개선 공간을 미리 먹지도 않는다.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 위생 관문만(G1~G6) | 가장 단순하지만 도전자가 운으로 승격해 세대 공선이 하락하는 구간이 생길 수 있음 |
| G7 + 행동 품질 관문(자멸률 상한 등) | Gen 1부터 이미 깔끔하게 플레이해 개선 계단의 앞마디가 잘리고, 자멸률을 발표용 그래프로 쓸 수 없게 됨 (D6의 장치와 충돌) |

---

### D8. 승격 기준 — 승점 승률 60%

**결정**
- 심사: **시드 50개 × 좌우 스왑 = 100경기**
- 무승부는 **0.5점** → 승점 승률 = `(승 + 0.5×무) / 전체`
- **60% 이상**이면 도전자가 챔피언을 교체
- 역대 전 세대 라운드로빈은 승격 판정에 쓰지 않고, **마지막에 한 번만 돌려 대진표 히트맵**으로 남긴다 (12세대 기준 약 1.4만 경기 ≈ 십수 분, 결정론이라 1회면 충분). 순환 우위가 나오면 그것도 그대로 보여줄 재료

**재현성이 준 선물.** 봇이 무상태 결정론이고 시작 위치가 시드로 고정되므로 **"승률"은 확률이 아니라 하나의 고정된 숫자**다. 몇 번을 돌려도 같은 값이 나온다. "표본이 충분한가", "우연인가" 같은 통계 논의가 통째로 사라지고 임계값만 정하면 된다.

**결정론이 연 새 구멍 — 시드 과적합.** 시드 집합이 고정되어 있으면 에이전트가 그 특정 시작 위치들에서만 통하는 수를 짜는 쪽으로 흐를 수 있고, 이건 G3 같은 관문으로 막히지 않는다. 그래서 시드를 두 벌로 나눈다.

- **심사 시드** — 승격 판정에 쓰고, 에이전트에게 공개
- **홀드아웃 시드** — 승격 후 기록용으로만 돌리고, 에이전트에게 노출하지 않음

두 승률의 격차가 곧 과적합 신호이고, **그 격차 자체가 발표에서 보여줄 하네스 엔지니어링 사례**가 된다.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 55% 기준 | 세대 수는 늘지만 개별 세대의 변화가 미미해 "이번 세대는 무엇을 배웠는가"를 설명하기 어려움 |
| 65% 기준 | 계단은 가장 극적이나 후반 수렴 시 루프가 계속 반려되며 세대가 멈출 위험 |
| 라운드로빈 1위를 승격 조건으로 | 순환 우위는 원천 차단되나 비용이 12배. 히트맵은 어차피 기록용으로 마지막에 돌리므로 이점을 흡수함 |

> §11-4에 따라 **이 60%는 이후 루프가 못 넘는다는 이유로 낮추지 않는다.**

---

### D9. 루프 구조와 재시도 한도

**결정.**

```
Gen N 챔피언
   │
   ├─▶ 에이전트가 Gen N+1 도전자 코드 작성
   │      │
   │      ├─▶ 관문 G1~G7  ──반려──┐
   │      │        │              │
   │      │      통과             │  반려 사유(기계 판정 결과)를
   │      │        │              │  근거로 재작성
   │      └─▶ 챔피언전 100경기 ───┤
   │               │              │
   │        60% 미만 ─────반려────┘
   │               │
   └───── 60% 이상 → 승격 → Gen N+1 챔피언
```

- **재시도 한도는 세대당 5회.** 초과하면 실험을 종료하고 "수렴"으로 선언한다.
- 반려된 시도는 **전부 남긴다**(§8).

**근거.** §11-4가 기준값을 낮추는 걸 금지했으므로, 5회를 못 넘으면 그게 곡선의 끝인 게 맞다. "이 지점에서 더 못 갔다"는 발표에서 오히려 정직하고 설득력 있는 결말이다.

---

### D10. 반려 피드백 — 반례 + 자동 진단 리포트

**결정.** 관문 실패 시에는 재현 가능한 **최소 반례 국면**(격자 상태 + 시드)을, 대전 실패 시에는 패배 경기에서 자동 추출한 **치명적인 수와 그 순간의 공간 손실**을 돌려준다.

**패배 원인 자동 진단기.** 매 턴 flood fill로 각 봇의 접근 가능 공간 크기를 계산해두면, 패배한 경기에서 **그 값이 가장 크게 무너진 턴**이 곧 치명적인 수다. 사람 눈이 아니라 코드가 짚어낸다(R2). 같은 계산이 발표 화면에서 "이 순간 봇이 졌습니다"를 하이라이트하는 데도 그대로 쓰이므로 **구현 비용을 한 번만 낸다**(C3).

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 판정 결과 요약만 | 구현은 가장 싸지만 에이전트가 거의 짐작으로 재작성하게 되어 재시도 횟수가 늘고 개선 속도가 느려짐 |
| 진단 리포트 + 리플레이 원본 전체 | 컨텍스트가 빠르게 차고, 에이전트가 일반 전략이 아니라 특정 경기에 맞추는 방향으로 흘러 시드 과적합 위험이 커짐 |

---

### D11. 발표 실행 모델 — 사전 생성 + 재생

**결정.** **발표 당일에는 아무것도 계산하지 않는다.** 세대 생성과 모든 경기는 사전에 끝내고 리플레이로 저장하며, 발표 화면은 그걸 재생만 한다.

**근거.** 결정론 덕분에 이건 손해가 아니다 — 라이브로 돌려도 **결과가 글자 하나까지 똑같기** 때문에 라이브가 주는 이점은 없고 위험만 있다(R4). 저장된 리플레이가 §8의 기록 자산으로도 그대로 남는다.

---

### D12. 스택 (C4)

**결정.**
- **백엔드 Spring Boot** — 게임 엔진, 관문 G1~G7, 루프 오케스트레이션, 진단기, 리플레이 생성 및 서빙
- **프론트엔드 Next.js(App Router) + TypeScript + Tailwind + Canvas** — 리플레이 재생과 시각화

---

### D13. 메인 화면 — 세대 갤러리

**결정.** 모든 세대의 경기를 같은 시드로 **동시에 재생**한다. 앞 셀들은 몇 초 만에 멈춰 회색으로 죽고, 뒤로 갈수록 계속 돌며 격자를 빼곡하게 채운다.

```
같은 시드, 전 세대 동시 재생 (경과 40턴 시점)

  Gen0  ✖ 6턴   Gen1  ✖ 14턴  Gen2  ✖ 23턴
  ┌──────┐      ┌──────┐      ┌──────┐
  │ ░░   │      │░░░░  │      │░░░░░░│
  │ ░    │      │░  ░  │      │░  ░░ │
  │      │      │░░░   │      │░░░ ░ │
  └──────┘      └──────┘      └──────┘
   (회색)         (회색)         (회색)

  Gen9  ▶ 진행   Gen11 ▶ 진행  Gen12 ▶ 진행
  ┌──────┐      ┌──────┐      ┌──────┐
  │▓▓▓▓▓▓│      │▓▓▓▓▓▓│      │▓▓▓▓▓▓│
  │▓┌──┐▓│      │▓┌─┐▓▓│      │▓┌─┐▓▓│
  │▓│@ │▓│      │▓│@│▓▓│      │▓│@│▓▓│
  └──────┘      └──────┘      └──────┘
   (살아있음)      (살아있음)      (살아있음)
```

**근거.** 설명 없이 30초면 "뒤로 갈수록 잘한다"가 보인다. **R3을 화면 하나로 증명한다.**

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| Gen 0 vs 최종 1:1 대비 | 메시지는 강렬하나 중간 세대가 안 보여 "점진적으로 개선되었다"는 루프의 서사가 약해짐 |
| 단일 아레나 + 세대 슬라이더 | 경기 한 판은 크게 보이지만 비교가 기억에 의존해 "설명 없이 지목 가능"이라는 R3 조건을 가장 약하게 만족 |

---

### D14. R3 검증 지표

**결정.** 화면에서 눈에 보이는 것과 코드가 재는 것이 **같은 양**이어야 한다. 갤러리에서 눈에 보이는 건 "얼마나 오래 버티는가"이므로 그걸 그대로 지표로 쓴다.

- **최종 챔피언 상대 평균 생존 턴 수** — 화면에서 패널이 멈추는 시점 그 자체
- **격자 점유율** — 화면에서 격자가 채워진 정도 그 자체
- **자멸률** — D6에서 관문에서 뺀 대신 지표로 남기기로 한 값

**합격선: Gen 0 대비 최종 세대의 평균 생존 턴 수 10배 이상.**
못 넘으면 기준을 낮추는 게 아니라 §7대로 세대를 더 돌린다.

---

### D15. 갤러리 공통 상대 — 최종 챔피언

**결정.** 역대 모든 세대가 **최종 챔피언**에게 도전하는 구도로 갤러리를 만든다.

**근거.** 생존 턴 수가 세대순으로 깔끔하게 단조 증가하고, 뒤로 갈수록 접전이 되면서 화면에 긴장이 생긴다. "전원이 최강자에게 덤빈다"는 서사도 설명이 필요 없다.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 약한 고정 상대(벽회피봇) | 중반 이후 세대가 전부 압승해 패널들이 서로 비슷해지고 개선 공선이 후반에 날아감 |
| 상대 없는 솔로 생존 주행 | 비교는 가장 깨끗하나 이건 대전 실력이 아니라 공간 채우기 능력이라, 대전용으로 진화한 봇이 오히려 솔로 성적은 나빠지는 역전이 나올 수 있음 |
| 직전 세대와의 대전 | 패널마다 상대가 달라 패널 간 비교가 불가능 |

---

### D16. 루프 실행 주체 — 생성은 하네스 밖, 평가는 하네스 안

**결정.** 경계선은 **봇 코드가 파일로 확정되는 순간**이다.

- **하네스 밖 (비결정론)** — Claude Code 같은 CLI 에이전트가 개발 시간에 봇 코드를 작성. 몇 번을 다시 써도 상관없음
- **하네스 안 (완전 결정론)** — Spring Boot의 엔진·관문·대전·진단·리플레이 생성. LLM을 일절 호출하지 않음

**근거.** **LLM 호출은 비결정론이다.** 봇 코드 생성을 Spring Boot 안에 넣는 순간 R1이 무너지고, R1이 무너지면 C2의 판정이 흔들린다.

**부수 효과 둘**
1. 발표 당일 **API 호출 0회** → R4가 공짜로 해결된다.
2. 발표 메시지가 정확해진다 — 이건 "제품 안에 AI를 넣었다"가 아니라 **"개발 과정에 루프를 넣었다"** 는 이야기이고, 그게 §1이 전하려는 메시지 그 자체다.

---

### D17. 세대 작성법 — 증분 개선

**결정.** 에이전트는 직전 챔피언을 출발점으로 새 전략 로직을 얹는다.

**근거.** 세대별 diff가 작고 선명해서 "이번 세대는 flood fill을 배웠다"를 한 문장으로 설명할 수 있고, **그 diff 자체가 발표 보조 화면**이 된다. 개선도 안정적으로 누적된다. 파라미터 튜닝이 아니라 전략 로직을 새로 쓰는 것이므로 §6과도 정합.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 매 세대 백지 재작성 | 세대 간 변화가 커서 "무엇이 달라졌는가"를 짚기 어렵고 반려 횟수가 늘어날 가능성이 큼 |
| 증분 기본 + 막힐 때만 백지 | 국소 최적 탈출 경로는 남지만 루프에 분기가 생겨 규칙이 늘고 발표에서 설명할 것이 하나 늘어남 |

---

### D18. 모듈 구조와 의존 방향 — *승인됨 (설계 섹션 1)*

```
arena-core         게임 엔진. 규칙·격자·턴 판정·리플레이 생성
                   의존: 없음 (순수 Java, Spring 없음)
                        ▲
       ┌────────────────┼────────────────┬──────────────────┐
       │                │                │                  │
arena-bots       arena-gate      arena-diagnostics    arena-tournament
Bot 인터페이스    관문 G1~G7       flood fill 진단기      대전·승격 판정
세대별 봇 구현    퍼징/벤치/검사    지표 계산            라운드로빈·기록 저장
베이스라인 봇          │                │                  │
       ▲───────────────┴────────────────┴──────────────────┘
                        │
                   arena-api
                   Spring Boot. 리플레이 서빙 + CLI 커맨드
```

**핵심 원칙 셋**
1. **`arena-core`는 Spring을 모른다.** 엔진이 순수 자바 함수라 테스트가 밀리초로 끝나고 R1 검증이 간단해진다. Spring은 `arena-api`에만.
2. **봇은 `arena-core`의 `GameView`와 `Direction`만 본다.** 엔진 내부도, 관문 구현도, 상대 봇도 타입 시스템상 접근할 수 없다 — "무엇을 감출 것인가"가 규율이 아니라 **모듈 경계로 강제**된다.
3. **의존은 단방향.** 관문은 봇을 알지만 봇은 관문을 모른다. 봇이 관문의 존재를 알면 관문을 겨냥해 코드를 쓸 수 있다.

**하네스의 두 얼굴.** C1은 규칙과 관문이 **문서와 코드**로 존재할 것을 요구한다. 코드 쪽은 `arena-gate`, 문서 쪽은 저장소 루트의 **`CLAUDE.md`(봇 작성 규칙서)**. 관문에서 반려된 항목은 규칙서에도 반영해 **문서와 관문이 같은 것을 말하도록** 유지한다.

**에이전트가 쓰는 명령은 셋뿐**
```bash
./gradlew gate      --bot=Gen07Bot   # 관문 G1~G7 → JSON 리포트
./gradlew challenge --bot=Gen07Bot   # 챔피언전 100경기 → 승격 판정 + 진단 리포트
./gradlew record                     # 최종 기록 생성 (갤러리·라운드로빈·리플레이)
```

---

### D19. 관문 실행 순서와 상세 — *승인됨 (설계 섹션 2)*

**결정.** `G1 → G7` 순서로 돌리고 **첫 실패에서 즉시 중단**한다. 비용 순서와 피드백 속도가 같은 방향이다.

| | 관문 | 판정 방법 | 합격 조건 | 반려 시 산출물 |
|---|---|---|---|---|
| G1 | 컴파일 | `compileJava` | 에러 0 | 컴파일러 메시지 원문 |
| G2 | 무상태 | 리플렉션 `getDeclaredFields()` | non-static 필드 0개 | 위반 필드명 목록 |
| G3 | 금지 API | ASM 바이트코드 스캔 | 위반 명령 0개 | 클래스·메서드·라인 위치 |
| G4 | 합법 수 | 국면 N개 퍼징 | 예외 0 · null 0 · 4방향 외 반환 0 | 최소 반례 국면 |
| G5 | 결정론 | 2회 호출 비교 + 리플레이 해시 | 100% 일치 | 불일치 국면 |
| G6 | 시간 예산 | 국면 N개 벤치 | p99 < X ms | 실측 p50/p99 |
| G7 | 회귀 방지 | 베이스라인 3종 대전 | **패배 0회** | 패배한 시드와 진단 |

**G3 금지 목록** — R1을 깨뜨릴 수 있는 것 전부.

```
시드 없는 난수   Math.random(), new Random()   // 시드 있는 Random은 허용
시계             System.currentTimeMillis/nanoTime, java.time.*
동시성           Thread, ExecutorService, ForkJoinPool
외부 세계        java.io.*, java.nio.file.*, java.net.*
전역 가변 상태   non-final static 필드, final이어도 가변 객체
우회             java.lang.reflect.*, Unsafe
```

**정정 — G7을 "전 시드 격파(전승)"에서 "패배 0회"로 바꿨다.**
사유: 랜덤봇 상대로는 정면 충돌 무승부가 구조적으로 발생할 수 있어, 전승을 요구하면 실력과 무관하게 반려된다. 무승부는 허용하고 패배만 금지한다. (D7의 초안을 대체)

---

### D20. 진단기 — "실수"를 기계가 정의한다

**결정.** 핵심 정의는 **손실(loss)** 하나다. 매 턴, 봇이 고를 수 있었던 방향들에 대해 각각 flood fill로 도달 가능 칸 수를 재고, 최선 대안과 실제 선택의 차이를 손실로 본다.

```
loss(t) = max_d reach(d 선택 후) − reach(실제 선택 후)

치명적인 수 = loss(t)가 최대인 턴
```

**이 정의 하나가 셋을 해결한다.**
- **반려 피드백** — 패배 경기에서 손실 상위 3개 수를 뽑아 에이전트에게 돌려준다(D10)
- **자멸률** — "즉시 죽지 않는 대안이 있었는데 죽는 방향을 고른" 턴의 비율. 위 계산의 부산물로 공짜(D14의 R3 지표)
- **발표 화면 하이라이트** — "이 순간 봇이 졌습니다"에 같은 값을 쓴다(C3)

구현 비용을 한 번만 낸다.

---

### D21. 리포트 스키마와 이력 보존

**결정.**

```jsonc
// 관문 반려
{ "generation": 7, "attempt": 3, "verdict": "REJECTED", "stage": "GATE",
  "failedGate": "G4",
  "counterexample": { "seed": 41, "turn": 63, "grid": "...", "myHead": [12,7],
                      "error": "ArrayIndexOutOfBoundsException: 30" } }

// 챔피언전 반려
{ "generation": 7, "attempt": 4, "verdict": "REJECTED", "stage": "CHAMPIONSHIP",
  "scoreRate": 0.48, "threshold": 0.60,
  "wins": 44, "draws": 8, "losses": 48,
  "diagnosis": [
    { "seed": 12, "turn": 87, "chose": "UP", "best": "LEFT",
      "reachBefore": 214, "reachAfter": 31, "loss": 183 } ] }
```

**이력 보존 구조** — 반려된 `bot.java`도 지우지 않는다(§8).

```
records/gen-07/
  attempt-1/  bot.java  gate-report.json                        (반려: G3 시계 사용)
  attempt-2/  bot.java  gate-report.json                        (반려: G4 인덱스 초과)
  attempt-3/  bot.java  gate-report.json  championship.json     (반려: 48%)
  attempt-4/  bot.java  gate-report.json  championship.json     (승격: 63%)
```

5회를 채우면 `CONVERGED`로 실험 종료(D9).

---

### D22. 재현 검증을 명령으로 만든다

**결정.** R1을 문서상의 약속이 아니라 **실행 가능한 검사**로 둔다.

```bash
./gradlew record --verify   # 전체 실험을 재실행해 리플레이 해시를 대조
```

모든 경기의 정규화된 이동 시퀀스를 SHA-256으로 해시해 저장해두고, 재실행 결과와 대조한다. 하나라도 다르면 실패다. **발표에서 R1을 주장할 때 이 명령의 출력이 곧 증거다.**

**부수 효과.** 결정론이므로 **경기들을 병렬로 돌려도 결과가 바뀌지 않는다.** 라운드로빈 수천 경기를 코어 수만큼 나눠 실행할 수 있다.

---

### D23. 확정 수치

| 항목 | 값 | 근거 |
|---|---|---|
| 격자 크기 | **30 × 30** | 최대 900턴 상한. 발표 화면에서 12개 패널로 나눠도 셀이 보임 |
| G4·G5·G6 국면 수 N | **10,000** | 실제 랜덤 대전에서 수집한 도달 가능 국면 |
| G6 상한 X (p99) | **5 ms** | flood fill 900칸은 수십 μs 수준이라 매우 넉넉. 100경기가 수 분 내에 끝남 |
| 심사 시드 | **50개** (× 스왑 = 100경기) | 에이전트에게 공개 |
| 홀드아웃 시드 | **50개** | 에이전트에게 비공개, 과적합 검출용 |
| 라운드로빈 시드 | **10개** (× 스왑 = 20경기/페어) | 히트맵용이므로 심사보다 가볍게 |

> §11-4에 따라 이 값들은 나중에 루프가 못 넘는다는 이유로 낮추지 않는다.

---

### D24. 기록을 두 층으로 나눈다 — *승인됨 (설계 섹션 3)*

**결정.**

| 층 | 위치 | 용도 | 규모 |
|---|---|---|---|
| **원본 기록** | `records/` | 개발 중 진단, 재현 검증, §8 이력 | 수천 경기 |
| **발표 번들** | `web/public/data/` | 화면이 실제로 읽는 정적 JSON | 수 MB |

발표 번들은 `./gradlew record`가 원본에서 추려 생성한다.

**근거.** 프론트엔드가 정적 파일만 읽으므로 **발표 당일 백엔드가 떠 있지 않아도 화면이 뜬다.** 서버 장애라는 위험 범주 자체가 사라진다(R4를 구조로 해결).

**리플레이 스키마**

```jsonc
{
  "schema": 1,
  "matchId": "gen07-vs-champion-seed12",
  "grid": { "w": 30, "h": 30 },
  "seed": 12, "swapped": false,
  "bots": [ { "id": "Gen07Bot", "start": [4,15],  "dir": "RIGHT" },
            { "id": "Gen12Bot", "start": [25,15], "dir": "LEFT"  } ],
  "moves": "RLRLRUUL…",                      // 턴당 2문자, 두 봇 인터리브
  "result": { "winner": 1, "turns": 187, "reason": "P0_HIT_OWN_WALL" },
  "metrics": {                                // 진단이 필요한 경기에만
    "reach": [[900,899,…],[900,898,…]],
    "loss":  [[0,0,3,…],[0,12,0,…]],
    "occupancy": [0.42, 0.38],
    "suicideRate": [0.00, 0.03] },
  "hash": "sha256:…"
}
```

`moves`를 문자열로 인코딩하면 **187턴 경기가 374바이트**다. 수천 경기를 저장해도 부담이 없어 **리플레이를 선별하지 않고 전부 남길 수 있다**(§8).

**발표 번들 구성**

```
web/public/data/
  gallery.json        세대별 대표 경기 — 최종 챔피언 상대, 전 세대 동일 시드
  generations.json    세대별 지표 시계열 (생존 턴 · 점유율 · 자멸률 · 승률 · 시도 횟수)
  loop-history.json   세대별 attempt 이력 (통과/반려, 반려 사유)
  roundrobin.json     히트맵 매트릭스
  sources/            세대별 bot.java 원문 + 직전 세대 대비 diff
```

---

### D25. 화면 서사 순서 — 결과 먼저, 과정 나중

**결정.** 한 페이지 스크롤 구성. 각 화면이 어떤 조건의 증거인지 매핑한다.

| | 화면 | 무엇을 말하는가 | 증거 |
|---|---|---|---|
| 1 | **세대 갤러리** (메인) | "뒤로 갈수록 잘한다" — 설명 없이 30초 | **R3** |
| 2 | **개선 곡선** | 눈으로 본 것을 숫자로 확인 (생존 턴 · 점유율 · 자멸률) | R3, R2 |
| 3 | **루프 타임라인** | "어떻게 이렇게 됐나" — 세대별 시도를 O/X로, 반려 사유별 색 | **C2** |
| 4 | **세대별 코드 diff** | "이번 세대는 무엇을 배웠나" — 한 문장으로 읽히는 변화 | **C1**, §6 |
| 5 | **단일 경기 + 진단** | 기계가 실수를 어떻게 짚었나 — loss 최대 턴 하이라이트 | C2, R2 |
| 6 | **히트맵 · 과적합 격차** | 순환 우위와 심사/홀드아웃 승률 차 | 부록 |

**근거.** §1이 "과정이 주인공"이라 했지만, 과정을 먼저 설명하면 비개발자가 따라올 이유가 없다. **결과로 먼저 붙잡고 과정으로 데려간다.**

3번이 발표의 진짜 주인공이다. **반려된 시도들이 빨간 칸으로 줄줄이 늘어선 화면**이 "루프가 돌았다"의 가장 직접적인 증거다(§8이 반려 이력을 지우지 말라 한 이유).

---

### D26. 연출과 렌더링 (C3)

**결정.**
- **라이트사이클 트레일** — 최근 지나온 칸일수록 밝고 뒤로 갈수록 어둡게. 정지 화면에서도 진행 방향이 읽힌다
- **사망 순간** — 흰 플래시 한 프레임 → 패널 전체 회색조 전환. 어느 패널이 죽었는지 주변시로도 잡힌다
- **패널 하단 생존 턴 카운터** — 죽으면 그 값에서 고정. 갤러리가 그대로 순위표가 된다
- **색은 청/주황** — 색각 이상에서도 구분되고 프로젝터 대비에 강하다
- **발표자 컨트롤** — 재생/정지/속도/처음으로. 자동 재생에 끌려다니지 않는다

**렌더링 비용은 문제가 되지 않는다.** 트론은 벽이 영구적이라 **캔버스를 지울 필요가 없다.** 매 프레임 새로 그리는 셀은 봇당 1칸, 12패널을 합쳐도 24칸이다. 10,800셀을 매번 다시 그리는 게 아니다.

---

### D27. 테스트 전략 — 하네스를 테스트하는 하네스 — *승인됨 (설계 섹션 4)*

**출발 질문.** 하네스가 곧 판정 인프라다. 그러면 **하네스는 무엇이 판정하나?** 답이 없으면 C1 전체가 신뢰를 잃는다.

**T1 — 엔진 규칙 (arena-core) · TDD 대상.** 여기가 틀리면 이후 모든 판정이 무의미하다. 예제 테스트에 더해 속성 테스트 셋을 건다.

```
대칭성    임의의 봇 A, B와 임의의 시드에 대해
          play(A, B, seed)의 승자 == play(B, A, seed)의 승자를 뒤집은 것
          → D2의 "A/B를 바꿔도 결과가 같다"를 기계가 검증
종료 보장  임의의 봇 조합에 대해 경기는 900턴 이내에 반드시 끝난다
벽 단조성  W(t) ⊆ W(t+1) 이고 |W(t+1)| − |W(t)| == 생존 봇 수
```

**T2 — 관문 (arena-gate) · 함정 봇 스위트.** 각 관문마다 **일부러 그 관문을 위반하는 봇**을 fixture로 두고, 관문이 정확히 그 봇을 반려하는지 검증한다.

| 함정 봇 | 위반 | 반려해야 할 관문 |
|---|---|---|
| `StatefulTrap` | 인스턴스 필드 보유 | G2 |
| `ClockTrap` | `System.nanoTime()` 사용 | G3 |
| `CrashTrap` | 특정 국면에서 예외 | G4 |
| `NondeterministicTrap` | `Object.hashCode()` 순서에 의존 | G5 |
| `SlowTrap` | 의도적으로 무거운 연산 | G6 |
| `WeakTrap` | 벽회피봇에게 지는 수준 | G7 |

발표에서 "관문이 진짜 작동하는지 어떻게 압니까?"에 대한 답이 이 스위트이고, **C1의 가장 강력한 증거**다. 관문을 추가하면 함정 봇도 함께 추가하는 것을 규칙으로 둔다.

**T3 — 진단기 · TDD 대상.** 손으로 만든 5×5 격자에서 `loss` 값이 손계산과 일치하는지 검증한다. 진단기가 틀리면 반려 피드백이 에이전트를 잘못된 방향으로 끈다.

**T4 — 재현성 · CI.** `./gradlew record --verify`(D22)를 CI에서 돌린다. **R1이 테스트로 존재한다.**

**T5 — 프론트엔드.** 발표 번들 JSON의 스키마 계약 테스트만 둔다. 시각 회귀 테스트는 YAGNI로 뺀다 — 화면은 사람이 보면서 만든다.

---

### D28. 베이스라인 봇 3종 — 절대 수정 금지

**결정.** `arena-bots`의 `baseline` 패키지에 고정하고, **한번 커밋한 뒤로는 수정하지 않는다.**

| 봇 | 정의 | 대략적 강도 |
|---|---|---|
| `StraightBot` | 항상 현재 방향 유지 | 벽에 박아 죽음 |
| `RandomBot` | 시드 있는 균등 4방향 | 후진 자멸로 평균 ~4턴 |
| `WallAvoidBot` | 즉시 죽지 않는 방향 중 고정 우선순위로 선택 (1수 앞만 봄) | 위 둘을 압도 |

**근거.** 기준이 움직이면 세대 간 비교가 무의미해진다. G7(D7)과 R3 배수 계산이 모두 이 셋에 의존한다.

---

### D29. Gen 0 = `StraightBot`

**결정.** 챔피언 계보의 출발점을 베이스라인의 최하단과 일치시킨다.

**`RandomBot`을 기각한 이유 둘**
1. **화면에서 읽힌다.** 직선 하나가 쭉 그려지다 벽에서 멈추는 그림은 "얘는 아무 생각이 없다"가 즉시 전달된다. 랜덤봇은 4턴 만에 뒤엉켜 죽어서 관객이 볼 시간조차 없다.
2. **R3 합격선을 더 엄격하게 만든다.** 중앙 근처 시작 기준 직진봇은 약 15턴을 산다. D14의 "10배" 기준이 **150턴**이 된다. 랜덤봇(~4턴)을 기준선으로 삼으면 40턴만 넘겨도 통과라 기준이 무의미해진다.

---

### D30. 시드 생성과 좌석 교대

**결정 — 좌석 교대(side swap)의 정확한 의미.** 위치를 미러링하는 게 아니라 **같은 두 위치에 봇 배정만 바꾼다.**

```
시드 s  →  위치 쌍 (P1, P2)
  1판:  A@P1  vs  B@P2
  2판:  B@P1  vs  A@P2      ← 위치는 그대로, 앉는 봇만 교대
```

**근거.** 미러링하면 격자가 비대칭일 경우 이점이 상쇄되지 않는다. 좌석 교대는 어떤 격자에서도 정확히 상쇄된다. (D2·D8에서 "좌우 스왑"이라 적었던 표현을 이것으로 확정)

**위치 생성 규칙**
```
· 가장자리에서 최소 3칸 안쪽      (초반 즉사 방지, 어느 방향이든 최소 3턴 보장)
· 두 위치 간 맨해튼 거리 ≥ 10     (초반 즉시 접촉 방지)
· 초기 방향은 시드에서 4방향 균등
· 심사 시드 = 1‥50,  홀드아웃 시드 = 1001‥1050   (범위로 분리, 겹칠 여지 없음)
```

---

### D31. 목표 세대 수 — 12세대 (상한 아님)

**결정.** 목표 12세대. 갤러리가 3×4로 화면에 정확히 들어맞고, 예상되는 개선 계단(무작위 → 벽 회피 → 1수 예측 → flood fill → Voronoi → 관절점 → 상대 수 읽기)이 7단계 정도라 여유가 있다.

**단, 12는 목표이지 상한이 아니다.** §7이 "시간이 남으면 기능을 늘리지 말고 세대를 더 돌린다"고 했으므로 **갤러리는 N개 패널을 받도록 구현**해 16세대까지 가면 4×4로 자연스럽게 늘어나게 만든다.

---

## §10 항목 결정 현황

BRIEF §10이 "대화로 정할 것"으로 비워둔 항목이 모두 결정되었다.

| §10 항목 | 결정 | 위치 |
|---|---|---|
| 종목과 규칙 | 트론 라이트사이클, 동시 판정 | D1, D2 |
| 봇에게 무엇을 보여주고 감출 것인가 | 전지 시점 + 무상태 | D5 |
| 승격 기준과 그 수치 | 승점 승률 60%, 100경기 | D8 |
| 관문 목록과 각 기준값 | G1~G7 | D6, D7, D19, D23 |
| 루프의 재시도 한도와 실패 처리 | 세대당 5회, 초과 시 CONVERGED | D9, D10 |
| 기록 포맷 | 두 층 분리, 리플레이 스키마 | D21, D24 |
| 화면 구성 | 세대 갤러리 + 보조 5화면 | D13, D15, D25, D26 |
| 모듈 구조와 의존 방향 | 6개 모듈, core 무의존 | D18 |
| R3을 어떤 수치로 검증할 것인가 | 생존 턴 수 10배 | D14, D29 |

---

### D32. 구현을 세 계획으로 나눈다

**결정.** 스펙이 독립적으로 동작·테스트 가능한 세 덩어리를 담고 있어 계획을 분할했다.

| | 계획 | 산출물 | 의존 |
|---|---|---|---|
| **1** | **하네스 (백엔드)** | 엔진·관문·진단·대전·기록. 그 자체로 완결되고 CLI로 검증 가능 | 없음 |
| 2 | 시각화 (프론트엔드) | Next.js 화면 6종 | 1의 발표 번들 |
| 3 | 세대 루프 실행 | Gen 1~12 실제 개선 | 1, 2 |

**근거.** 계획 3은 코드 작성이 아니라 하네스를 **돌리는** 운영 절차다. 1이 완성되어 실제 반려 양상을 본 뒤에 쓰는 것이 맞다.

계획 1은 `docs/superpowers/plans/2026-08-19-arena-harness.md`에 작성했다 — 21개 태스크, 112개 스텝.

---

### D33. 계획을 쓰다가 발견해 고친 것 넷

설계를 코드 수준으로 내리면서 드러난 문제들이다. 설계 단계에서는 보이지 않았다.

**1. `arena-core`가 `arena-bots`에 의존할 뻔했다.**
엔진이 `Bot` 인터페이스를 받으면 의존이 역류한다. `BotFunction` 함수형 인터페이스를 `arena-core`에 두어 끊었다. 엔진은 "방향을 내는 함수"만 알면 되고 `Bot`이라는 개념을 몰라도 된다.

**2. `arena-gate`가 `arena-tournament`에 의존할 뻔했다.**
G7이 시리즈 대전을 필요로 하는데 그게 tournament에 있으면 형제 모듈 간 의존이 생긴다. 좌석 교대 실행기(`SeriesRunner`)를 `arena-core`로 옮겼다. 순수 엔진 기능이므로 core가 제자리다.

**3. G3와 G5의 책임이 겹쳤다.**
`NondeterministicTrap`이 비결정론을 만들려면 `hashCode` 계열을 써야 하는데, 그게 G3 금지 목록에도 있어 G5에 도달하기 전에 걸린다. 그러면 G5를 시험할 수단이 없어진다.
→ 계획에 조건부 지침을 넣었다. 함정이 G3에서 먼저 걸리면 `Object.hashCode`를 G3 목록에서 빼고 G5에게 맡긴다. **결정론 위반은 G5의 책임이고, G3는 R1을 깨는 호출을 막는 역할**이라는 경계를 분명히 했다.

**4. 스펙 §7.1의 `reachBefore`/`reachAfter`가 오해를 부른다.**
시간 순서(수를 두기 전/후)로 읽히지만 실제 의미는 최선 대안과 실제 선택의 reach다. **`reachIfBest`/`reachChosen`으로 바꾸고 스펙 §7.1도 갱신했다.**

---

### D34. superpowers를 저장소에 벤더링

**결정.** `obra/superpowers` v6.3.0의 스킬 14종을 `.claude/skills/`에 **파일 그대로 복사해 커밋했다.** 마켓플레이스 선언 방식과 플러그인 통째 벤더링은 둘 다 실측으로 기각했다.

**근거.** 계획 문서가 `superpowers:subagent-driven-development`, `superpowers:executing-plans`를 실행 지침으로 지목한다. 그 스킬이 개인 머신 설정에만 있으면 다른 사람이나 다른 세션에서 계획이 그대로 돌아가지 않는다. 하네스가 저장소 안에 있어야 한다는 C1의 요구는 스킬 자체에도 똑같이 적용되고, 세션마다 설치 명령이 필요한 구조는 R4(발표 중 라이브 환경 의존 금지)에도 걸린다.

**측정.** 세 방식을 모두 만들어 놓고, 브랜치를 그대로 체크아웃한 **새 세션**에서 스킬이 실제로 뜨는지 물어 판정했다. 두 번 뒤집혔다.

| 방식 | 새 세션에서 자동 로드 | 스킬 이름 | 훅 |
|---|---|---|---|
| `.claude/settings.json`에 마켓플레이스 선언 | **✗** | `superpowers:...` | ✓ |
| `.claude/skills/superpowers/`에 플러그인 통째로 | **✗** (신뢰 수락한 로컬에서만 ✓) | `superpowers:...` | ✓ |
| **`.claude/skills/`에 스킬 파일만** | **✓** | `brainstorming` (접두사 소실) | ✗ |

1안이 실패하는 이유는 `settings.json`이 **선언일 뿐 설치가 아니기** 때문이다. 설치 기록은 사용자 레벨 `~/.claude/plugins/installed_plugins.json`에 **프로젝트 경로별로** 남고 저장소를 따라가지 않는다. 새 세션의 자체 판정이 `superpowers plugin declared but not installed`였다.

2안이 실패하는 이유는 **프로젝트 스코프 플러그인이 워크스페이스 신뢰(trust)를 수락하기 전까지 억제되기** 때문이다. 신뢰를 수락시킨 로컬에서는 `superpowers:brainstorming`이 정상으로 떴지만, 매번 새 컨테이너로 뜨는 세션에서는 그 조건이 성립하지 않아 스킬이 하나도 안 떴다.

3안만 신뢰 여부와 무관하게 떴다.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 사용자 스코프(`~/.claude`)에 설치 | 저장소를 클론한 다른 사람에게 전달되지 않음. 재현 가능성이 사람마다 달라짐 |
| `.claude/settings.json`에 마켓플레이스 선언 | 선언만으로는 설치되지 않는다. 컨테이너가 매번 새로 뜨는 환경에서는 매 세션 설치 명령 반복 |
| `.claude/skills/superpowers/`에 플러그인 통째로 | 접두사와 훅을 살리지만 신뢰 수락 전에는 억제된다. 새 세션에서 하나도 뜨지 않았다 |

**대가.** 셋을 다 재고 나서 남은 것은 접두사와 훅을 잃는 안뿐이었다.

- 스킬 이름이 `subagent-driven-development`처럼 접두사 없이 등록된다. 계획 문서의 `superpowers:` 표기는 접두사를 뗀 나머지가 이름이라고 읽으면 된다
- SessionStart 훅이 빠진다. 훅은 플러그인에만 붙고, 플러그인이 되는 순간 신뢰 조건이 다시 걸린다
- 업스트림 갱신이 수동이 되고 저장소에 남의 코드 550KB가 들어온다. 갱신 절차와 고정한 커밋 해시는 `.claude/README.md`에 적었다

**남긴 것.** 이 판단은 "선언이 아니라 상태를 저장소에 넣는다"는 C1의 축소판이다. 마켓플레이스 선언은 그럴듯해 보였고 첫 커밋에서 실제로 그걸 택했지만, 재어 보니 동작하지 않았다. 기각 사유가 취향이 아니라 측정인 사례로 발표에 쓸 수 있다.

`.gitignore`에는 `.claude/settings.local.json`만 넣었다. 공유 설정은 커밋하고 개인 설정은 커밋하지 않는다.

---

### D35. Task 5 브리프의 참조 구현·테스트에서 결함 둘을 찾아 고쳤다

**배경.** Task 5 브리프는 `Match.java`와 `MatchTest.java`를 "그대로 쓰라"며 완성 코드로 제공했다. TDD RED 확인 후 GREEN 단계에서 6개 중 2개가 실패했다. `arena-core`가 프로젝트의 심장이라는 지시를 받았으므로, 실패를 지우고 넘어가지 않고 원인을 끝까지 추적했다.

**결정 1 — 반전(reversal) 사망이 수학적으로 불가능했다. 시작 칸 바로 뒤 칸도 벽으로 만들어 고쳤다.**

브리프 코드는 시작 칸 `p0` 하나만 벽으로 만든다. `Point.move(Direction)`은 항상 새 칸으로 이동하고 자기 자신으로 되돌아오는 경우가 없으므로, `p0.move(아무_방향)`은 절대 `p0`와 같을 수 없다 — 4방향 전부 실측으로 확인했다(`Probe4`). 즉 "시작 칸을 벽으로 만들면 후진이 자기 벽에 부딪힌다"는 브리프의 주석과 테스트 이름(`시작_칸이_벽이라_후진하면_자기_벽에_박는다`, `turns()==1` 기대)은 **이 구현으로는 어떤 시드를 넣어도 성립할 수 없다.** 브리프가 준 시드 3으로 실행하면 실제로는 `P0_OUT_OF_BOUNDS`가 7턴 만에 나왔다(반대 방향으로 계속 달려 결국 가장자리에 닿음).

고친 방법: 시작 칸뿐 아니라 **시작 칸 바로 뒤 칸**(현재 방향의 반대쪽 인접 칸, `head.move(dir.opposite())`)도 초기화 시점에 벽으로 만든다. 봇이 이미 방향 `d`를 향한 채로 게임을 시작한다고 보면, 그 방향으로 한 칸 오기 직전 칸은 이미 자기 궤적의 일부라는 뜻이다. 이러면 정확히 반대 방향으로의 첫 이동이 그 칸에 부딪혀 자기 벽 충돌로 죽는다 — 여전히 "후진 감지"라는 별도 규칙 없이, 초기화 시점의 벽 배치만으로 자연히 나온다.

**결정 2 — 결정 1의 수정이 시드 7의 다른 테스트(`벽에_박은_쪽이_진다`)와 충돌해, 시드를 1로 바꿨다.**

`claimBehind`를 추가하자 시드 7에서 `always(UP)` 봇이 첫 수부터 죽었다(`P0_HIT_OWN_WALL`, 1턴). 원인: 시드 7에서 0번의 초기 방향 `d0`이 우연히 `DOWN`이었고, `always(UP)`의 첫 수(UP)가 정확히 `d0`의 반대 — 즉 진짜 "반전"과 구조적으로 동일한 상황이었다. 이건 버그가 아니라 결정 1의 수정이 **의도한 대로** 동작한 것이다 — 봇이 "반전을 의도했는지"는 엔진이 알 방법이 없고 알 필요도 없다, 좌표상 반대 방향으로의 첫 이동은 전부 같은 결과여야 한다(재현성·규칙의 단순성).

다만 이 테스트의 목적은 반전이 아니라 "격자 밖으로 나가서 짐" 경로를 검증하는 것이므로, `d0`이 `UP`이 아닌 시드로 바꿔야 원래 의도(여러 턴을 달려 가장자리에 닿음)가 살아난다. 시드 1(`d0=RIGHT`, 20턴 후 `P0_OUT_OF_BOUNDS`)로 교체했다.

**결정 3 — "순서 의존성 없음" 테스트는 손대지 않았다. 다만 그 테스트가 검증하는 것과 브리프가 주장하는 것이 다르다는 걸 실측으로 확인해뒀다.**

이 테스트는 같은 시드로 좌석(0번/1번) 배정을 바꿔 `avoid()` vs `always(UP)`를 실행하고, 승자(아이디 기준)가 같은지 본다. 결정 1을 적용하기 전에는 시드 25, 33에서 어긋났다. 원인을 진짜 순서 편향(엔진이 봇0을 봇1보다 유리하게 처리)인지 확인하려고 매 턴 "스왑 통과"(양쪽이 서로의 현재 칸으로 동시에 교차하는데 미검출되는 경우) 여부까지 턴 단위로 추적했다(`Probe6`) — 없었다. 실제 원인은 `p0`, `p1`이 무작위로 독립 배치되어 벽까지 거리·형태가 다르고, `avoid()`는 고정 우선순위(RIGHT>DOWN>LEFT>UP)라 위치를 반사해도 행동이 반사되지 않는다는 것. 즉 **"좌석을 바꿔도 승자가 같다"는 이 테스트의 전제는, 무작위 비대칭 배치 + 위치 의존적 봇이 있는 한 어떤 올바른 엔진에서도 일반적으로 성립할 수 없다** — 엔진 버그가 아니라 조합의 문제.

결정 1의 수정(반전 시 죽음)을 적용하면 공교롭게 시드 1–50 구간에서는 전부 일치했다(빈 우연이 아닌지 시드 500개까지 넓혀 재확인 — 402, 412, 423에서 다시 어긋남을 확인했다, `Probe9`). 브리프가 요구하는 범위(1–50)는 통과하므로 테스트는 **수정하지 않고 그대로 뒀다.** 다만 이 한계를 여기 적어 둔다: 이 테스트가 나중에 시드 범위를 넓히거나 다른 봇 조합으로 재사용되면 다시 실패할 수 있다. 이건 테스트가 약하다는 신호이지 엔진이 순서에 의존한다는 신호가 아니다.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| id 문자열을 정렬해 좌석을 결정론적으로 배정 (`"avoid" < "back"`처럼) | `시작_칸이_벽이라_후진하면_자기_벽에_박는다` 테스트가 깨진다 — 그 테스트는 인자 순서상 첫 번째 봇이 `p0`/`d0`을 받는다고 전제한다 |
| 순서 테스트를 `headOnForTest`처럼 결정론적 추격(chase) 시나리오로 다시 설계 | 결정 1을 적용하니 브리프가 준 범위(시드 1–50)에서 그대로 통과해 불필요해졌다. 대신 그 한계(500 시드까지 넓히면 재현됨)를 D35에 남겨 나중에 재사용될 때 참고하게 했다 |
| `벽에_박은_쪽이_진다`의 기대 사유를 `P0_HIT_OWN_WALL`로 바꿔 시드 7을 그대로 씀 | 이 테스트가 유일하게 `P0_OUT_OF_BOUNDS` 경로를 검증한다 — 사유를 바꾸면 그 커버리지가 사라진다 |

**검증.** `Probe4`(반전 불가능성 실측), `Probe7`/`Probe8`(시드 7·1의 `d0`, 새 사유 확인), `Probe6`(스왑 통과 미검출 확인), `Probe9`(순서 테스트를 시드 500까지 넓혀 한계 재확인). `./gradlew :arena-core:test` 19개 전부 통과(`DirectionTest`, `GridTest`, `MatchTest` 6개, `StartPositionsTest`).

**남긴 것.** Task 5 브리프가 "정확한 값을 그대로 쓰라"고 못박았지만, 실행해보지 않고는 드러나지 않는 결함이었다. 코드를 눈으로만 읽었으면 통과했을 리뷰다 — RED/GREEN을 실제로 돌리고, 실패를 우연이라 넘기지 않고 원인을 끝까지 추적한 결과다.

---

### D36. Task 5 리뷰 반려 — D35의 판단은 확정, `Match.java`를 세 군데 더 고쳤다

**결과.** D35의 세 판단(`claimBehind` 추가, 시드 7→1 교체, 순서 테스트 보류)은 리뷰에서 스펙 §2.2/§7.1/§12를 근거로 전부 확정됐다 — 뒤집힌 것 없음. 대신 `Match.java` 자체에서 세 가지를 더 고치라는 지시를 받았다.

**1. 클래스 javadoc이 이미 반증된 문장을 게시하고 있었다.** "A와 B를 바꿔도 결과가 같다"는 좌석 교대로 읽히는데, 이건 D35가 이미 증명한 반례(시드 25/33, 나중에 500 시드까지 넓혀 402/412/423도 확인)로 거짓이다. `Match.java`는 하위 전부가 읽는 파일이라 방치할 수 없었다. 실제로 보장되는 성질(한 턴 안에서 봇0/봇1 중 누구를 먼저 묻든 결과가 같다 — `W(t)` 고정 + 동시 판정)로 다시 쓰고, 좌석은 시드로부터 독립적으로 뽑히므로 대칭이 아니라는 것(스펙 §2.2)을 명시했다.

**2. `headOnForTest`가 판정 로직의 두 번째 사본이었고, 이미 갈라져 있었다.** `dead0`/`dead1` 판정식을 그대로 다시 적었고, `claimBehind`를 호출하지 않아 진짜 규칙을 따르지 않았다 — `Match.java`의 판정식이 내일 바뀌어도 그 테스트는 계속 통과했을 것이다. `playResult`를 시드 기반 공개 진입점과, 배치를 직접 받는 패키지 전용 오버로드(`playResult(BotFunction, BotFunction, StartPositions, int, int)`)로 나눴다. 공개 진입점은 이 오버로드로 위임하므로 판정 루프가 하나뿐이다. `headOnForTest`는 삭제했다. 덤으로 리뷰가 짚은 잠재 버그도 같이 없앴다 — `headOnForTest`는 `width`를 인자로 받으면서 `x=10`/`x=16`을 하드코딩해 `width≤17`이면 범위 검사가 없는 `Grid.claim`이 격자 밖에 썼다. 새 정면 충돌 테스트는 `width/2±3`을 써서 폭에 무관하게 안전하다.

**3. 첫 커밋 메시지가 유일한 실질적 편차(`claimBehind`)를 언급하지 않았다.** `--amend`는 쓰지 않았다(이미 푸시됨, CLAUDE.md §2는 기록을 다시 쓰지 않고 덧붙이는 게 원칙). 수정 커밋 메시지에 `claimBehind`를 이름으로 밝히고, 시작 칸 하나만으로는 왜 안 되는지(`Point.move(d)`는 항상 새 칸으로 이동하므로 시작 칸만 벽이면 첫 반전이 그 칸으로 돌아올 수 없다)를 적었다.

**겸사겸사.** `P1_*` 사유·`BOTH_DIED`·`*_HIT_OPPONENT_WALL`이 어떤 테스트에도 닿지 않고 있었다(`reasonFor()`의 거울 분기가 뒤바뀌어도 그린이 나오는 구조). `P1_HIT_OWN_WALL`(반전 봇을 1번 자리에 둔 거울 테스트)과 `BOTH_DIED`(양쪽 다 반전, 서로 다른 칸) 두 개를 추가했다. `claimBehind`의 범위 밖 무시와 `BotFunction`의 `null` 반환 시 NPE는 지시대로 손대지 않고 유보로 남겼다.

**검증.** `./gradlew :arena-core:test` 21개 전부 통과(기존 19 + 신규 2) — `--rerun`으로 캐시 무시하고 재확인.

---

### D37. Task 6 브리프의 속성 하나가 거짓이었다 — 컨트롤러 룰링으로 교체

**배경.** Task 6 브리프는 `속성_대칭성_좌석을_바꿔도_승자가_같다`를 시드 500개로 요구했다: 같은 시드로 두 봇의 좌석(0번/1번)을 바꿔 돌리고 승자가 같은지 본다. D35(순서 의존성 결정 3)에서 이미 시드 1–50은 우연히 다 맞고 500까지 넓히면 402/412/423에서 어긋난다는 걸 실측해뒀다. 이번엔 그 관찰을 다시 확인하는 대신, 컨트롤러가 직접 "이 속성은 구현하지 말라"고 룰링했다 — 실패할 뿐 아니라 **스펙이 부정하는 성질**을 검사하는 테스트이기 때문이다.

**왜 거짓인가.** 스펙 §2.1이 요구하는 순서 독립성은 "한 턴 안에서 W(t)를 고정한 채 봇0을 먼저 묻든 봇1을 먼저 묻든 결과가 같다"는 것이다. "좌석을 바꾸면 같은 경기가 반복된다"는 뜻이 아니다. 스펙 §2.2가 명시한다: 좌석 교대는 "위치는 그대로, 앉는 봇만 바꾼다(미러링이 아니다)". 두 좌석의 시작 위치·방향은 시드로부터 각자 독립적으로 뽑히므로, 좌석을 바꾸면 두 봇이 받는 시작 조건 자체가 통째로 달라진다 — 애초에 다른 경기다. 게다가 `claimBehind`(D35) 이후로는 구체적 메커니즘까지 있다: 고정 방향만 내는 봇은 자기 좌석의 시작 방향이 정반대일 때 턴 1에 죽는데, `d0`/`d1`이 독립적으로 뽑히므로 좌석을 바꾸면 그 봇이 받는 방향도 바뀐다 — 두 실행이 "같은 경기를 두 번 관찰"하는 게 아니라 넷 중 하나 꼴로 한쪽이 1턴짜리 경기가 돼버린다. 이 속성이 성립했다면 Task 14의 좌석 교대 메커니즘 자체가 무의미했을 것이다.

**교체.** `MatchPropertyTest.java`에 스펙 §2.1을 실제로 고정하는 속성 두 개를 넣었다.

1. `속성_동시_사망은_언제나_무승부다` — 자기 시작 방향의 정반대로만 움직이는 봇(`reverse()`)을 양쪽에 앉히면 `claimBehind` 덕분에 시드와 무관하게 항상 턴 1에 서로 다른 칸에서 동시 사망한다(`BOTH_DIED`). 시드 500개 전부에서 이 경로를 강제로 재현해 매번 `winner == -1`인지 확인하고, 재현 횟수가 정확히 500인지도 단언한다(공허 검사 방지). 덧붙여 `avoid()`/`hugLeft()`/`always(UP)`/`reverse()` 조합으로 시드 1–100 × 16조합(1,600경기)을 자연 실행해, 우연히 나오는 동시 사망(`BOTH_DIED` 184건 + `HEAD_ON_COLLISION` 143건 = 327건)에서도 전부 무승부인지 확인했다.
2. `속성_같은_칸_동시_진입은_인덱스와_무관하게_무승부다` — 시드에 기대지 않고 package-private `playResult(BotFunction, BotFunction, StartPositions, int, int)`로 정면 충돌을 직접 구성한다. 원래 배치(왼쪽 봇=0번, 오른쪽 봇=1번)와, 그 자리에 앉는 봇 인덱스를 뒤바꾼 배치 둘 다 `HEAD_ON_COLLISION` + `winner == -1`인지 확인한다 — 어느 인덱스를 먼저 평가하든 같은 칸 동시 진입은 무승부라는 것.

**`MatchTest.판정은_순서에_의존하지_않는다` 처리.** 다시 쓰지 않고 지웠다 — 그 자리에 왜 지웠고 대신 무엇이 §2.1을 고정하는지 가리키는 주석만 남겼다. 다시 쓰는 대신 지운 이유: 이 테스트가 검증하던 "좌석을 바꿔도 승자가 같다"는 애초에 참인 성질이 아니라서, 같은 이름 아래 다른 주장으로 바꿔치기하면 나중에 누가 "이 테스트가 뭘 보장했었지"를 되짚기 어렵다. §2.1의 진짜 내용은 `MatchPropertyTest`의 위 두 속성이 이미 갖고 있으므로 중복해서 남겨둘 이유도 없었다.

**겸사겸사 발견한 결함 둘 (브리프 코드 자체).**
- `속성_벽_단조성` 브리프 코드는 `for (long seed = ...)` 루프 변수를 관찰자 람다 안에서 그대로 참조해 컴파일이 안 됐다(effectively final 위반). `final long s = seed;`로 복사해서 고쳤다.
- 같은 테스트의 `previous[0] = 2`("시작 칸 2개")는 `claimBehind`를 빼먹었다. 실제로 턴 1 시작 전 벽은 이미 4칸(시작 칸 2 + 그 바로 뒤 칸 2)이라, 그대로 두면 시드 1 턴 1에서 `expected: 4 but was: 6`으로 실패한다. `4`로 고쳤다. 이건 속성의 주장(벽은 줄지 않고 매 관찰 턴마다 2칸씩 는다)을 바꾸는 게 아니라, 브리프가 Task 5의 `claimBehind` 도입 전에 쓰인 상수를 그대로 물려받은 것으로 보인다.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 브리프대로 좌석 대칭성 속성을 그대로 구현 | 실패가 확정적이다(시드 402/412/423). 실패를 알면서 커밋하는 건 R1(재현 가능성)과 무관하게 그 자체로 거짓 스펙을 코드에 박아넣는 일이다 |
| `판정은_순서에_의존하지_않는다`를 같은 이름으로 남기고 내용만 §2.1 진짜 주장으로 바꿔치기 | `MatchPropertyTest`의 두 속성과 내용이 겹친다. 하나의 사실을 두 곳에서 다른 이름으로 주장하면 나중에 어느 쪽이 원본인지, 어느 쪽을 고쳐야 하는지 헷갈린다 |
| 동시 사망 속성을 `avoid()`/`hugLeft()`만으로, 강제 유발 없이 자연 발생에만 의존 | 시드 범위 안에서 몇 건이나 걸릴지 실행 전엔 모른다 — 0건이면 공허한 테스트가 된다. `reverse()`로 500개 시드 전부에서 강제 재현하는 걸 주로 쓰고, 자연 발생은 보너스 확인으로만 붙였다 |

**검증.** `./gradlew :arena-core:test` 26개 전부 통과(기존 19 + 신규 4 `MatchPropertyTest` − 1 삭제 `MatchTest.판정은_순서에_의존하지_않는다` + 3 나머지 `MatchTest` 조정 없음 = 순증 4). 스크래치 프로그램(`Stats.java`, 저장소 밖)으로 각 속성의 전제 발생 횟수를 별도 실측: 동시 사망 강제 500/500, 자연 발생 327/1,600, 종료 보장 1,600경기, 벽 단조성 관찰 콜백 16,767회 — 넷 다 공허하지 않음을 확인.

**기록 정정 (D37 본문은 CLAUDE.md §2에 따라 고치지 않는다).** 위 검증 문단의 산수 "기존 19 + 신규 4 − 1 삭제 = 순증 4"는 두 군데 틀렸다. D36이 끝난 시점(커밋 `215f217`)의 실제 테스트 개수를 세어 보면 `DirectionTest` 5 + `GridTest` 5 + `StartPositionsTest` 5 + `MatchTest` 8 = **23개**다(19가 아니다). 순증도 신규 4 − 삭제 1 = **+3**이다(+4가 아니다). 23 + 3 = 26으로, "총 26개"라는 결론 자체는 맞았다 — 도출 과정(기존 개수와 순증 값 둘 다)만 틀렸다.

---

### D38. Task 6 리뷰 반려 — 벽 단조성 훅이 자기 이름을 증명하지 못했고, 관찰자가 살아있는 grid를 그대로 받고 있었다

**결과.** D37의 판단(대칭성 속성 대체, 두 신규 속성의 비-동어반복성, `previous[0]=4`·`final long s` 수정)은 리뷰에서 전부 확정됐다. 대신 세 가지를 더 고치라는 지시를 받았다.

**1. `속성_동시_사망은_언제나_무승부다`의 카운터가 죽은 코드였다.** `assertEquals(SEEDS, guaranteedDoubleDeaths)`는 루프 본문 맨 끝에서 무조건 증가하는 카운터를 검사한다 — 안에서 어떤 단언이든 실패하면 그 실패로 테스트가 이미 끝나므로, 이 등식이 실패할 수 있는 경로가 없다. 지웠다. 진짜 전제 검사는 매 반복의 `assertEquals(DeathReason.BOTH_DIED, ...)`였다. 반대로 자연 발생 절반(`organicDoubleDeaths`)에는 발생 횟수를 세기만 하고 바닥을 안 걸어놨었다 — `assertTrue(organicDoubleDeaths > 0, ...)`를 추가해 327건(실측)을 저장소가 스스로 강제하는 사실로 만들었다. 리뷰가 짚은 아이러니: guard가 필요 없는 쪽엔 guard를 달고, 필요한 쪽엔 안 달았다.

**2. `TurnObserver`가 엔진의 살아있는 `Grid` 인스턴스를 그대로 받고 있었다.** `Grid.claim(Point,int)`는 public이고 범위 검사도 없다 — 관찰자(미래의 리플레이 레코더·시각화·부주의한 테스트 무엇이든)가 실수로든 의도적으로든 판정 중인 엔진 상태를 훼손할 수 있는 구멍이었다. `Grid.wallSnapshot()`이 봇에게 방어적 복사본만 주는 것과 정합성이 안 맞았다. 브리프가 지정한 시그니처였지만 리뷰가 오버라이드했다: `observer.onTurn`에 `grid`가 아니라 `grid.copy()`를 넘긴다. 다만 관찰자가 없는(내부 no-op) 경로에서는 복사조차 만들지 않는다 — `observer != NO_OP_OBSERVER` 참조 비교로 챔피언전 같은 대량 무관전에서 턴마다 격자를 복사하는 비용을 물지 않게 했다.

**3. "생존자 수만큼"이 자기 이름이 주장하는 걸 검증한 적이 없었다.** 원래 엔진은 `resolve()`를 매치를 끝내는 턴에 어떤 `grid.claim`보다 먼저 호출했다 — 즉 한쪽만 죽든 둘 다 죽든, 그 턴엔 아무도 벽으로 확정되지 않았다. `observer.onTurn`도 같은 이유로 매치를 끝내는 턴엔 절대 불리지 않았다(둘 다 생존해야 도달하는 코드 경로에만 있었다). 결과: 벽 단조성 테스트는 "둘 다 생존한 턴은 +2"만 100개 시드에서 검증했고, 한쪽만 죽는 턴의 +1(스펙 §7.1 `|W(t+1)|-|W(t)| == 생존 봇 수`)은 저장소 어디에서도 실제로 확인된 적이 없었다.

이건 테스트만의 문제가 아니라 **엔진 자체의 스펙 위반**이었다. 스펙 §2.1의 `W(t+1) = W(t) ∪ {생존한 봇의 새 머리 좌표}`는 매치를 끝내는 턴에 예외를 두지 않는다. `Match.java`를 고쳤다: 판정(`dead0`/`dead1` 계산, §2.1의 W(t) 고정 규칙 그대로)은 손대지 않고, 그 판정 *다음에* — `resolve()`로 반환하기 전에 — 생존한 쪽의 새 좌표를 `grid.claim`한다. 죽은 쪽의 목표 좌표는 여전히 확정되지 않는다(정면 충돌처럼 둘 다 죽는 경우 판정식이 대칭이라 `dead0`·`dead1`이 항상 함께 true이므로, 죽은 쪽 좌표가 실수로 벽이 될 위험도 없다). 이 김에 `observer.onTurn`도 매치를 끝내는 턴을 포함해 매 턴 부르도록 옮겼다 — 원래 인터페이스 문서("턴이 끝날 때마다 호출된다")가 이미 그렇게 약속하고 있었는데 구현이 그 약속을 어기고 있었다.

이 수정으로 벽 단조성 테스트를 다시 썼다: 관찰자가 수집한 (턴, 벽 개수) 목록을 매치 종료 후 검사해, 마지막 턴이 아니면 +2, 마지막 턴이면 무승부일 때 +0·단독 승자일 때 +1인지 확인한다. 관찰자 호출 횟수가 `r.turns()`와 정확히 같은지도 확인한다(이게 곧 "매 턴 불렸다"의 비공허성 증거다). 리뷰가 요구한 "관찰자 밖에서 확인하는" 별도 시나리오도 추가했다 — 반전 봇(시드와 무관하게 항상 턴 1에 자기 벽으로 죽는다) vs `avoid()`로 한쪽만 죽는 상황을 결정론적으로 20개 시드에서 구성하고, 관찰자로 값을 받아오되 단언은 람다 밖(테스트 본문)에서 한다: 시작 전 4칸 + 생존자 1칸 = 정확히 5칸.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 벽 단조성 훅을 여전히 "둘 다 생존한 턴에만" 불리게 두고, 이름만 그 범위에 맞춰 축소(`...둘_다_생존한_턴은...`) | 리뷰가 "close it rather than just renaming"이라고 못박았다. 이름을 좁히는 건 스펙 §7.1 위반을 감추는 것이지 고치는 게 아니다 |
| 관찰자가 매치를 끝내는 턴엔 안 불리는 채로 두고, "한쪽만 죽음" 시나리오는 `MatchResult`(승자·사유)만으로 검증 | `MatchResult`는 벽 개수를 담지 않는다 — 승자가 누군지는 알아도 그 턴에 벽이 실제로 몇 칸 늘었는지는 별도 접근 경로 없이는 확인할 수 없다. 이게 바로 이 결함이 지금까지 숨어 있었던 이유다 |
| `grid.copy()` 대신 `Grid`에 읽기 전용 뷰(인터페이스)를 새로 만들어 관찰자에게 넘김 | 범위가 이 리뷰가 요구한 것보다 크다 — `Grid`의 공개 API를 바꾸는 건 `GridTest`·`GameView`에도 영향이 번진다. `copy()`는 이미 있고 이 리뷰의 요구(관찰자가 훼손 못 하게)를 정확히 충족한다 |

**검증.** `./gradlew :arena-core:test --rerun` 27개 전부 통과(기존 26 + 신규 1 `속성_벽_단조성_한쪽만_죽은_턴은_벽이_1칸만_는다`). 스크래치 프로그램을 Match.java 수정 후 다시 돌려 재확인: 벽 단조성 관찰 콜백이 16,767 → 16,867(시드 100개 × 매치를 끝내는 턴 1개씩 늘어난 만큼 정확히 +100), 자연 발생 동시 사망은 327로 불변(그 값은 `MatchResult`만으로 정해지므로 벽 클레임 타이밍 수정과 무관).

**남긴 것.** "이 훅으로는 절대 안 불리는 코드 경로가 있다"는 걸 자기 검증(non-vacuity) 삼아 확인하려던 시도가, 실은 그 코드 경로 자체가 스펙을 어기고 있었다는 걸 드러냈다. 공허성 점검이 버그 헌팅으로 이어진 사례라 발표에 쓸 만하다.

---

### D39. Task 7 — 리플레이 인코딩과 해시. 브리프의 리팩터 레시피가 낡아 있어 컨트롤러 룰링대로 적용

**배경.** `Match.java`는 D35~D38을 거치며 `playResult`가 네 개 오버로드로 갈라진 상태였다(공개 2개 + 시드 위임, 패키지 전용 2개 + `StartPositions` 직접 지정 — Task 6의 속성 테스트가 뒤의 것으로 결정론적 배치를 구성한다). Task 7 브리프는 "`playResult(…, observer)`의 본문 전체를 `playInternal`로 옮긴다"고 적었는데, 이는 오버로드가 하나였던 옛 구조를 가리킨다. 브리프 텍스트를 그대로 옮기면 네 오버로드 중 실제 턴 루프를 담고 있던 것(4번째: `playResult(BotFunction, BotFunction, StartPositions, int, int, TurnObserver)`)의 본문만 옮기면 되고, 나머지 세 오버로드의 시그니처와 위임 관계는 그대로 둬야 한다고 판단했다(컨트롤러 룰링 그대로 적용).

**적용한 룰링 둘.**
1. **턴 루프는 하나만 남긴다, 하지만 네 시그니처 전부 보존.** 4번째 `playResult` 오버로드의 본문(그리드 초기화 ~ 턴 루프 ~ `resolve`)을 새 `private static playInternal(BotFunction, BotFunction, StartPositions, int, int, TurnObserver, StringBuilder moves)`로 옮겼다. `moves.append(d0.code()).append(d1.code())`는 `p0`/`p1` 계산 직후, `dead0`/`dead1` 판정 이전에 넣어 치명적인 마지막 이동도 기록되게 했다(브리프 요구 그대로). 4번째 `playResult`는 `playInternal(..., new StringBuilder())`로 위임해 moves를 버린다. 3번째(NO_OP 위임)·2번째(시드→`StartPositions.of`)·1번째(NO_OP 위임) 오버로드는 이전과 동일한 본문·시그니처를 유지한 채 결국 이 사슬을 통해 `playInternal`에 도달한다. 새 `play`도 직접 `playInternal`을 호출한다(`StartPositions.of`를 자신이 뽑아서 `Replay`에도 심어야 하므로 `playResult`를 한 번 더 감싸지 않았다).
2. **`play`는 `NO_OP_OBSERVER` 센티널 자체를 넘긴다, 브리프의 새 람다 `(t,g,h)->{}`가 아니라.** `playInternal`은 `observer != NO_OP_OBSERVER` 참조 비교로 `grid.copy()` 여부를 결정한다(D38에서 도입). 브리프대로 새 람다를 쓰면 이 참조 동일성이 깨져 `play`가 호출될 때마다(챔피언전 등 대량 호출 경로) 매 턴 30×30 그리드를 복사하게 된다 — `play`는 관찰자가 필요 없는 정확히 그 경로이므로, 센티널을 그대로 넘겨 D38이 만든 비용 절감을 무너뜨리지 않았다.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 브리프 텍스트를 문자 그대로 따라 옛 두-오버로드 구조로 되돌림 | Task 6의 속성 테스트가 3·4번째(패키지 전용, `StartPositions` 직접 지정) 오버로드를 호출한다 — 삭제하거나 시그니처를 바꾸면 기존 27개 테스트 중 일부가 깨진다 |
| `play`가 `playResult(..., observer)` 공개 오버로드를 거쳐 위임 | `play`는 `Replay`에 `sp`(시작 배치)를 직접 담아야 하는데, 공개 오버로드는 `StartPositions`를 자기 내부에서만 뽑고 밖으로 내보내지 않는다. 직접 `playInternal`을 호출하는 편이 `StartPositions.of`를 두 번 계산(공개 오버로드 안에서 한 번, `play` 안에서 한 번)하지 않아도 된다 |
| `play`가 새 람다 관찰자를 넘김(브리프 원문) | 컨트롤러 룰링으로 명시 오버라이드됨 — 위 2번 |

**검증.** RED: `./gradlew :arena-core:test --tests '*ReplayTest*'` — `Replay`·`Match.play` 심볼 없음으로 16개 컴파일 에러(예상대로: 아직 아무것도 구현 안 함). GREEN: `./gradlew :arena-core:test --rerun` — 33개 전부 통과(기존 27 + 신규 `ReplayTest` 6개). `Match.java`에 턴 루프가 `playInternal` 안에 정확히 하나뿐임을 육안 확인, `arena-core/build.gradle`은 의존성 0줄로 무변경.

---

### D40. Task 8 — flood fill과 손실 분석. `Match.initialGrid` 추출, 그리고 브리프 코드에서 실제 결함 발견

**배경.** Task 8 브리프의 `LossAnalyzer.replayToFinalGrid`(그리고 `replayAndAnalyze`)는 시작 격자를 `grid.claim(head[0], 0); grid.claim(head[1], 1);` 두 줄로 재구성했다. 하지만 실제 엔진(`Match.playInternal`)은 시작 칸 2개뿐 아니라 그 바로 뒤 칸 2개까지, 총 4칸을 벽으로 확정한 채로 매치를 시작한다(D35의 `claimBehind` — 첫 턴 반전을 자기 벽 충돌로 만들기 위한 장치). 이 규칙을 `arena-diagnostics`에 다시 베끼면 두 사본이 갈라지는 사고가 재발한다 — `MatchTest`의 `headOnForTest`가 판정식을 복사했다가 갈라졌던 D36의 반복이다.

**적용한 룰링: `Match.initialGrid(StartPositions, int, int)`를 public API로 추출.** `playInternal`의 그리드 초기화 네 줄(시작 칸 2개 claim + `claimBehind` 2번)을 그대로 옮기고, `playInternal`은 이 메서드를 호출하도록 바꿨다. 설명 주석도 함께 옮겼다. 순수 추출이라 동작 변화가 없어야 한다는 게 전제였고, 이를 두 가지로 확인했다: (1) 기존 `arena-core` 33개 테스트가 리팩터 후에도 전부 그대로 통과, (2) `initialGrid`를 직접 겨냥한 신규 테스트 두 개(벽이 정확히 4칸인지, 시작 위치에서 후진하면 그 칸이 이미 자기 벽인지)를 `MatchTest`에 추가해 35개 전부 통과. `LossAnalyzer`의 `replayAndAnalyze`·`replayToFinalGrid`는 브리프의 두 줄짜리 재구성 대신 `Match.initialGrid`를 호출한다.

**자체 검토 중 브리프 코드 자체의 결함을 하나 찾았다.** `replayToFinalGrid`(그리고 `replayAndAnalyze`)의 턴 루프는 `dead0`/`dead1`을 판정한 뒤 곧장 `if (dead0 || dead1) break;`로 빠져나가고, 그 다음에야 `grid.claim(p0, 0); grid.claim(p1, 1);`을 실행했다 — 즉 매치를 끝내는 턴에는 **누구의 좌표도 claim되지 않는다**. 그런데 실제 엔진은 D38 이후로 "매치를 끝내는 턴이라도 생존한 쪽의 새 좌표는 벽으로 확정한다"(스펙 §2.1 `W(t+1) = W(t) ∪ {생존한 봇의 새 머리 좌표}`, 생존 봇 수만큼 벽이 는다는 §7.1 벽 단조성과 동치)는 규칙으로 이미 고쳐져 있다. 한쪽만 죽고 한쪽은 살아남는 매치(예: 이번 테스트의 `suicide` vs `avoid`)에서 재구성한 최종 격자는 생존자의 마지막 한 칸을 놓치고, 그만큼 `occupancy`가 조용히 틀어진다.

두 함수 모두 `grid.claim`을 판정 직후·break 이전으로 옮겨 `playInternal`과 순서를 맞췄다(생존한 쪽만 claim, 죽은 쪽은 그대로 둔다 — 양쪽 다 죽는 경우는 판정식이 대칭이라 항상 함께 죽으므로 안전). 회귀 테스트(`점유율의_분자합은_엔진이_실제로_낸_최종_벽_칸수와_같다`)를 추가했다: `Match.playResult`의 `TurnObserver`로 엔진이 실제로 낸 최종 벽 칸수를 잡고, 같은 시드·같은 봇으로 얻은 `Replay`를 `LossAnalyzer.analyze`에 넣어 `occupancy` 배열의 합에서 역산한 칸수와 비교한다. 고치기 전 코드로 되돌려 이 테스트가 실제로 실패하는 것(FAILED, `LossAnalyzerTest.java:105`)까지 확인한 뒤 고친 코드로 복구했다 — 공허한 회귀 테스트가 아니다.

**손실 계산(`analyzeMove`)은 "같은 시점의 보드"에서 네 방향 모두를 평가한다.** 네 후보 방향 전부 이 턴 시작 시점의 `grid` 스냅샷을 `copy()`해서 독립적으로 평가하므로, "실제 선택 vs 최선 대안"이 서로 다른 시점의 보드를 비교하는 일이 없다. `FloodFill.reach`는 재귀가 아니라 `ArrayDeque` 기반 BFS다 — 30×30=900칸이 전부 열려 있어도 스택을 먹지 않는다.

**의도적으로 손대지 않은 것: 상대방과의 동시 진입(정면 충돌).** `analyzeMove`가 어떤 방향이 "안전한지"를 판단할 때는 이 턴 시작 시점의 벽만 본다 — 상대가 이번 턴에 어디로 움직일지는 (실제로 선택하지 않은 대안 방향에 대해서는) 알 수 없으므로 반영하지 않는다. 그래서 만약 실제 선택이 정면 충돌로 죽는 경우, 그 방향 자체는 "안전"으로 잡혀 `suicide=false`가 될 수 있다. 이건 결함이 아니라 이 지표의 정의 범위다 — `suicide`는 "대안이 있는데도 이미 확정된 벽(자기 벽이든 상대 벽이든)에 스스로 들이받았는가"를 재는 것이고, 정면 충돌은 양쪽이 동시에 만든 사건이라 한쪽의 "자멸"로 귀속시키는 게 애초에 부정확하다. `loss`는 이 경우에도 항상 0 이상을 유지한다(선택한 방향도 네 후보 중 하나로 포함되므로).

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 브리프의 `replayToFinalGrid`를 문자 그대로 구현(시작 칸 2개만 claim) | 룰링 자체가 이걸 금지한다 — 재구성한 보드가 턴 0부터 엔진과 어긋나 모든 지표가 조용히 틀려진다 |
| 정면 충돌도 `suicide=true`로 잡히게 `analyzeMove`를 확장(상대의 실제 이번 턴 이동을 미리 반영) | 범위 밖이다. 이 함수는 "그 방향으로 갔을 때 내가 얼마나 갇히는가"를 재는 단일 에이전트 지표이지, 상대의 실제 수를 아는 사후 판정이 아니다. 게다가 대안 방향 3개에 대해서는 상대가 실제로 무엇을 했을지 알 방법이 없어(반사실이므로) 정면 충돌 판정을 일관되게 적용할 수도 없다 |

**검증.** RED: `./gradlew :arena-core:test`(`Match.initialGrid` 심볼 없음, 2개 컴파일 에러) → `./gradlew :arena-diagnostics:test`(`FloodFill`·`MatchMetrics`·`LossAnalyzer`·`MoveAnalysis` 심볼 없음, 15개 컴파일 에러). GREEN: `Match.initialGrid` 구현 후 `./gradlew :arena-core:test --rerun` 35개 전부 통과(기존 33 + 신규 2). `arena-diagnostics` 구현 후 `./gradlew :arena-diagnostics:test --rerun` 10개 전부 통과(브리프 9개 + 회귀 테스트 1개). `./gradlew test --rerun`(전체) — `arena-bots` 7 + `arena-core` 35 + `arena-diagnostics` 10 = 52개 전부 통과, `arena-gate`·`arena-tournament`·루트·`arena-api`는 소스 없음(NO-SOURCE). 커밋 `88dac8e`.

---

### D41. Task 8 리뷰 반려 — 턴별 재구성이 여전히 이중화돼 있었고, 정면 충돌이 "안전한 수"로 보였다

**배경.** D40의 판단(`Match.initialGrid` 추출, 생존자 claim 결함 발견·수정)은 리뷰에서 전부 확정됐다 — 순수 추출 검증, 진단이 복사가 아니라 호출로 규칙을 따르는 것, loss의 대칭성, flood fill의 반복 구현, 회귀 테스트의 비공허성(엔진의 `TurnObserver` 출력과 직접 대조하지 상수와 비교하지 않음) 전부 리뷰가 확인했다. 대신 두 가지를 더 고치라는 지시를 받았다.

**1. 턴별 재구성 규칙이 `replayAndAnalyze`와 `replayToFinalGrid` 두 곳에 그대로 남아 있었다.** D40의 룰링은 *초기 격자* 규칙의 중복만 없앴을 뿐, *턴 진행* 규칙(초기화 → 사망 판정 → 생존자만 claim)은 여전히 두 함수에 복제돼 있었다. 그리고 이 diff 자체가 그 위험이 살아있다는 증거였다 — D40에서 찾은 생존자 claim 결함을 실제로 두 곳(`:98-99`와 `:176-177`)에서 각각 고쳐야 했다. `replayAndAnalyze`가 `MoveAnalysis` 목록과 매치 종료 시점의 최종 `Grid`를 함께 담는 private record `ReplayAnalysis(List<List<MoveAnalysis>> perBot, Grid finalGrid)`를 반환하도록 바꾸고, `replayToFinalGrid`를 통째로 지웠다. `analyze`는 이제 이 한 번의 재생에서 두 산출물을 모두 얻는다 — 재생 루프가 파일 안에 정확히 하나뿐이라 부수적으로 리플레이를 두 번 훑던 것도 없어졌다.

**2. 정면 충돌(HEAD_ON_COLLISION)로 죽은 수가 loss 계산에서 "안전한 수"로 잡혔다.** `isSafe`는 이 턴 시작 시점의 벽만 본다 — 상대가 이번 턴에 같은 칸으로 동시 진입한다는 사실은 그 시점의 벽 집합엔 없으므로, 정면 충돌로 죽은 실제 선택의 `reachAfterChosen`이 양수로 나오고 `loss`는 거의 0이 되며 `suicide`는 false로 남는다. `HEAD_ON_COLLISION`은 엔진이 실제로 내는 사망 사유인데, 이 경기에서 `worstMoves`(반려 피드백이 읽는 바로 그 값)가 실제로 진 턴이 아니라 무관한 이전 턴을 "최악의 수"로 뽑아버리는 구체적 비용이 있었다. 브리프에서 물려받은 결함이라 리뷰가 직접 룰링했다.

**적용한 룰링 네 가지.**
1. **사실을 명시적으로 담는다.** `MoveAnalysis`에 `fatal` 필드를 추가했다 — 재생 루프가 이미 계산해 둔 `dead0`/`dead1`(자기 벽·상대 벽·격자 밖·정면 충돌 전부 포함)을 그대로 옮긴다. *대안* 방향에 대해서는 반사실이 필요하지만(상대가 그 상황에서 무엇을 했을지 모른다), *실제로 선택한* 방향에 대해서는 리플레이가 상대의 진짜 수를 담고 있고 엔진이 이미 그 수를 사망으로 판정했으므로 반사실이 필요 없다.
2. **`loss`에 접지 않는다.** `reachAfterChosen`을 0으로 죽이면 완전한 정보를 가진 실제 선택(사후에 알고 보니 죽었다)과 벽만 보고 계산한 대안들을 비대칭으로 비교하게 돼, 더 나은 수가 없었는데도 양의 손실을 보고할 수 있다. `loss`는 대칭을 그대로 유지한다.
3. **`suicideRate`에 넣지 않는다.** 스펙의 자멸률은 "그 자체로 치명적인 방향을 골랐다"(고정된 보드에서 봇 스스로 알 수 있는 것)를 잰다. 정면 충돌은 상대의 이번 턴 동시 선택에 달려 있고, 스펙 §2.1상 어떤 봇도 그 선택을 미리 볼 수 없다 — 자멸로 돌리면 스펙이 정의한 값을 부풀린다. 이 결정은 `log.md`뿐 아니라 `MoveAnalysis`의 javadoc에도 적었다(미래의 독자가 먼저 만나는 곳이므로).
4. **`worstMoves`가 실제로 진 턴을 반드시 뽑는다.** 정렬 키를 `fatal` 우선, 그 다음 `loss`로 바꿨다 — `Comparator.comparing(MoveAnalysis::fatal).thenComparingInt(MoveAnalysis::loss).reversed()`. `limit`이 1이어도 실제로 경기를 끝낸 수가 밀려나지 않는다.

**곁들여 해소한 불일치.** `MatchMetrics.reach[bot][사망턴]`이 죽은 봇에게 양의 reach를 그리고 있었다. 사망 턴엔 0으로 정했다 — 죽은 봇에게 남은 공간은 없다는 게 이 배열의 용도(화면에 실제로 남은 공간을 그린다)에 맞다. `MoveAnalysis.reachAfterChosen`은 `loss`의 대칭을 지키려고 반사실 값을 그대로 유지하므로 둘이 사망 턴에 다른 값을 낸다 — 왜 다른지 `MatchMetrics`·`MoveAnalysis` 양쪽 javadoc에 적었다.

**곁들여 고친 것 둘 (지시받은 대로, 값싼 것들).**
- `Match.initialGrid`의 javadoc이 "합쳐서 항상 4칸"이라고 임의의 `StartPositions`에 대해 잘못 단언하고 있었다 — `claimBehind`는 격자 밖 뒤 칸을 조용히 건너뛰므로, `StartPositions`의 공개 정준 생성자로 가장자리에 봇을 두면 4칸 미만일 수 있다. "최대 4칸, `StartPositions.of`는 MARGIN(≥3) 덕분에 항상 4칸"으로 정정.
- `점유율은_자기_벽_칸수를_전체로_나눈_값이다`가 총합만 검사해, `LossAnalyzer.analyze`의 owner==0/owner==1 카운팅 루프가 뒤바뀌어도 안 걸렸다. `Match.playResult`의 `TurnObserver`로 엔진이 실제로 낸 0번 소유 칸수를 잡아 `occupancy()[0] * cells`와 대조하는 단언을 추가했다.

**유보로 남긴 것 (지시받은 대로 손대지 않음).** `worstMoves`가 `loss == 0`인 항목으로 패딩하는 것과 `limit`을 검증하지 않는 것, `FloodFill.reach`가 격자 밖 머리에 대해 예외를 던지는 것, `arena-diagnostics/build.gradle`의 중복된 `testImplementation` 줄, 봇 0만 겨냥한 길이 단언.

**정면 충돌을 재현한 방법.** `Match.play`는 시드 기반 공개 진입점만 있어(`StartPositions`를 직접 지정하는 오버로드는 `arena.core` 패키지 전용) 시드를 하드코딩하는 대신, "상대 쪽으로 곧장 다가가는" 봇(`towardOpponent` — 주축 우선, 막히면 부축, 그마저 막히면 아무 안전한 방향) 두 개를 맞붙여 시드 1..500을 스캔해 `HEAD_ON_COLLISION`이 나오는 첫 시드를 찾는다. 같은 범위를 스캔하므로 매번 같은 시드가 나온다(R1). 그 시드의 리플레이에서 두 봇 모두 `worstMoves(..., 1)`이 실제 사망 턴을 1위로 뽑는지, `fatal=true`·`suicide=false`인지, `MatchMetrics.reach`가 그 턴에 0인지 확인한다.

**결함 재현 증거.** 두 수정 모두 되돌린 뒤 새 테스트가 실제로 실패하는 것을 확인했다: (1) `replayAndAnalyze`/`replayToFinalGrid` 이중화 결함은 D40에서 이미 재현·복구 확인됨(변경 없음, 재확인만). (2) `fatal`을 정렬 키에서 빼고 `loss`만으로 정렬하면 `정면_충돌로_죽은_수는_fatal이지만_suicide는_아니다`가 `LossAnalyzerTest.java:195`에서 FAILED — 복구 후 재확인 GREEN.

**검증.** `./gradlew :arena-diagnostics:test --rerun` 11개 전부 통과(기존 10 + 신규 정면 충돌 테스트 1, 점유율 테스트는 단언 추가라 개수 불변). `./gradlew :arena-core:test --rerun` 35개 전부 통과(Match.java는 javadoc만 수정, 동작 무변경). `./gradlew test --rerun`(전체) — `arena-bots` 7 + `arena-core` 35 + `arena-diagnostics` 11 = **53개 전부 통과**. 커밋 `aadee9d`.

---

### D42. Task 9 — 함정 봇 스위트

**결정.** `arena-gate` 모듈을 새로 만들고, 관문(G1~G7)이 아직 하나도 없는 상태에서 관문이 잡아야 할 함정 봇 8종과 대조군 `CleanBot`을 `arena-gate/src/test/java/arena/gate/traps/`에 먼저 심었다. 브리프(`task-9-brief.md`)가 파일 내용을 그대로 못 박았으므로 전부 그대로 옮겼다 — 변형 없음.

| 함정 | 어긴 규칙 (단 하나) | 잡는 관문 |
|---|---|---|
| `StatefulTrap` | 인스턴스 필드(`callCount`)를 갖는다 | G2 |
| `ClockTrap` | `System.nanoTime()`으로 시계를 읽는다 | G3 |
| `UnseededRandomTrap` | 시드 없는 `new Random()` | G3 |
| `CrashTrap` | 아래 가장자리에서 배열 범위 밖 접근 | G4 |
| `NondeterministicTrap` | `new Object().hashCode()`(아이덴티티 해시)에 의존 | G5 |
| `SlowTrap` | 한 수에 4천만 회 루프를 돌려 시간 예산을 넘긴다 | G6 |
| `WeakTrap` | 직진만 해서 위생 관문은 통과하되 벽회피봇에게 진다 | G7 |
| `CleanBot` | (없음 — 대조군) | 모든 관문 통과 |

**TDD 순서에 관한 판단.** 브리프의 Step 순서는 "함정 봇 8종 작성 → `TrapSanityTest` 작성 → 테스트 통과 확인"이고, 함정 봇의 코드 자체가 브리프에 완전히 명시돼 있어 테스트가 구현을 이끄는 고전적 RED→GREEN 사이클이 성립하지 않는다(테스트가 먼저 있어도 대상 클래스가 없으면 컴파일조차 안 되므로 "실패하는 테스트"를 독립적으로 관찰할 지점이 없다). 대신 이 테스트는 "함정이 진짜로 함정인가"를 사후 검증하는 성격이라 브리프가 정한 순서(구현 먼저, 검증 나중)를 그대로 따랐다. `./gradlew :arena-gate:test`가 5개 전부 GREEN으로 통과하는 것을 확인했다(RED 단계 없음 — 위 이유).

**자체 검토에서 확인한 것.**
- 함정 8종 각각 정확히 한 규칙만 어긴다. `ClockTrap`이 시계를 읽는 부작용으로 결과가 실행마다 달라지는 것은 G3 위반의 *결과*이지 별도의 G5 위반으로 세지 않는다 — G3가 먼저 그 호출 자체를 막을 것이기 때문. `NondeterministicTrap`의 `Object.hashCode()`가 G3 금지 목록(`java.lang.reflect.*`류 우회 API)과 부딪힐 수 있다는 점은 태스크 지시문이 미리 조정해 둔 내용이라 손대지 않았다.
- `CleanBot`은 `PRIORITY` static final 배열 하나만 필드로 갖고(리플렉션으로 non-static 필드 0개 확인하는 테스트로 고정), 금지 API 호출 없음, 항상 유효한 `Direction`을 반환한다(모든 좌표에서 확인하는 테스트로 고정).
- `TrapSanityTest`는 `StatefulTrap`(리플렉션으로 인스턴스 필드 존재 확인), `CrashTrap`(실제로 `ArrayIndexOutOfBoundsException` 던지는지 확인), `NondeterministicTrap`(100,000회 반복 호출 중 다른 답이 실제로 나오는지 확인), `CleanBot`(모든 위치에서 유효한 방향 반환 + 인스턴스 필드 없음) 넷을 행동으로 검증한다. `ClockTrap`·`UnseededRandomTrap`·`SlowTrap`·`WeakTrap`은 브리프가 별도 sanity 단언을 요구하지 않았다 — 코드만 봐도 위반이 자명하고("무인자 생성자만 잡혀야 한다"는 주석처럼 미묘한 경계가 있는 경우가 아니면), 이 넷의 진짜 검증은 각자를 잡을 관문(G3·G3·G6·G7)이 나중 태스크에서 직접 한다. 브리프 범위를 넘겨 추가 단언을 붙이지 않았다("정확히 브리프가 명시한 것만" 원칙).
- `arena-gate`의 프로덕션·테스트 코드 어디도 `arena.bots.baseline.*`(동결된 베이스라인 3종)을 import하지 않는다. 의존 방향도 브리프가 준 `build.gradle` 그대로다 — `arena-gate`가 `arena-core`·`arena-bots`·`arena-diagnostics`를 의존하고, 그 반대는 없다. 관문 코드는 이번 태스크에 없다(브리프·태스크 지시문 모두 "관문 없음"을 명시).

**검증.** `./gradlew :arena-gate:test` 5개 GREEN. `./gradlew test --rerun` 전체 58개 GREEN(기존 53 + 신규 5), 빌드 출력 오류·경고 없음. 커밋 `1683d0c`.

---

### D43. Task 10 — 관문 프레임워크와 G2(무상태)

**결정.** `arena-gate`에 처음으로 `src/main` 코드를 추가했다. `Gate`(인터페이스), `GateResult`(record), `GateContext`(record), `StatelessGate`(G2) 넷 다 브리프(`task-10-brief.md`)가 준 코드를 그대로 옮겼다. TDD 순서는 브리프대로: `GateContextFixture`·`StatelessGateTest` 먼저 작성 → `./gradlew :arena-gate:test --tests '*StatelessGateTest*'`로 RED 확인(`Gate`/`GateResult`/`GateContext`/`StatelessGate` 네 심볼이 없어 컴파일 자체가 실패, 정확히 브리프가 예고한 실패) → 구현 → 같은 명령으로 GREEN 확인(4개 통과).

**자체 검토에서 찾은 구멍과 그 자리에서 고친 것.** 태스크 지시문이 "상속된 필드를 올바르게 처리하는가"를 명시적으로 묻길래 확인해봤다. `Class#getDeclaredFields()`는 **그 클래스 자신에 선언된 필드만** 돌려준다 — 슈퍼클래스 필드는 안 보인다. 즉 브리프에 적힌 코드 그대로는, 봇이 `abstract class LeakyBase implements Bot { private int hiddenCounter; ... }`를 만들고 `class SneakyBot extends LeakyBase`로 상속만 받으면 `SneakyBot.class.getDeclaredFields()`가 빈 배열을 내놓아 G2를 그냥 통과한다. 직접 리플렉션 스크립트로 재현해 확인했다(`SneakyBot.class.getDeclaredFields()` → `[]`, `hiddenCounter`는 안 잡힘).

이건 "브리프를 그대로 따르라"는 지시와 충돌하는 것처럼 보이지만, 컨트롤러 노트가 못 박은 건 **구조적 검사 vs 행동적 검사**(`move`를 두 번 불러 비교하는 방식으로 바꾸지 말라)이지, 리플렉션 자체를 얕게 하라는 뜻이 아니다. 클래스 계층을 걸어 올라가는 것도 여전히 순수한 구조적 검사이므로 그 제약을 어기지 않는다. G2의 존재 이유가 "봇 작성자가 상태를 숨기지 못하게" 막는 것인데, 상속 한 단계로 뚫리는 구멍을 그대로 두는 건 관문의 목적 자체를 배신한다고 판단해 고쳤다.

**적용한 수정.** `StatelessGateInheritanceTest`를 새로 추가해(브리프가 못 박은 `StatelessGateTest.java`는 손대지 않고 별도 파일로) `abstract class LeakyBase`에 인스턴스 필드를 두고 `InheritedStateTrap extends LeakyBase`가 그 필드를 상속만 받는 케이스로 RED를 확인했다(`assertFalse(r.passed())`가 실패 — `InheritedStateTrap`이 부당하게 통과). `StatelessGate.check`의 단일 `for (Field f : ctx.botClass().getDeclaredFields())`를 `for (Class<?> c = ctx.botClass(); c != null && c != Object.class; c = c.getSuperclass())`로 감싸 슈퍼클래스 체인을 `Object` 직전까지 걸어 올라가며 매 단계에서 static/synthetic 필드를 걸러내도록 바꿨다. 다시 실행해 GREEN 확인.

**synthetic·static 확인.** 별도 리플렉션 스크립트로 두 가지를 검증했다. (1) non-static 내부 클래스가 outer 인스턴스를 참조하면 컴파일러가 넣는 `this$0` 필드는 `isSynthetic()==true`로 잡혀 `StatelessGate`가 정확히 건너뛴다. (2) 람다 캡처는 봇 클래스 자신에 필드를 만들지 않는다(캡처값은 합성 람다 클래스의 생성자 인자로 전달되지, enclosing 클래스의 필드가 되지 않는다) — 그래서 별도 처리가 필요 없다. `static final` 상수(`WallAvoidBot.PRIORITY`)는 `Modifier.isStatic` 검사로 매 계층에서 계속 허용된다 — `static_final_상수는_허용한다` 테스트로 고정.

**반려 메시지 검토.** `StatefulTrap`을 반려하면 `detail`이 "인스턴스 필드가 1개 있다: int callCount — 봇은 무상태 순수 함수여야 한다"를 낸다 — 타입과 이름을 둘 다 명시해 봇 작성자가 소스를 보지 않고도 무엇을 지워야 하는지 알 수 있다.

**GateContext 범위 검토.** `bot`·`botClass`·`width`·`height`·`judgingSeeds` 다섯 필드 전부 브리프가 명시한 그대로다. G2는 `botClass`만 쓰지만, 나머지 넷은 이후 태스크(G4의 격자 크기, G5·G6의 심사 시드, 일부 관문이 실제로 `bot`을 실행해야 하는 경우)에서 쓰일 것으로 예상되는 최소 집합이라 지금 단계에서 더 줄이지 않았다.

**검증.** `./gradlew :arena-gate:test --tests '*StatelessGate*'` 5개(`StatelessGateTest` 4 + `StatelessGateInheritanceTest` 1) GREEN. `./gradlew test --rerun` 전체 63개 GREEN(기존 58 + 신규 5), 빌드 출력 오류·경고 없음. 커밋은 아래.

---

### D44. Task 11 — G3(금지 API 바이트코드 검사), ASM 단일 클래스 스캔의 구멍을 자체 검토에서 찾아 고침

**결정.** `ForbiddenApiGate`를 브리프(`task-11-brief.md`)가 준 코드 그대로 옮겼다. `FORBIDDEN_PREFIXES`·`FORBIDDEN_METHODS` 목록도 손대지 않았다 — `java/lang/Object.hashCode`를 빼거나 다른 것을 더하지 않았다. TDD 순서는 브리프대로: `ForbiddenApiGateTest` 작성 → `./gradlew :arena-gate:test --tests '*ForbiddenApiGateTest*'`로 RED 확인(`ForbiddenApiGate` 심볼 없음, 컴파일 실패) → 구현 → 같은 명령으로 GREEN 확인(7개 통과).

**자체 검토에서 찾은 구멍.** 태스크 지시문이 "봇 클래스만 스캔하는가, 아니면 중첩 클래스·람다도 스캔하는가"를 명시적으로 묻길래 확인해봤다. 임시 함정 봇 셋(스캐폴드로만 쓰고 커밋 전 삭제)으로 실측했다: 람다(`() -> System.nanoTime()`)는 컴파일러가 같은 클래스 안의 private synthetic 메서드로 넣기 때문에 이미 잡혔다(`passed=false`). 하지만 정적 중첩 헬퍼 클래스(`HelperClassTrap.Helper.pick()`이 `System.nanoTime()`을 호출)와 익명 내부 클래스(`new Supplier<>() { ... System.nanoTime() ... }`)로 옮긴 금지 호출은 둘 다 그냥 통과했다(`passed=true`) — 브리프 코드가 `ctx.botClass()` 하나만 읽어 그 파일 하나만 스캔하기 때문이다. G2가 상속으로 뚫렸던 것(D43)과 정확히 같은 모양의 구멍: 구조적 검사가 "한 겹만" 보면 봇 작성자가 다른 클래스로 옮기는 것만으로 우회한다.

**적용한 수정.** `ForbiddenApiGateNestedClassTest`를 새로 추가해(브리프가 못 박은 `ForbiddenApiGateTest.java`는 손대지 않고 별도 파일로) 위 두 우회 케이스로 RED를 확인했다(`assertFalse(r.passed())`가 실패 — 둘 다 부당하게 통과). `ForbiddenApiGate.check`를 단일 클래스 읽기에서 BFS로 바꿨다: 봇의 최상위 클래스부터 시작해, ASM의 `visitInnerClass` 콜백(클래스가 참조하는 모든 중첩 클래스를 컴파일러가 `InnerClasses` 애트리뷰트에 남기는 것을 이용)으로 발견한 자손 클래스를 큐에 넣어 계속 따라간다. 무관한 라이브러리 내부 클래스(예: 봇이 쓰는 `Map.Entry`)까지 끌려오지 않도록, 봇의 최상위 클래스 internal name을 접두어로 삼아 그 자손(`BotClass$...`)만 추린다. 각 클래스는 클래스로더의 `getResourceAsStream`으로만 읽고 `Class.forName`으로 로드하지 않는다 — 봇 코드를 실행하지 않고 바이트만 본다는 원칙을 중첩 클래스에도 유지하기 위해서다. 다시 실행해 GREEN 확인.

**나머지 자체 검토 항목.**
- **jar 패키징.** `arena-bots-0.1.0.jar`만 클래스패스에 올린 격리된 `URLClassLoader`로 `RandomBot`을 로드하고 그 `Class`를 `GateContext.botClass()`에 직접 넣어 실행해봤다 — `getResourceAsStream`이 jar 안에서도 정상적으로 리졸브되어 `passed=true`가 정확히 나왔다(이 확인용 스캐폴드도 커밋 전 삭제). 디렉터리 클래스패스든 jar든 소유 클래스로더가 통일해서 처리하므로 별도 분기가 필요 없다.
- **다른 classloader.** `ctx.botClass().getClassLoader()`를 그대로 쓰므로 봇을 로드한 로더가 무엇이든(플러그인 로더 포함) 그 로더의 리소스 공간에서 클래스 파일을 찾는다. `getClassLoader()`가 `null`인 경우(부트스트랩 로더)는 이론상으로만 존재 — `Bot` 인터페이스를 구현하는 실제 봇 클래스가 부트스트랩 클래스패스에 있을 수 없으므로 다루지 않았다.
- **`Random` 생성자 폭.** `javap java.util.Random`으로 실제 생성자를 확인했다 — `Random()`과 `Random(long)` 딱 둘뿐이다. 디스크립터 `()V` vs `(J)V` 분기가 전체 생성자 표면을 정확히 덮는다는 뜻이고, 브리프 코드가 놓친 세 번째 형태는 없다.
- **`detail` 정밀도.** 중첩 클래스에서 잡힌 위반의 `detail`은 `ForbiddenApiGateNestedClassTest$HelperClassTrap$Helper.pick → java/lang/System.nanoTime`처럼 실제 클래스 파일 이름(내부 클래스 포함)과 메서드, 정확한 금지 API를 다 담는다 — 봇 작성자가 소스를 뒤지지 않고도 어느 파일의 어느 메서드를 고쳐야 하는지 안다.
- **`NondeterministicTrap` 충돌 재확인.** 태스크 지시문이 예고한 대로, G3가 `NondeterministicTrap`을 잡는다 — `FORBIDDEN_METHODS`의 `java/lang/Object.hashCode` 규칙을 통해서다(`detail`: `NondeterministicTrap.move → java/lang/Object.hashCode`). 이건 이미 조정된 충돌이므로 목록을 손대지 않았다. G5(결정론 검사)가 생겼을 때 G3보다 먼저 이 함정을 못 잡는다는 것이 테스트로 증명되면, 그때 `java/lang/Object.hashCode`를 목록에서 빼는 판단을 내린다.

**검증.** `./gradlew :arena-gate:test --tests '*ForbiddenApiGate*'` 9개(`ForbiddenApiGateTest` 7 + `ForbiddenApiGateNestedClassTest` 2) GREEN. `./gradlew test --rerun` 전체 72개 GREEN(기존 63 + 신규 9), 빌드 출력 오류·경고 없음. 커밋은 아래.

---

### D45. Task 12 — 국면 수집기(`PositionSampler`)와 G4(합법 수), 그리고 컨트롤러 판정: 공유 표본의 재사용 안전성

**결정.** `PositionSampler`·`LegalMoveGate`를 브리프(`task-12-brief.md`)가 준 코드로 옮기되, 컨트롤러 노트가 못 박은 한 지점만 고쳤다: `viewsOf`에서 한 턴의 두 시야(봇0 시야·봇1 시야)가 `grid.wallSnapshot()` 결과 배열 하나를 그대로 공유하던 부분을, 시야마다 `grid.wallSnapshot()`을 따로 불러 독립된 배열을 갖게 했다. TDD 순서는 브리프대로: `PositionSamplerTest`·`LegalMoveGateTest` 작성(브리프의 8개 테스트 그대로) → `./gradlew :arena-gate:test --tests '*PositionSamplerTest*' --tests '*LegalMoveGateTest*'`로 RED 확인(`PositionSampler`·`LegalMoveGate` 심볼 없음, 컴파일 실패 14건) → 구현 → 같은 명령으로 GREEN 확인.

**공유 표본 재사용 안전성 — 컨트롤러 판정에 대한 결정.** `GameView`는 `wall` 배열을 방어적으로 복사하지 않고 내부 참조를 그대로 내준다. 엔진 안에서는 안전했다(`Match`가 매 시야마다 `Grid.wallSnapshot()`으로 새 배열을 만들어 주므로). 하지만 `PositionSampler.sample()`이 돌려주는 `List<GameView>`는 G4·G5·G6가 반복해서 공유·재사용한다 — 특히 G5는 같은 국면을 같은 봇에게 두 번 먹여 비결정성을 잡을 예정(다음 태스크)이므로, 한 봇이 자기가 받은 국면의 `wall`을 훼손했을 때 그 훼손이 표본 리스트의 "다른" 국면까지 오염시키면 안 된다 — 오염되면 이후 다른 봇을 심사할 때 조용히 잘못된 국면을 쓰게 되고, 그 실패는 봇 버그처럼 보이지만 실제로는 수집기 결함이다.

브리프가 준 원본 코드는 정확히 이 구멍을 갖고 있었다: 한 턴 안에서 `boolean[][] snapshot = grid.wallSnapshot();`을 한 번만 만들어 봇0 시야와 봇1 시야 둘 다에 그 배열 참조를 그대로 물려줬다. 봇0을 심사하는 도중 봇0이 자기 시야의 `wall`을 고쳐 쓰면, 리스트 안에 이미 들어 있는 "같은 턴의 봇1 시야"가 즉시 오염된다.

**선택한 방식과 이유.** 세 가지 대안(시야마다 독립 스냅샷 / 수집기가 매번 방어적 복사본을 내줌 / 재사용 지점에서 명시적으로 복사) 중 **"시야마다 독립 스냅샷을 만든다"**를 택했다. `viewsOf`의 두 번째 `for (int me = 0; me < 2; me++)` 루프 안에서 `grid.wallSnapshot()`을 시야마다 한 번씩(턴당 두 번) 부르도록 바꿨다 — `Grid.wallSnapshot()`은 호출할 때마다 이미 완전히 새로운 `boolean[height][width]`를 할당해 채우므로, 이렇게 하면 표본 리스트의 어떤 두 `GameView`도 같은 `wall` 배열 참조를 공유하지 않는다. 이 방식을 고른 이유는 (1) 표본은 한 번 수집되면 그 뒤로 절대 다시 계산하지 않고 여러 관문·여러 봇 심사에 계속 재사용되므로, "만들 때 한 번 안전하게" 만들어 두는 편이 "쓸 때마다 안전하게" 챙기는 것보다 실수할 자리가 적다 — 소비자(G4·G5·G6) 쪽에 복사 책임을 지우면 그중 하나라도 깜빡하면 조용히 뚫린다. (2) 비용이 무시할 만하다: 30×30 `boolean[][]` 하나가 900바이트 남짓이고, 10,000개 표본이면 총 9MB 안팎 — 실측으로도 국면 10,000개 수집이 179ms·힙 사용량 약 18MB로 끝났다(`Xmx` 기본값에서 여유 있음). 매 시야 재계산 자체가 O(900) 스캔이라 CPU 비용도 체감되지 않는다.

**한계를 명시한다.** 이 수정이 막는 것은 "표본 리스트 안의 서로 다른 국면끼리 오염"이다. G5가 예고한 대로 "같은 `GameView` 객체를" 같은 봇에게 두 번 먹이는 패턴을 쓴다면, 그 봇이 첫 호출에서 자기가 받은 바로 그 객체의 `wall`을 고쳐 쓸 경우 두 번째 호출은 이미 훼손된 배열을 보게 된다 — 이건 "다른 국면으로의 전파"가 아니라 "같은 객체의 자기 오염"이라 이번 수정 범위 밖이다. 컨트롤러 노트가 제시한 세 번째 대안("재사용 지점에서 복사")이 바로 이 경우를 위한 것이므로, G5 구현 시 봇에게 같은 국면을 두 번째로 먹일 때는 그 시점에서 `wall`을 다시 복사해 넘겨야 한다는 것을 다음 태스크로 넘긴다.

**증명 테스트.** `PositionSamplerTest.한_국면의_wall을_훼손해도_다른_국면은_영향받지_않는다()` — 표본 50개를 뜬 뒤 모든 국면의 `wall`을 깊은 복사로 스냅샷해 두고, 인덱스 0의 `wall`을 전부 `true`로 덮어쓴 다음, 나머지 국면 전부가 원래 내용과 여전히 같은지 확인한다(`Arrays.deepEquals`). 인덱스 0과 1은 같은 턴의 서로 다른 봇 시야이므로, 이 테스트가 정확히 브리프 원본 코드의 구멍을 잡는다 — 수정 전 코드로 되돌려 실행해보면 인덱스 1이 실패한다(오염 전파 확인, 커밋 전 되돌려 재확인 후 원상복구). `LegalMoveGateTest.낙서_봇_다음에_돌려도_다른_봇_판정은_멀쩡하다()`도 같은 취지로, `wall`을 직접 훼손하는 `ScribbleTrap`으로 G4를 한 번 돌린 뒤 같은 `positions` 리스트로 `WallAvoidBot`을 심사해 정상 통과하는지 본다.

**수집기가 목표 개수에 못 미치면.** `sample()`은 시드 10,000개(전체 조합 `pool×pool` 9가지 매치업)를 다 써도 요청 개수를 못 채우면 조용히 적은 개수를 돌려주지 않고 `IllegalStateException`을 던진다 — 메시지에 요청 개수와 실제로 모은 개수를 둘 다 담는다. 실측: `sample(2_000_000, 15, 15)`로 강제 재현해보니 `"시드 10,000개를 다 써도 국면 2000000개를 못 모았다 (모은 것 1616484개)"`로 정확히 실패했다. 실제 사용 조건인 30×30·10,000개는 전혀 위태롭지 않다 — 별도 스크립트로 `sample(10_000, 30, 30)`을 재보니 179ms에 10,000개를 다 모았고(시드 10,000개 중 극히 일부만 써서 끝남), 힙 사용량도 약 18MB에 그쳤다.

**검증.** `./gradlew :arena-gate:test --tests '*PositionSamplerTest*' --tests '*LegalMoveGateTest*'` 10개(브리프 8개 + 추가 증명 테스트 2개) GREEN. `./gradlew test --rerun` 전체 82개 GREEN(기존 72 + 신규 10), 빌드 출력 오류·경고 없음. 커밋은 아래.

---

### D46. Task 12 리뷰 반려 — D45가 "막았다"고 적은 것은 절반만 사실이었다. 소비 시점 방어가 빠져 있었다

**D45를 뒤집는다.** D45는 "표본 리스트 안 서로 다른 국면끼리 오염"을 막았다고 적었고, 한계를 "G5가 같은 `GameView` 객체를 같은 봇에게 두 번 먹이는 경우"로만 좁혀 적었다. 리뷰가 지적한 대로 그건 사실을 축소한 것이었다 — 실제 노출 범위는 훨씬 넓었다. D45의 판단과 증거는 지우지 않는다(D45가 고친 생성 시점 결함은 실재했고 그 수정도 옳았다); 여기서는 D45가 놓친 것과 그걸 어떻게 마저 막았는지를 이어 적는다.

**놓쳤던 것.** `LegalMoveGate.check`는 `positions.get(i)`를 그대로 `ctx.bot().move(view)`에 넘기고 있었다 — 소비 시점에 복사가 없었다. D45의 수정(시야마다 독립 스냅샷)은 "같은 턴에 동시에 만들어지는 두 시야가 배열을 공유하는 것"만 막았을 뿐, "관문이 리스트에서 국면을 꺼내 봇에게 먹이는 매 순간"은 전혀 건드리지 않았다. 그래서 실제 노출은 이렇다: 관문 하나를 만들어 봇 A(낙서 봇)를 먼저 심사하고, 같은 관문 인스턴스(또는 같은 `positions` 리스트로 만든 다른 관문)로 봇 B를 이어서 심사하면, B는 A가 남긴 낙서를 본다. 이건 D45가 적은 "G5의 이중 호출" 시나리오보다 훨씬 흔하고 훨씬 심각하다 — G4 하나만 여러 봇에게 반복해서 돌리는 정상적인 심사 순서에서 이미 일어난다.

**고친 곳.** `PositionSampler`에 `copyOf(GameView)`를 공개 메서드로 추가했다 — `wall`만 새 배열로 복제하고 나머지 필드는 그대로 둔 새 `GameView`를 만든다. `LegalMoveGate.check`가 국면을 봇에게 먹이기 직전에 반드시 이 메서드를 거치도록 바꿨다(`GameView view = PositionSampler.copyOf(original);`). 복사 책임을 관문이 아니라 "먹이는 지점" 하나로 모은 이유: G5·G6도 같은 리스트를 재사용하므로, 복사 책임이 여러 곳에 흩어지면 그중 하나가 빠뜨릴 때마다 같은 사고가 반복된다. `GameView` 자체는 여전히 손대지 않았다(다른 코드가 그 모양에 의존하므로).

**G6을 위해 미리 잡아둔 모양.** G6은 수 하나당 처리 시간의 p99를 잰다(다음 태스크). 복사가 그 타이머 안에 들어가면 측정치가 복사 비용만큼 부풀려진다. `copyOf`를 `move()` 호출과 완전히 분리된, 그 자체로 끝나는 메서드로 둔 이유가 그것이다 — 호출자가 `copyOf`를 타이머 밖에서 먼저 부르고 `move()` 호출만 타이머 안에 두면 된다. `LegalMoveGate`(G4)는 시간을 재지 않으므로 지금은 이 분리가 드러나지 않지만, G6이 그대로 재사용할 수 있게 미리 그렇게 짰다.

**증명 테스트를 다시 썼다.** 리뷰가 지적한 대로, 예전 `낙서_봇_다음에_돌려도_다른_봇_판정은_멀쩡하다` 테스트는 "낙서 봇 다음에 다른 봇이 G4를 통과하는가"만 봤는데, G4는 `wall` 내용에 아예 무감각한 검사(예외 없이 값을 반환하는가)라 표본이 실제로 훼손됐어도 그 assert는 통과했다 — 아무것도 증명하지 못했다. `낙서_봇에게_먹여도_표본_원본의_wall_내용은_바뀌지_않는다`로 다시 썼다: 낙서 봇을 먹이기 전 표본의 `wall`을 전부 깊은 복사로 떠 두고, 낙서 봇이 (G4가 국면을 넘길 때마다) 자기가 받은 `wall`을 전부 `true`로 덮어쓰게 한 뒤, 표본이 여전히 원래 내용과 같은지 `Arrays.deepEquals`로 직접 비교한다. 이 테스트는 공유 static `positions`가 아니라 이 테스트 전용의 별도 표본(`PositionSampler.sample(300, 30, 30)`)을 쓴다 — `positions`를 건드리면 클래스의 다른 테스트가 실행 순서에 몰래 의존하게 되기 때문이다(리뷰가 짚은 두 번째 문제).

이 테스트가 실제로 결함을 잡는지 되돌려서 확인했다. `LegalMoveGate`의 복사 줄(`GameView view = PositionSampler.copyOf(original);`)만 `GameView view = original;`로 되돌리고 같은 테스트를 돌리니 `낙서_봇에게_먹여도_표본_원본의_wall_내용은_바뀌지_않는다()`가 `AssertionFailedError`로 실패했고, 나머지 4개(`아이디는_G4다`, `예외를_던지는_봇을_반려하고_반례를_알려준다`, `정상_봇을_통과시킨다`, `null을_반환하는_봇을_반려한다`)는 여전히 통과했다 — G4가 `wall` 내용에 무감각하다는 것과 정확히 일치한다. 수정을 되돌려 복구한 뒤 5개 전부 GREEN을 다시 확인했다.

**리포트도 고쳤다.** 1차 리포트의 "우려 사항. 없음"은 틀렸다 — 소비 시점 결함이 그대로 있었으니 거짓 안심이었다. `task-12-report.md`에 이번 수정을 별도 절로 덧붙이고, D45가 과소평가했던 지점을 이 항목에서 바로잡았다고 적었다(D45 본문은 편집하지 않았다).

**남겨둔 것(원장에 미룬 항목).** `sample()`의 부족분 처리(`IllegalStateException`) 경로는 커밋된 회귀 테스트가 없다 — 강제 재현 스크립트로 한 번 확인했을 뿐 저장소에 남지 않았다. 리뷰가 "이건 그대로 두라"고 명시했으므로 이번 커밋에서는 손대지 않는다. 다음에 이 경로를 만지는 사람이 참고할 수 있도록 여기 남긴다.

**검증.** `./gradlew :arena-gate:test --tests '*LegalMoveGateTest*'` 5개 GREEN(수정 전 되돌림 → 1개 FAILED 확인 → 복구 → 5개 GREEN 재확인). `./gradlew test --rerun` 전체 82개 GREEN, 빌드 출력 오류·경고 없음. 커밋은 아래.

---

### D47. Task 13 — G5(결정론)와 G6(시간 예산): 반복 호출마다 새로 복사, 타이머 밖에서 복사, 그리고 함정이 진짜 함정인지 실측으로 확인

**결정.** `DeterminismGate`(G5)·`TimeBudgetGate`(G6)를 브리프(`task-13-brief.md`)가 준 뼈대로 구현하되, 컨트롤러가 못 박은 세 가지를 반영해 브리프 예시 코드에서 갈라섰다. TDD 순서는 브리프대로: `DeterminismGateTest`·`TimeBudgetGateTest` 작성(브리프의 6개 테스트 그대로) → `./gradlew :arena-gate:test --tests '*DeterminismGateTest*' --tests '*TimeBudgetGateTest*'`로 RED 확인(`DeterminismGate`·`TimeBudgetGate` 심볼 없음, 컴파일 실패 7건) → 구현 → 같은 명령으로 GREEN 확인.

**G5 — 브리프 예시와 갈라선 지점.** 브리프의 `checkPerCall` 예시는 국면 하나를 `positions.get(i)`에서 한 번만 꺼내 그 같은 `GameView` 객체를 첫 호출과 반복 호출 셋에 그대로 재사용했다. 이건 D45/D46이 G4에서 이미 겪은 문제의 G5판이다 — 봇이 자기가 받은 국면의 `wall`을 첫 호출에서 고쳐 쓰면, 두 번째 호출은 이미 훼손된 배열을 보게 되고, 그 결과 나온 "다른 답"은 비결정론이 아니라 자기 오염이다. `PositionSampler.copyOf`를 매 호출마다(반복 호출 포함) 새로 불러 원본에서 매번 새 복사본을 먹이도록 고쳤다.

이걸 증명 없이 "고쳤다"고만 적으면 D45의 실수를 반복하는 것이므로, 직접 국면 내용에 따라 답이 갈리면서 동시에 자기 `wall`을 훼손하는 `WallReadingScribbleTrap`을 만들어 `DeterminismGateTest.자기_국면을_훼손해도_반복_호출마다_원본_기준으로_판정한다()`를 추가했다(브리프가 요구한 6개 테스트 외 추가). 공유 static `positions` 대신 이 테스트 전용의 손으로 짠 국면 하나만 쓴다 — 실제 표본은 `(0,0)`의 `wall` 값을 보장하지 않으므로 진단 정확도가 표본 내용에 우연히 좌우되면 안 된다. 이 테스트가 실제로 결함을 잡는지 되돌려서 확인했다: `checkPerCall`의 두 `PositionSampler.copyOf(original)` 호출을 `original`로 되돌리고 돌리니 정확히 이 테스트만 `AssertionFailedError`로 실패했고(나머지 3개는 여전히 GREEN — G5의 다른 특성에는 무감각하다는 뜻), 복구 후 4개 전부 GREEN을 재확인했다.

브리프의 `checkReplayHash`(②층, 베이스라인 상대 리플레이 해시 대조)는 그대로 가져왔다 — `Match.play`가 내부에서 `grid.wallSnapshot()`으로 매 시야마다 새 배열을 만들어 주므로 이 층에는 원래도 오염 경로가 없다.

**G6 — 브리프 예시와 갈라선 지점.** 브리프의 `check` 예시는 `ctx.bot().move(positions.get(i))`를 타이머 안에서 직접 불러, (1) 복사가 아예 없어 원본 표본을 훼손할 위험이 있었고 (2) 설령 복사를 넣더라도 타이머 안에 들어가면 그 비용(30×30 기준 약 900바이트 배열 클론)이 매 수 측정치에 얹혀 p99를 부풀린다. `PositionSampler.copyOf`를 `System.nanoTime()` 호출보다 먼저 끝내도록 고쳤다 — 웜업 루프에서도 마찬가지로 적용했다(표본은 G4·G5·G6가 공유하므로 웜업이라고 원본을 그대로 넘기면 안 된다).

**웜업 설계.** 브리프의 `WARMUP=1_000`은 표본 개수로 캡을 걸어(`Math.min(WARMUP, positions.size())`), `TimeBudgetGateTest`의 20개 국면 서브리스트(`SlowTrap` 테스트)처럼 표본이 적으면 웜업이 20회에 그친다는 뜻이었다. 콜드 JIT을 측정치에 섞으면 빠른 봇도 어쩌다 p99를 넘겨 이 관문에서 들쭉날쭉 떨어질 위험이 있으므로(`CleanBot`처럼 항상 통과해야 하는 봇이 어쩌다 떨어지면 그건 봇이 아니라 관문의 결함이다), 반복 횟수 상한(3,000회)과 시간 상한(300ms)을 **동시에** 거는 "and" 조건으로 바꿨다 — 표본이 적으면 인덱스를 모듈로 순환해서라도 3,000회를 채운다. 이렇게 하면 빠른 봇은 반복 상한에 먼저 걸려 넉넉히 데워지고, `SlowTrap`처럼 느린 봇은 시간 상한(300ms)에 먼저 걸려 웜업 단계에서 테스트 실행 시간을 잡아먹지 않는다 — 느린 봇은 데워도 느리므로 웜업을 오래 끌 이유가 없다.

**p99 계산.** 브리프의 `(int) (nanos.length * 0.99)`는 인덱스를 반올림 없이 절삭해서 쓰는 방식이었다. 최근접 순위법(`idx = ceil(n * 0.99) - 1`, [0, n-1]로 클램프)으로 바꿨다 — "표본의 99%가 이 값 이하"가 되는 가장 작은 값이라는 p99의 정의에 더 정확히 들어맞는다. n=10,000이면 인덱스 9,899(상위 100개, 정확히 1%만 이 값을 넘을 수 있다).

**`SlowTrap`이 실제로 걸렸는가 — 첫 시도에서 그대로 걸렸다.** 컨트롤러 노트가 경고한 대로 `SlowTrap`의 40M 반복 누산기는 결과에 드물게만 반영되어 JIT가 죽은 코드로 지워버릴 위험이 있었다. `TimeBudgetGateTest.느린_봇을_반려하고_실측값을_알려준다()`는 첫 구현·첫 실행에서 바로 GREEN(반려)이 나왔지만, 그것만으로 "함정이 진짜 함정"이라고 믿지 않고 별도 스크래치 프로브로 `r.detail()`의 실측값을 직접 찍어봤다: `p50 47.355 ms, p99 79.094 ms (상한 5.0 ms)`. 인터프리터 속도라도 40M회 단순 모듈로 연산이 마이크로초 단위에 그칠 리 없으므로(실제로 CleanBot의 p99는 아래에서 보듯 마이크로초 단위다), 이 수치는 루프가 실제로 돌았다는 확실한 증거다. **`SlowTrap` 자체는 고치지 않았다** — 이미 충분히 느렸고, 5ms 상한을 낮추거나 관문을 완화할 필요도 없었다.

**G6 흔들림(flaky) 위험 — 실측으로 가늠했다.** `CleanBot`이 어쩌다 이 관문에서 떨어지면 안 되므로, 실제 프로덕션 조건(국면 10,000개, 상한 5.0ms)으로 5회 반복 측정했다: p50은 매번 0.0001~0.0001ms, p99는 0.0003~0.0017ms, 최댓값도 0.03~0.08ms를 넘지 않았다 — 5ms 상한 대비 여유가 3,000배~16,000배(p99 기준), 최댓값 기준으로도 60배 이상이다. `TimeBudgetGateTest`도 같은 이유로 5회 연속 재실행해 흔들림이 없음을 확인했다. 이 정도 여유라면 노이즈 있는 CI 머신에서 GC 정지나 OS 스케줄링 지연이 몇 개 표본에 끼어들어도(p99가 허용하는 1%, 즉 10,000개 기준 100개 표본까지는 흡수), 결과를 뒤집으려면 그 지연이 5ms에 근접해야 하므로 사실상 무시할 만하다고 판단했다. 다만 이건 이 머신·이 JVM 조건에서의 실측이며, 훨씬 느린 CI 하드웨어에서는 여유가 줄어들 수 있다는 것은 남는 리스크로 적어둔다.

**두 관문 모두 예외·null을 관문 붕괴 없이 반려로 처리한다.** `RuntimeException | StackOverflowError`를 잡아 반례 국면과 함께 `GateResult.fail`로 돌리고, `null` 반환도 명시적으로 검사해 반려한다 — G5는 `checkPerCall`·`checkReplayHash` 양쪽에, G6은 웜업·측정 양쪽에 적용했다. 자체 검토 중 `TimeBudgetGate`가 빈 `positions`를 받으면 `percentileMillis`가 `ArrayIndexOutOfBoundsException`을 던진다는 것을 발견해(G4가 빈 리스트에 공허하게 통과하는 것과 다른 동작), `check` 진입부에 `positions.isEmpty()` 조기 반환을 추가해 G4와 같은 방식(공허 통과)으로 맞췄다.

**검증.** `./gradlew :arena-gate:test --tests '*DeterminismGateTest*' --tests '*TimeBudgetGateTest*'` 7개 GREEN(브리프 6개 + 추가 증명 테스트 1개). `./gradlew test --rerun` 전체 89개 GREEN(기존 82 + 신규 7), 빌드 출력 오류·경고 없음. 커밋은 아래.

---

### D48. Task 14 — 시리즈 실행기(`SeriesRunner`): 좌석 교대, 그리고 브리프 예시(`IntStream.parallel()`)를 명시적 `ExecutorService`로 바꾼 이유

**결정.** `SeriesRunner`를 `arena-core`에 만들었다 — G7(회귀 관문)과 챔피언전이 둘 다 시리즈를 돌려야 하는데, 대전 모듈에 두면 `arena-gate → arena-tournament`라는 형제 의존이 생기기 때문이다. TDD 순서는 브리프(`task-14-brief.md`)대로: `SeriesRunnerTest`(브리프의 5개 테스트 그대로) 작성 → `./gradlew :arena-core:test --tests '*SeriesRunnerTest*'`로 RED 확인(`SeriesRunner` 심볼 없음, 컴파일 실패 7건) → 구현 → 같은 명령으로 GREEN 확인.

**좌석 교대는 미러링이 아니다.** 0번과 1번의 시작 위치·방향은 시드로부터 각자 독립적으로 뽑히므로(스펙 §2.2) 두 좌석은 대칭이 아니다. 좌석 교대 경기는 같은 시드의 같은 두 시작 위치에 앉는 봇만 뒤바꿔 만든다 — `Match.play`를 `(id1, bot1, id0, bot0)` 순서로 그대로 부르는 것으로 충분하다. 이렇게 하면 `winner`가 항상 "그 경기에서 실제로 0번 자리에 앉은 쪽이 이겼는가"를 뜻하고 `bot0Id`/`bot1Id`가 실제 좌석과 항상 일치해서, 소비자는 `winner == 0 ? bot0Id : bot1Id`만 보면 봇 이름으로 승자를 알 수 있다 — 교대 여부를 따로 뒤집어 계산할 필요가 없다. `SeriesRunnerTest.교대_경기는_같은_시작_위치에_봇만_바꿔_앉힌다()`가 이걸 직접 고정한다: 교대 경기의 `start0`/`start1`이 정방향 경기와 완전히 같고(위치가 그대로임을 확인), `bot0Id`만 `"b"`로 바뀌었는지(앉는 봇만 바뀌었음을 확인) 본다.

**브리프 예시와 갈라선 지점 — `IntStream.parallel()` 대신 명시적 `ExecutorService`.** 브리프가 준 최소 구현은 `IntStream.range(0, total).parallel().forEach(...)`로 공용 `ForkJoinPool`을 썼다. 이것도 결정론은 지킨다(각 인덱스가 배열의 자기 칸에만 쓰고, `forEach` 종료가 happens-before를 세우므로) — 하지만 이 태스크의 자체 검토 항목이 명시적으로 "병렬 경로가 모든 종료 경로(예외 포함)에서 실행기를 종료하는가"를 묻는데, 공용 풀을 쓰는 스트림에는 애초에 "우리가 만들고 우리가 꺼야 할 실행기"가 없어서 그 요구를 코드로 보여줄 수가 없었다. `Executors.newFixedThreadPool`로 바꾸고 `try/finally`에서 반드시 `shutdown()`하도록 구현해, 정상 종료·예외·인터럽트 어느 경로로 빠져나가든 스레드 풀이 남지 않게 했다. 결과 순서는 완료 순서가 아니라 `ExecutorService.invokeAll`이 보장하는 "제출한 태스크 리스트와 같은 순서의 `Future` 리스트"를 그대로 순회해서 채운다 — 어느 태스크가 먼저 끝나든 최종 리스트 순서는 항상 시드 순서다.

**"경기가 실패하면 시리즈가 조용히 리플레이를 빠뜨리지 않고 통째로 실패해야 한다"는 것도 자체 검토 항목이었다.** 순차 경로는 `task.call()`을 잡아 `RuntimeException`으로 다시 던지고, 병렬 경로는 `future.get()`이 던지는 `ExecutionException`의 원인을 그대로 다시 던진다(인터럽트는 별도로 잡아 현재 스레드의 인터럽트 플래그를 복원한 뒤 던진다). 두 경로 모두 executor 종료는 `finally`에서 일어나므로 예외가 나도 스레드 풀이 새지 않는다. 이 경로가 실제로 작동하는지 커밋 전 스크래치 프로브(`ThrowCheck.java`, 저장소에는 남기지 않음)로 직접 확인했다 — 항상 예외를 던지는 봇으로 `parallel=false`·`parallel=true` 양쪽을 돌려보니 둘 다 `RuntimeException`을 던졌고 `cause`가 원래 봇이 던진 예외였다(리플레이를 부분적으로 반환하지 않았다).

**병렬·순차 동일성 — 무엇을 비교했는가.** 브리프의 `병렬_실행이_순차_실행과_같은_결과를_낸다()`는 시드 1‥50 전체(경기 100개)를 순차·병렬로 각각 돌려 `Replay` 리스트를 인덱스별로 `hash()`까지 비교한다 — 승패 개수가 아니라 각 경기의 전체 기록을 정규화해 만든 해시(`ReplayHash`, 이동 기록·결과·시드·봇 id·격자 크기를 전부 포함)를 비교하므로, "우연히 승자 수만 같고 이동 순서가 다른" 경우를 놓치지 않는다. `반복_실행이_항상_같은_결과를_낸다()`는 같은 병렬 실행을 두 번 돌려 스레드 스케줄링에 따라 결과가 흔들리지 않는지 확인한다. 이 병렬 경로가 실제로 여러 스레드를 쓰는지는 `Runtime.getRuntime().availableProcessors()` 크기의 고정 풀을 쓰는 구현 자체가 보장하며, 별도 계측 없이도 두 테스트가 100경기 전체에 대해 통과한 것으로 스레드 간 결과 오염이 없음을 확인했다.

**검증.** `./gradlew :arena-core:test --tests '*SeriesRunnerTest*'` 5개 GREEN. `./gradlew test --rerun` 전체 94개 GREEN(기존 89 + 신규 5), 빌드 출력 오류·경고 없음. 커밋은 아래.

---

### D49. Task 14 리뷰 반려 — 1-vCPU 머신에서 결정적 테스트가 무력화될 수 있었다

**리뷰 결과.** Approved(Critical·Important 없음). 결정론이 구조적으로 지켜진다는 것도 리뷰어가 직접 확인했다 — 순서는 `invokeAll`의 제출 순서 보장에서, 무상태는 `Match.play`에 정적 가변 상태가 없고 불변 `NO_OP_OBSERVER`뿐이라는 것에서, executor 종료는 모든 경로의 `finally`에서, 실패 전파는 원인 포함 재던지기에서 확인했다. `ReplayHash.of`가 `matchId`·`swapped`·시작 좌표를 입력으로 받지 않으므로 `withSwapFlag`가 원본 `hash()`를 재사용해도 무효화될 수 없다는 것도 확인했다(D48이 "재사용이 옳다"고만 적었던 것을 리뷰가 왜 옳은지까지 증명했다).

Minor 4건이 왔고, 그중 첫 2건을 컨트롤러가 상향했다 — "이 태스크의 결정적 테스트가 애초에 실패할 수 있는가"를 결정하는 문제였기 때문이다.

**1) `SeriesRunner`의 풀 크기 하한이 1이었다.** `Math.max(1, availableProcessors())`는 vCPU 1개짜리 CI 컨테이너(흔한 조건)에서 `newFixedThreadPool(1)`이 되고, 그러면 100개 태스크가 제출 순서 그대로 한 스레드에서 순차 실행된다. 그 결과 `병렬_실행이_순차_실행과_같은_결과를_낸다`는 사실상 "순차 대 순차"를 비교하는 셈이 되어 스케줄링 의존성을 전혀 잡아내지 못한 채 항상 GREEN이 나온다 — 이 태스크의 핵심 요구사항(재현 가능성)을 지켜야 할 바로 그 테스트가, 그걸 지켜야 하는 하드웨어에서는 아무것도 증명하지 않게 되는 셈이었다. 이번 구현·검증 환경은 4 vCPU였으므로 이미 받은 GREEN 자체는 진짜 증거였지만, 그 증거가 다른 하드웨어로 이동하지 않는다는 게 문제였다. `Math.max(2, ...)`로 하한을 올려, 어떤 하드웨어에서도 이 테스트가 최소 2개의 실제 스레드를 쓰도록 강제했다.

**2) 병렬·순차 동일성 테스트가 `hash()`만 비교했다.** 브리프가 지정한 모양이라 이건 작업자의 drift가 아니지만, `hash()`만 비교하면 `swapped`·`matchId`·`start0/1`·`dir0/1`은 두 경로 사이에 아무것도 검증되지 않는다. `Replay`가 record라는 점을 살려 `assertEquals(sequential, parallel)`로 리스트 전체를 한 줄에 비교하도록 바꿨다 — "병렬과 순차가 정확히 같은 리플레이 목록을, 같은 순서로 낸다"는 원래 요구사항을 그대로 코드로 옮긴 것이다.

**Minor 나머지 2건도 접었다.** `반복_실행이_항상_같은_결과를_낸다`에 `first.size() == second.size()` 단정이 빠져 있어, 크기가 다르면 읽기 어려운 `IndexOutOfBoundsException`으로 죽거나(짧은 쪽) 빈 리스트끼리는 루프가 스킵돼 아무것도 검증하지 않고 통과하는(빈 쪽) 구멍이 있었다 — 크기 비교를 앞에 추가했다. `교대_경기는_같은_시작_위치에_봇만_바꿔_앉힌다`는 시작 위치(`start0`/`start1`)만 비교하고 스펙 §2.2가 명시하는 독립적으로 뽑히는 시작 방향(`dir0`/`dir1`)은 비교하지 않아 "미러링이 아니다"의 절반만 고정하고 있었다 — `dir0()`/`dir1()` 비교를 추가했다.

**보류(원장에 미룸, 컨트롤러 지시).** 순차·병렬 경로의 예외 처리 비대칭, javadoc이 무상태 전제를 spec §3/G2가 아니라 `BotFunction`을 가리키며 설명하는 부분, 빈/단일/중복 시드 리스트 엣지 케이스 테스트 부재.

**검증.** `./gradlew :arena-core:test --tests '*SeriesRunnerTest*'` 5개 GREEN(수정된 어서션 포함). `./gradlew test --rerun` 전체 94개 GREEN(변경 전과 동일한 개수), 빌드 출력 오류·경고 없음. 커밋은 아래.

---

### D50. Task 15 — 승점 집계(`Standing`)·G7(회귀 방지)·관문 실행기(`GateRunner`): D1(G3/G5 충돌)이 예측대로 갔고, CleanBot이 G7을 겨우 못 넘겼다

**결정.** 브리프(`task-15-brief.md`)가 준 뼈대(`Standing`·`RegressionGate`·`GateReport`·`GateRunner`)를 그대로 구현하되, 컨트롤러가 미리 못 박은 네 가지 판단과 실행 중 발견한 성능·정확성 문제를 반영했다. TDD 순서는 브리프대로: `StandingTest`(브리프 3개 테스트 그대로) 작성 → RED 확인(`Standing` 심볼 없음) → 구현 → GREEN. `GateRunnerTest`(브리프 9개 테스트 그대로, 이후 2개 추가) 작성 → RED 확인(`GateRunner`·`GateReport`·`RegressionGate` 심볼 없음) → 구현 → GREEN.

**결정 1 — G3/G5 충돌은 브리프가 예측한 대로 갔다.** `NondeterministicTrap은_G5에서_걸린다`를 처음 돌리자 `failedGate()`가 `"G3"`였다 — `java/lang/Object.hashCode`가 `ForbiddenApiGate.FORBIDDEN_METHODS`에 있어 G3가 먼저 잡았다. 사전에 합의된 대로 `FORBIDDEN_METHODS`에서 `java/lang/Object.hashCode` 항목을 제거했다: 결정론 위반은 G5의 몫이고, G3의 몫은 재현성을 깨는 "호출"을 막는 것인데 `Object.hashCode` 자체는 그런 호출이 아니라 증상일 뿐이며 G5가 그 증상을 직접, 더 잘 잡는다. 제거 후 재실행하니 `NondeterministicTrap은_G5에서_걸린다`가 기대대로 `"G5"`로 통과했다(`ForbiddenApiGateTest`·`ForbiddenApiGateNestedClassTest`의 기존 9개 테스트는 이 항목을 쓰지 않아 회귀 없이 그대로 GREEN).

**Standing — 좌석 교대 귀속.** `winner`는 좌석 기준이므로, 봇 이름으로 승패를 판정하려면 `r.bot0Id().equals(subjectId)`로 "이 경기에서 내가 몇 번 자리에 앉았는가"부터 구한 뒤 `result().winner()`와 비교해야 한다 — `swapped` 플래그 자체는 보지 않는다(D48이 `SeriesRunner`에서 이미 확립한 불변식: `bot0Id`/`bot1Id`가 항상 실제 좌석과 일치하므로 `swapped`를 다시 뒤집어 계산할 필요가 없다). `StandingTest.좌석_교대_경기의_승자를_올바르게_귀속한다`가 이걸 직접 고정한다: 정방향 경기에서 hero가 0번 자리로 이기고, 교대 경기에서는 hero가 1번 자리에서(`bot0Id="rival"`, `winner=1`) 이기는 두 케이스를 섞어 `wins()==2`를 확인한다 — `swapped`를 무시하고 항상 `winner==0`으로 판정하는 버그가 있었다면 두 번째 케이스가 `losses`로 잘못 잡혀 이 테스트가 실패했을 것이다. 무승부 0.5점 산식(`(wins + 0.5*draws) / total`)은 브리프 예시 그대로이며, 60%대 정확한 경계값(0.875 = (3+0.5)/4)까지 테스트로 고정했다.

**결정 2 — 관문이 예외를 던지면 GateRunner가 그 관문의 실패로 변환한다.** `ForbiddenApiGate.readClassFile`은 클래스 바이트를 못 찾으면 체크되지 않은 `IllegalStateException`을 던지고, 이게 `check()` 시그니처를 그대로 뚫고 나간다. `GateRunner.run`이 각 관문 호출을 `checkSafely`로 감싸 `RuntimeException`을 잡고 `GateResult.fail(gate.id(), ...)`로 바꾼다 — 이렇게 해야 "봇은 반려당할 뿐 하네스를 무너뜨릴 수 없다"는 계약이 지켜진다. 스캔 불가능한 봇 하나 때문에 나머지 봇 심사까지 통째로 죽으면 안 된다.

이게 실제로 작동하는지는 증거로 확인했다 — 진짜로 클래스 바이트를 못 찾는 상황을 재현해야 의미가 있으므로, `getResourceAsStream`이 항상 `null`을 돌려주는 `ClassLoader`(`BlindLoader`)로 무해한 봇 클래스를 다시 정의했다: 클래스 자체는 정상 로드·실행되지만(인스턴스를 만들 수 있어야 하므로) 그 바이트를 리소스로 되찾을 방법은 없다. `GateRunnerExceptionTest.클래스_바이트를_못_읽으면_G3를_실패로_돌리고_하네스는_죽지_않는다`가 이 시나리오로 `GateRunner.run(ctx)`를 호출해 (1) `assertDoesNotThrow`로 `run` 자체가 예외를 던지지 않는지, (2) `failedGate()=="G3"`인지, (3) `detail()`에 `IllegalStateException`이 남아 진단 가능한지, (4) `results().size()==2`(G2 통과 + G3 실패, 그 뒤 관문은 안 돔)인지를 확인한다. 4개 assert 모두 GREEN.

**결정 3 — `GateRunner.SAMPLE_SIZE`·`P99_LIMIT_MILLIS`는 값을 낮추지 않고, 테스트 비용만 별도 오버로드로 줄였다.** 처음 구현대로(패키지 전용 오버로드 없이) `GateRunnerTest`를 프로덕션 표본(10,000)·심사 시드(1..50) 그대로 돌리자 `SlowTrap은_G6에서_걸린다`가 2분을 넘겨도 안 끝났다(강제 종료). 원인은 `SlowTrap`이 한 수에 수십 ms(D47 실측: p50 47ms)를 쓰는데, G4(10,000회)·G5 ①층(40,000회, `positions.size()*(1+REPEATS)`) 두 겹만으로 수십 분이 걸리기 때문이다 — 브리프의 "수십 초" 추정은 G6에 한정된 이야기(기존 `TimeBudgetGateTest`처럼 국면 20개로 G6만 떼어 재는 경우)였지, `GateRunner`의 전체 파이프라인(G4~G6를 프로덕션 표본으로 다 거친 뒤 G6에서 반려)까지 감안한 게 아니었다.

`SAMPLE_SIZE`·`P99_LIMIT_MILLIS` 상수 자체를 낮추는 건 금지 사항이므로(요구사항·CLAUDE.md 둘 다 명시), 대신 `GateRunner.run(GateContext ctx)`가 내부적으로 위임하는 패키지 전용 오버로드 `run(GateContext ctx, int sampleSize, double p99LimitMillis)`를 추가했다. 프로덕션 경로(`run(ctx)`)는 언제나 `run(ctx, SAMPLE_SIZE, P99_LIMIT_MILLIS)`로 위임하므로 상수 자체는 그대로다 — 오버로드는 순전히 "어느 관문에서 걸리는가"라는 라우팅 질문을 확인하는 테스트의 비용을 줄이는 용도다. `SlowTrap은_G6에서_걸린다`는 국면 50개·시드 2개로 이 오버로드를 써서 47초로 끝난다(첫 실패에서 멈추는 관문 특성상, 표본·시드를 줄여도 "G4·G5를 통과하고 G6에서 반려된다"는 결론은 바뀌지 않는다). `관문_상수는_스펙값을_그대로_쓴다` 테스트가 `SAMPLE_SIZE==10_000`·`P99_LIMIT_MILLIS==5.0`을 값으로 직접 고정한다.

이 오버로드를 만들다가 진짜 버그를 하나 잡았다 — 초안에서 `run(ctx, sampleSize, p99LimitMillis)`가 `TimeBudgetGate`를 만들 때 파라미터 `p99LimitMillis`가 아니라 클래스 상수 `P99_LIMIT_MILLIS`를 그대로 참조하고 있었다(복사-붙여넣기 실수). 그 상태로는 오버로드에 넘긴 `p99LimitMillis` 값이 완전히 무시된다 — 지금 테스트들은 우연히 항상 `GateRunner.P99_LIMIT_MILLIS`를 그대로 넘겨서 겉으로는 드러나지 않았을 뿐이다. 코드를 다시 읽다가 발견해 `positions, p99LimitMillis`로 고쳤다.

**GateContextFixture 오버로드.** `of(Bot bot, List<Long> seeds)`를 추가해 심사 시드도 테스트에서 줄일 수 있게 했다 — `SlowTrap`의 G5 ②층(리플레이 해시 대조, 시드 수 × 3 베이스라인 × 2)이 시드 수에 비례해서 비싸기 때문이다. 프로덕션 시드(1..50)는 손대지 않는다.

**CleanBot — G7을 처음 붙여 실측하니 대조군이 최상단 기준선(WallAvoidBot)에 못 미쳤다.** 기존 CleanBot("그 칸의 바로 옆 빈 칸 수"만 세는 1칸 앞 공간 감각)으로 `GateRunner.run(ctx)`를 직접 돌려보니 WallAvoidBot 상대 100판(judgingSeeds 1..50, 좌석 교대) 중 48판을 졌다 — G7이 요구하는 "0패"에 한참 못 미쳤다. `CleanBot`은 이 태스크의 요구사항이 명시하는 대조군("모든 관문을 통과해야 한다")이자 브리프가 그대로 준 `GateRunnerTest`의 첫 테스트 대상이므로, 이걸 고치는 게 이 태스크의 범위 안이라고 판단했다(베이스라인 3종은 동결이지만 CleanBot은 테스트 픽스처다).

여덟 가지 접근을 실측으로 비교했다(전부 CleanBot.java 자체의 javadoc에도 남겨 뒀다):

| # | 접근 | WallAvoidBot 상대 100판 중 패배 |
|---|---|---|
| 1 | 1칸 앞 빈 칸 수 | 48 |
| 2 | 보로노이(상대보다 내가 먼저 닿는 칸 수) | 29 |
| 3 | 보로노이 + 고정순서 DFS 최장경로(예산 20,000) | 7(10시드 축소표본) |
| 4 | 보로노이 + 이웃수 정렬 DFS(동률만, 예산 4,000) | 18 |
| 5 | 상대를 적대적으로 미니맥스(깊이 6) | 41 — 실제로 안 일어날 최악의 수에 과잉 대비 |
| 6 | 상대 고정, 내 수만 완전탐색(깊이 6) | 57 — 상대도 그 사이 움직인다는 걸 무시해 리프 평가가 낡음 |
| 7 | 6)을 버리고 상대를 "직진 예측"으로 미리 벽에 얹은 뒤 4) 재적용 | 7 |
| 8 | 7)의 DFS를 "결정론적 무작위 롤아웃 평균(500회)"으로 교체 | **2 ← 최종** |

핵심 진단 두 가지. (a) 4)에서 진 경기(seed=5)를 `Match.TurnObserver`로 직접 재생해 보니, CleanBot이 상대와 나란히(다른 행에서 같은 방향으로) 8턴 넘게 걸으며 왼쪽 아래 구석까지 몰렸다 — "지금 이 순간의 벽 스냅샷"만 보는 한 복도 끝은 항상 뚫려 있어 보이는데, 상대가 나란히 전진하며 그 복도의 지붕을 계속 깎아내고 있다는 건 스냅샷에 안 잡힌다. 상대를 "지금 방향으로 K칸 직진한다"고 예측해 미리 벽으로 얹는 것(7단계)으로 이 특정 경기를 고쳤다. (b) 4)에서 DFS 예산을 4,000→60,000으로 늘렸더니 오히려 18패→22패로 나빠졌다 — 고정 순서로 첫 번째 가지에 예산을 몰아 쓰다가 그 가지가 "커 보인다"는 이유만으로 낫다고 착각하는 편향이 있었고, 예산을 늘릴수록 그 편향도 깊어졌다. 8단계에서 DFS를 접고 "매번 새로 뽑은 난수열로 무작위 경로를 걷는 롤아웃을 여러 번 돌려 평균 생존 칸 수"로 바꾸니 이 편향이 없어져 트라이얼 수를 60→200→500으로 늘릴수록 손실이 18→4→2로 뚜렷이 줄었다.

**남은 한계 — 0패에 도달하지 못했다.** 8단계 기준으로도 100판 중 2판은 진다(seed 13 교대, seed 21 — 둘 다 상대와 무관한 자기 벽 충돌). 트라이얼 수를 더 늘리면(650·800·1000) 오히려 손실이 늘었다 — 이건 진짜 무작위 시뮬레이션이 아니라 결정론적으로 고정된 난수열 하나를 매번 그대로 재생하는 것이라 "표본이 늘수록 매끄럽게 좋아진다"는 보장이 없기 때문이다(근방 값 350·450·550·650을 훑어 500이 국지적 최적임을 확인했다). 상대 예측을 "즉사만 피하며 꺾을 수도 있다"로 정교화하는 것도 시도했지만 더 나빠졌다(7패→15패) — 일반화된 고정 우선순위가 실제 베이스라인의 우선순위와 어긋나 꺾는 방향을 계속 틀리게 예측했다.

**이 태스크의 핵심 우려.** `GateRunnerTest.CleanBot은_모든_관문을_통과한다`가 여전히 FAILED다 — CleanBot이 WallAvoidBot에게 100판 중 2판을 진다(G7 요구사항은 "0패"). 요구사항("CleanBot은 모든 관문을 통과해야 하는 대조군이다")·G7 스펙("전승이 아니라 패배 0회") 어느 쪽도 낮추지 않았고, 낮출 수 없다고 판단했다. 최선을 다한 결과가 48%→2%까지 좁혀졌을 뿐, 정확히 0에는 못 미쳤다 — 두 봇이 동시에 움직이며 서로의 벽을 실시간으로 깎아내는 게임에서 단일 수 시점의 지역 탐색만으로 수백 턴에 걸친 자기 봉쇄를 완전히 막는 건, 이 태스크에 주어진 시간 안에서는 풀지 못했다. 통제자에게 이 상태 그대로 보고한다.

**CleanBot 변경의 여파 — 다른 태스크의 기존 테스트가 느려졌다.** `CleanBot.move()`가 무작위 롤아웃(트라이얼 500회)을 도입하면서 호출 하나당 비용이 마이크로초 단위에서 밀리초 단위로 늘었다. `DeterminismGateTest`·`LegalMoveGateTest`·`TimeBudgetGateTest`(모두 이전 태스크에서 이미 GREEN이던 테스트)가 CleanBot을 국면 수백~수천 개에 먹이므로 실행 시간이 함께 늘었다. 이 파일들은 이 태스크의 소관이 아니라 손대지 않았다 — 실측 결과는 검증 절에 남긴다.

**검증.** `./gradlew :arena-core:test --tests '*StandingTest*'` 3개 GREEN. `./gradlew :arena-gate:test --tests '*GateRunnerTest*'` 10개 중 `CleanBot은_모든_관문을_통과한다` 1개만 FAILED, 나머지 9개(WeakTrap·ClockTrap·StatefulTrap·NondeterministicTrap·SlowTrap·CrashTrap·UnseededRandomTrap·관문_상수는_스펙값을_그대로_쓴다·첫_실패에서_멈추고_뒤_관문은_돌리지_않는다) GREEN. `./gradlew :arena-gate:test --tests '*GateRunnerExceptionTest*'` 1개 GREEN. `./gradlew test --rerun` 전체 108개(기존 94 + `StandingTest` 3 + `GateRunnerTest` 10 + `GateRunnerExceptionTest` 1) 중 107개 GREEN·1개 FAILED(`GateRunnerTest.CleanBot은_모든_관문을_통과한다`), 총 소요 2분 30초 — CleanBot 변경의 여파(위 문단)로 늘어난 시간을 포함해도 실행 시간 자체는 여전히 정상 범위다. 빌드 출력에 그 1건을 빼면 오류·경고 없음.

---

### D51. Task 15 리뷰 반려 — CleanBot에게 G7까지 요구한 건 범주 오류였다

**반려 결과.** D50이 "CleanBot이 G7을 통과 못 한다"를 핵심 우려로 보고했더니, 통제자가 그 실측 자체는 받아들이되(48패→2패 진행과 기각된 대안들은 정확히 이 원장이 쌓아야 할 기록이라며 D50을 그대로 남기라고 지시했다) 결론을 뒤집었다: **CleanBot에게 G7까지 요구한 게 범주 오류였다.**

**범주 오류가 뭐였나.** G2~G6은 계약 관문이다 — 무상태·금지 API·합법성·결정론·시간 예산. G7은 실력 관문이다. `CleanBot`의 일(Task 9에서부터)은 계약의 거짓양성 탐지기 역할이다 — 관문이 잘 짜인 봇까지 반려하지 않는다는 증거. 여기에 "베이스라인을 이겨야 한다"까지 얹으면 두 가지 다른 걸 하나로 묶는 것이고, 대조군을 오히려 제 일에 더 약하게 만든다 — 272줄짜리 전략 엔진이 계약 관문을 통과했다는 건, 40줄짜리 뻔한 봇이 통과했다는 것보다 훨씬 적게 증명한다(그 272줄 자체에 버그가 숨을 자리가 훨씬 많으므로).

**조치.**

1. **`CleanBot`을 Task 9 형태로 되돌렸다.** `openNeighbours`(그 칸의 바로 옆 빈 칸 수)만 보는 원래 버전 그대로(커밋 `1683d0c`에서 복원, diff 없음을 확인). 이 봇의 값어치는 "명백히 계약을 지킨다"는 것이고, 짧아야 그게 참이다.

2. **실력 쪽 작업은 지우지 않고 이름을 바꿔 옮겼다.** D50에서 8단계에 걸쳐 만든 272줄짜리 봇(보로노이 + 상대 진행 예측 + 결정론적 무작위 롤아웃)은 실제로 강한 봇이고 제 몫이 있다 — `arena-gate/src/test/java/arena/gate/traps/StrongBot.java`로 승격했다. javadoc(8단계 이력·D50 인용)은 그대로 옮겼고, 클래스 상단 설명만 "대조군"에서 "실력만을 위한 픽스처, CleanBot과 역할이 다르다"로 고쳤다.

3. **어서션을 쪼갰다.** `GateRunnerTest.CleanBot은_모든_관문을_통과한다`를 `CleanBot은_계약_관문에서_반려되지_않는다`로 바꿨다 — `report.results()`를 훑어 G2~G6 각각이 실제로 `passed()`인지만 본다(G7 결과는 안 본다). "다섯 관문을 다 거쳤는가"도 별도로 확인한다 — G7에서 일찍 멈춰 애초에 G2~G6를 다 못 거쳤다면 앞의 루프가 아무것도 못 보고 공허하게 통과해버리는 구멍을 막기 위해서다.

4. **G7의 accept 경로를 새로 증명했다.** `WeakTrap은_G7에서_걸린다`는 reject 경로만 증명한다 — G7이 뭐가 오든 항상 반려하도록 고장나 있어도 그 테스트는 여전히 GREEN이다. `RegressionGateTest.세_베이스라인_모두에게_패배_0회인_강한_봇을_통과시킨다`(신규)가 accept 경로를 `StrongBot`으로 `RegressionGate.check`를 직접 불러 증명한다. `StrongBot`은 judgingSeeds 1..50 전체에서는 WallAvoidBot에게 2패(seed 13 교대, seed 21 정방향)를 하므로, 이 테스트만 그 두 시드를 뺀 48개 부분집합으로 시드를 좁혔다 — 여기서 시험하는 건 StrongBot의 실력이 아니라 "패배 0회 ⇒ 통과"라는 `RegressionGate`의 규칙 자체이므로, StrongBot이 실제로 0패인 부분집합이면 그 규칙을 증명하는 데 아무 문제가 없다. `RegressionGate`의 임계값(패배 0회)도, 프로덕션 G7(`GateRunner`, judgingSeeds 1..50 전체)도 손대지 않았다 — 시드를 줄이는 건 이 테스트 하나뿐이다. `WeakTrap`도 손대지 않았다.

**GateRunner의 테스트 전용 오버로드 — 프로덕션과 갈라지지 않게 막힌 지점.** `GateRunner.run(GateContext ctx, int sampleSize, double p99LimitMillis)`는 패키지 전용(`static`, `public` 아님)이라 `arena.gate` 패키지 밖에서는 애초에 호출할 수 없다 — 프로덕션 코드(예: 앞으로 나올 세대 루프)가 실수로라도 이 오버로드를 골라 쓸 수 없다. 공개 진입점은 `run(GateContext ctx)` 하나뿐이고, 이 메서드의 본문 전체가 `return run(ctx, SAMPLE_SIZE, P99_LIMIT_MILLIS);` 한 줄이다 — 상수 두 개를 두 번 정의하는 게 아니라 한 번 정의해서 그대로 넘기므로, 값이 두 곳에서 따로 관리되다 갈라질 여지가 없다. `GateRunnerTest.관문_상수는_스펙값을_그대로_쓴다`가 그 상수 값 자체(10,000·5.0)를 고정하고, `CleanBot은_계약_관문에서_반려되지_않는다`를 비롯한 나머지 9개 트랩 테스트는 전부 오버로드 없는 `run(ctx)`를 그대로 써서 그 상수가 실제로 매 실행에 쓰인다는 것도 함께 고정한다 — 표본·시드를 줄이는 오버로드를 쓰는 테스트는 `SlowTrap은_G6에서_걸린다`(표본 50·시드 2) 하나뿐이다(D50 당시엔 CleanBot 테스트도 표본을 줄였으나, CleanBot을 Task 9 형태로 되돌리면서 다시 빨라져 그 필요가 사라졌다).

이 오버로드를 만드는 과정에서 정확히 이 종류의 위험이 실제로 벌어졌었다: 초안에서 `TimeBudgetGate`를 만드는 줄이 파라미터 `p99LimitMillis`가 아니라 클래스 상수 `P99_LIMIT_MILLIS`를 그대로 참조하고 있었다(복사-붙여넣기 실수, D50에 기록). 그 상태로는 오버로드에 넘긴 `p99LimitMillis` 값이 완전히 무시되는데, 테스트들이 우연히 항상 `GateRunner.P99_LIMIT_MILLIS`를 그대로 넘겨서 겉으로 드러나지 않았을 것이다 — 테스트 전용 경로가 프로덕션 경로에서 조용히 갈라지는 게 바로 이런 식으로 일어난다는 걸 직접 겪은 사례다. 코드를 다시 읽다가 발견해 고쳤다.

**이 태스크의 결론 — 하네스에 대한 발견으로 남긴다.** G7은 스펙이 명시한 그대로 엄격하다 — 계약만 지키는 단순한 봇은 그 엄격함을 통과하지 못한다(48패, 그리고 상당히 개선한 버전도 2패). 이건 낮출 임계값이 아니라, "계약 대조군과 실력 대조군은 다른 봇이어야 한다"는 하네스 설계에 대한 발견이었다 — 결론은 관문을 완화하는 게 아니라 대조군을 역할별로 분리하는 것이었다.

**검증.** `./gradlew :arena-gate:test` 전체 49개(arena-gate 모듈, `RegressionGateTest` 신규 1개 포함) GREEN, 58초. `./gradlew test --rerun` 전체 109개(D50 당시 108 + `RegressionGateTest` 1) 전부 GREEN, 실패·오류 0, 총 소요 1분 3초 — CleanBot을 되돌리면서 arena-gate 모듈 실행 시간이 2분 30초에서 58초로 되돌아갔다. 빌드 출력에 오류·경고 없음. 커밋은 아래.

---

### D52. Task 15 두 번째 리뷰 반려 — Standing의 좌석 판정이 문자열 하나로 뒤집힐 수 있었고, accept 경로와 드리프트 가드 둘 다 테스트가 없었다

**리뷰 결과.** D51 대응이 다시 반려됐다. 이번엔 지킨 것도 명시적으로 확인해 줬다 — G3 목록이 정확히 한 항목만 잃었고 나머지(시계·시드 없는 Random·스레드·파일/네트워크 I/O·리플렉션·Unsafe·ProcessBuilder)는 그대로라는 것, `BlindLoader` 예외 테스트가 진짜 클래스 바이트를 못 읽는 하드 모드 재현이라는 것, `checkSafely`가 `RuntimeException`만 잡고 `Throwable`은 안 잡아서 `OutOfMemoryError`는 여전히 전파된다는 것, CleanBot 분리에 `containsAll(contractGates)`라는 진짜 공허 통과 방지 장치가 있다는 것, `SlowTrap` 테스트가 표본만 줄이고 `P99_LIMIT_MILLIS`는 진짜 상수 그대로 넘긴다는 것(방향이 중요한 그 축은 안 건드렸다는 것) — 전부 리뷰어가 코드를 직접 따라가며 확인했다. 그 위에서 승인을 막은 건 세 가지였다.

**1) `Standing`의 좌석 판정이 봇이 제공하는 문자열 하나로 G7 증거의 절반을 뒤집을 수 있었다.** `int mySeat = r.bot0Id().equals(subjectId) ? 0 : 1;`은 "0번이 아니면 진짜 1번인지"를 확인하지 않았다. `RegressionGate`가 `ctx.bot().name()`을 그대로 `id0`으로 넘기고 `SeriesRunner`는 넘어온 id를 아무 고유성 검사 없이 모든 `Replay`에 그대로 복사하므로, 제출된 봇의 `name()`이 예컨대 `"RandomBot"`을 돌려주면 그 매치업 전체에서 `bot0Id == bot1Id`가 되고, `mySeat`은 항상 0으로 풀리며, 좌석 교대 절반(진짜로는 subject가 1번에 앉는 절반)에서 모든 패배가 승으로, 모든 승이 패로 뒤집힌다. G7의 판정 전부가 `losses() > 0` 하나이므로 이 문자열 하나가 양방향(false accept, false reject)을 다 만든다. 비적대적 형태도 같은 구멍이다 — `Standing.of(replays, "typo")`는 실패하는 대신 그럴듯하고 완전히 틀린 결과를 돌려준다.

**조치.**
- `RegressionGate`에 `SUBJECT_ID = "subject"`(베이스라인 이름과 절대 겹치지 않는 예약어)를 두고, `SeriesRunner.run`과 `Standing.of` 양쪽에 `ctx.bot().name()` 대신 이걸 넘긴다.
- `Standing`에 `seatOf(Replay, String)`를 새로 공개해, bot0Id 일치 → 0, bot1Id 일치 → 1, 둘 다 아니면 `IllegalArgumentException`을 던지도록 명시적으로 판정한다. `Standing.of`도 이걸 쓴다.
- `RegressionGate`의 진 시드 진단(48-51행)이 같은 좌석 판정을 따로 재구현하고 있었다 — 사본을 남기지 않고 `Standing.seatOf`를 그대로 쓰도록 고쳤다. 그 김에 발견한 것: 이 진단이 매치(정방향·교대)를 따로 세서, 한 시드가 두 방향 모두 지면 "진 시드(최대 5개)"에 같은 시드가 두 번 나와 실질적으로 서로 다른 시드는 3개만 보여줄 수 있었다 — `.distinct()`를 넣어 고쳤다.
- 테스트 두 갈래. `StandingTest`: `subjectId가_리플레이에_없으면_예외를_던진다`(오타 등으로 subject가 아예 없는 경우), `이름이_충돌하면_bot0Id_쪽으로_판정한다`(진짜 이름 충돌은 Standing 하나만으로는 풀 수 없는 잔여 모호함이라는 것과, 그게 지금 어떻게 결정론적으로 해소되는지를 문서로 고정). `RegressionGateTest`: `봇_이름이_베이스라인과_같아도_결과가_왜곡되지_않는다` — `name()`이 `"WallAvoidBot"`을 돌려주는 봇과 정상 이름의 봇을 같은 시드(13·21, StrongBot이 WallAvoidBot에게 진짜로 지는 시드)로 돌려 `GateResult`가 완전히 같은지(`passed()`와 `detail()` 문자열까지) 비교한다.
- **버그가 실제로 잡히는지 되돌려서 확인했다.** 반려 전 코드(`ctx.bot().name()`을 그대로 쓰는 버전)를 스크래치 클래스로 복원해 같은 두 봇·같은 시드로 돌려보니: 정상 봇은 `[13, 21]`(둘 다 진짜 패배)을 진 시드로 냈지만, 이름이 충돌한 봇은 `[21, 21]`을 냈다 — seed 13의 진짜 패배(교대 방향)가 버그 때문에 승으로 둔갑해 사라지고, 대신 seed 21이 두 번(정방향의 진짜 패배 + 교대 방향에서 새로 조작된 가짜 패배) 나타났다. 정확히 리뷰가 설명한 "false accept이자 false reject"가 실측으로 재현됐다. 수정된 코드로 같은 비교를 돌리면 두 결과가 완전히 같다(`assertEquals(normalResult.detail(), collidingResult.detail())`가 GREEN).

**2) 파이프라인이 봇을 실제로 통과시키는 경로를 아무 테스트도 실행해 보지 않았다.** `GateRunner.run` 호출부는 전부 `failedGate()` 문자열이나 `assertFalse(passed())`만 단정했고, CleanBot 테스트는 계약 관문만 보며, `RegressionGateTest`는 `GateResult`만 보지 `GateReport`는 안 본다. `GateRunner.java:69`(`return new GateReport(..., true, null, "", ...)`)가 항상 false를 반환하거나 `failedGate`가 절대 null이 안 되도록 망가져도 109개 테스트는 그대로 GREEN이었을 것이다 — `GateReport.passed()`는 세대 루프가 분기하는 단 하나의 비트인데, 그게 시험대에 오른 적이 없었다.

**조치.** `GateRunnerTest.강한_봇을_실제로_통과시킨다`을 추가했다 — `StrongBot`을 48-시드 부분집합(`RegressionGateTest`와 같은 근거)으로, 표본은 200으로 줄인 패키지 전용 오버로드를 통해 돌려 `report.passed()==true`·`failedGate()==null`·`results().size()==6`을 확인한다. 표본을 프로덕션 크기(10,000) 그대로 두지 않은 이유: 이 테스트가 확인하려는 건 "accept 경로에 실제로 도달하는가"이지 표본 크기가 아니고, G5 리플레이 겹까지 더해지면 이 테스트 하나가 40초 넘게 더 걸린다(실측: 표본 200으로 46초).

**3) 테스트 전용 오버로드의 "드리프트 방지"는 주장이었지 실체가 없었다 — D51 자체가 그 과장을 사실처럼 적어 놓았다.** D51은 "나머지 9개 트랩 테스트는 전부 오버로드 없는 `run(ctx)`를 그대로 써서 그 상수가 실제로 매 실행에 쓰인다는 것도 함께 고정한다"고 적었다(`task-15-report.md`도 같은 취지로 적었다). 틀렸다 — 함정 테스트는 전부 "어느 관문에서 걸리는가"만 단정하는데, 그건 표본 크기에 완전히 둔감하다(관문이 도는 순서는 표본이 10,000이든 100이든 바뀌지 않는다). `run(GateContext ctx)`의 본문을 `return run(ctx, 100, P99_LIMIT_MILLIS);`로 몰래 바꿔도 스위트 전체가 그대로 GREEN이었을 것이다 — 이 오버로드 자체가 이미 한 번 낳은 것과 정확히 같은 종류의 버그(파라미터가 조용히 무시되는 것)를 이번엔 값 자체가 조용히 줄어드는 방향으로 다시 낳을 수 있었다.

**조치 — 관측 가능한 가드를 만들었다.** `GateRunnerTest.프로덕션_경로는_SAMPLE_SIZE만큼_국면을_실제로_먹인다`를 추가했다. 국면 하나 볼 때마다 세는 봇을 만들어 프로덕션 진입점 `GateRunner.run(ctx)`(오버로드 없는 쪽)로 직접 돌린다. 카운터는 봇 클래스 자신의 필드로 못 둔다 — G2가 인스턴스 필드를 금지하고, `StatelessGate`는 `isSynthetic()` 필드만 건너뛴다. 그래서 테스트 메서드의 지역 변수 `int[] calls`를 익명 `Bot` 구현이 캡처하게 했다 — 캡처된 지역 변수는 컴파일러가 그 익명 클래스에 합성(synthetic) 필드로 넣으므로 G2가 건너뛰고, `calls[0]++`은 배열 원소 대입이라 금지 API 호출도 아니므로 G3도 건드리지 않는다(실제로 이 봇이 G2·G3를 통과해 G4까지 가는 걸 확인했다). 심사 시드는 1개로 좁혔다 — 시드 수에 비례하는 층(G5 리플레이 해시·G7 실전 대국)의 호출 수를 작게 눌러 둬야, 표본 크기에 비례하는 층(G4·G5 ①층·G6)의 변화가 총 호출 수에 뚜렷이 드러난다.

**가드가 실제로 드리프트를 잡는지 실측으로 확인했다.** 시드 1개로 프로덕션 `run(ctx)`를 돌리면 `calls=63,062`(G4 10,000 + G5 ①층 40,000 + G6 약 13,000 + 시드 1개짜리 나머지 층). 같은 조건에서 `run(ctx, 100, P99_LIMIT_MILLIS)`로(=SAMPLE_SIZE를 100으로 몰래 바꾼 상황을 그대로 재현) 돌리면 `calls=3,662`로 뚝 떨어진다 — `SAMPLE_SIZE`(10,000)를 한참 밑돈다. 그래서 단정을 `calls[0] >= GateRunner.SAMPLE_SIZE`로 걸었다 — 프로덕션 값에서는 여유 있게 통과하고(63,062 ≥ 10,000), 드리프트가 재현된 값에서는 실제로 실패한다(3,662 < 10,000이므로).

**D51의 과장을 바로잡는다(D51 본문은 편집하지 않는다).** D51이 "9개 트랩 테스트가 상수가 매 실행에 쓰인다는 것도 함께 고정한다"고 적은 문장은 틀렸다 — 그 테스트들이 실제로 고정하는 건 "어느 관문에서 걸리는가"라는 라우팅뿐이고, 그건 표본 크기와 무관하다. 상수가 실제로 쓰인다는 걸 고정하는 유일한 테스트는 이번에 추가한 `프로덕션_경로는_SAMPLE_SIZE만큼_국면을_실제로_먹인다`이며, 위 실측(63,062 vs 3,662)이 그 근거다.

**보류(원장에 미룸, 리뷰 지시).** `StandingTest`의 좌석 교대 픽스처가 "이름 기반 귀속"과 "swapped 기반 귀속"을 구분해서 보여주지 않는 것; `total()`과 빈 리스트 경로의 테스트 부재; `checkSafely`가 스택 트레이스를 버려서 하네스 버그가 봇 반려처럼 읽히는 것; `StackOverflowError`가 여전히 실행 전체를 죽이는 것; 48-시드 부분집합의 산출 근거가 코드로 고정돼 있지 않은 것.

**검증.** `./gradlew :arena-core:test --tests '*StandingTest*'` 5개(기존 3 + 신규 2) GREEN. `./gradlew :arena-gate:test --tests '*RegressionGateTest*'` 2개(기존 1 + 신규 1) GREEN, 12초 — 이름 충돌 테스트가 실제로 옛 버그를 잡는지는 별도 스크래치 프로브(저장소에는 남기지 않음)로 되돌려서 확인했다(위 1번 조치 참고). `./gradlew :arena-gate:test --tests '*GateRunnerTest*'` 12개(기존 10 + 신규 2) GREEN, 1분 32초. `./gradlew test --rerun` 전체 114개(D51 당시 109 + `StandingTest` 신규 2 + `RegressionGateTest` 신규 1 + `GateRunnerTest` 신규 2) GREEN, 1분 49초, 빌드 출력 오류·경고 없음. 커밋은 아래.

---

### D53. Task 15 세 번째 리뷰 반려 — 드리프트 가드의 여유가 6.3배라 절반짜리 드리프트를 놓쳤다

**리뷰 결과.** D52 대응 세 건은 전부 승인됐다(리뷰어가 코드를 직접 따라가며 확인: `seatOf`의 예외 분기는 정상 시리즈에서는 절대 안 걸린다는 것 — `RegressionGate`가 `Standing.of`의 유일한 프로덕션 호출자이고 `SUBJECT_ID`가 `SeriesRunner`가 만드는 모든 리플레이에 항상 있다는 것 둘 다 확인했다 —, `"subject"`로 바꿔도 반려 메시지가 원래도 베이스라인 이름만 담았지 subject 이름은 담은 적이 없어 진단 품질이 그대로라는 것, accept-path 테스트가 실제로 여섯 관문을 순서대로 다 돌고 하나도 건너뛰지 않는다는 것, 카운팅 픽스처가 진짜로 G2·G3를 통과한다는 것(캡처된 지역 변수가 합성 필드가 되어 `StatelessGate`가 건너뛴다는 것과 `calls[0]++`이 배열 대입이지 금지 호출이 아니라는 것 둘 다). 좌석 왜곡을 되돌려서 재현한 증거도 "그 테스트들이 실제로 무는지"를 보여주는 옳은 방법이었다고 확인해 줬다). 승인을 막은 건 새로 만든 가드 자체에 남은 문제 하나였다.

**문제.** `assertTrue(calls[0] >= GateRunner.SAMPLE_SIZE)`는 실측 63,062 대 문턱값 10,000을 비교한다 — 여유가 6.3배다. 이 정도 여유면 84% 넘게 무너져야(≈1,600 밑) 걸린다. 이 가드가 막으려던 버그 부류는 "몰래 더 작은 표본으로 판정하는 것"인데, 어중간한 드리프트는 그냥 통과한다 — 표본을 10,000에서 5,000으로 절반만 줄여도 호출 수는 약 33,062로, 여전히 문턱값 10,000을 훌쩍 넘어 테스트가 계속 GREEN이다. 정확히 이 가드가 막아야 할 것과 같은 모양의 실패인데 덜 극단적일 뿐이다.

**조치 — 하한을 고정값이 아니라 `SAMPLE_SIZE`의 배수로 걸었다.** 호출량이 표본 크기에 선형이다: G4는 국면 하나당 정확히 1번, G5 ①층은 정확히 `(1+DeterminismGate.REPEATS)`번(REPEATS=3이라 4배) 호출한다 — 이 봇처럼 G4·G5를 항상 통과하는 봇에게는 G6·G7이 시작되기도 전에 이미 `G4(1배)+G5①층(4배)=5배`가 확정적으로 쌓인다. 이 하한은 웜업 상한·시드 수·대국 길이 같은 G6·G7의 세부 사항에 기대지 않으므로, 그런 층이 나중에 바뀌어도 안 흔들린다. 배수 5(`SAMPLE_SIZE*5`=50,000)를 골랐다 — 정상 표본의 실측 비율(6.31배)보다 뚜렷이 낮고, 절반 표본의 실측 비율(3.31배)보다 뚜렷이 높다.

**실측으로 확인.**
```
floor (5x SAMPLE_SIZE) = 50,000
production calls=63,062 -> PASS (정상적으로 통과)
halved  calls=33,062 -> FAIL (절반 표본 드리프트를 실제로 잡는다)

(참고) old threshold (1x SAMPLE_SIZE) = 10,000
halved calls=33,062 vs old threshold -> PASS (옛 가드는 이 절반 드리프트를 놓쳤다)
```
옛 하한(1배)으로는 절반 표본이 통과해버렸다는 것과, 새 하한(5배)은 정확히 그 드리프트를 잡는다는 것을 나란히 실측으로 대조했다.

테스트의 javadoc에 이 유도 과정(G4 1배 + G5 ①층 4배 = 확정 하한 5배)과, `DeterminismGate.REPEATS`나 G4/G5의 호출 패턴이 나중에 의도적으로 바뀌면 이 배수를 다시 유도해야 한다는 것을 함께 적어 뒀다 — 그때 이 테스트가 요란하게 실패하는 게 맞는 동작이다.

**손대지 않은 것.** `SAMPLE_SIZE`·`P99_LIMIT_MILLIS`·어떤 관문의 로직도 숫자를 맞추려고 바꾸지 않았다 — 바뀐 건 테스트의 문턱값 계산 방식(고정값 → 배수)뿐이다.

**검증.** `./gradlew :arena-gate:test --tests '*GateRunnerTest*'` 12개 GREEN, 1분 32초(테스트 개수는 그대로 — 기존 가드 테스트를 고쳤을 뿐 새 테스트를 추가하지 않았다). `./gradlew test --rerun` 전체 114개 GREEN(D52 이후와 동일한 개수), 빌드 출력 오류·경고 없음. 커밋은 아래.

---

### D54. Task 16 — 챔피언전·승격 판정, 이름 충돌을 예약 id로 원천 차단, 회귀 픽스처 실측 오류 정정

**작업.** `arena-tournament` 모듈을 새로 만들고 `Championship.judge(Bot challenger, Bot champion, judgingSeeds, holdoutSeeds, width, height)` → `ChallengeReport`를 구현했다. 심사(시드 1‥50, 100경기) 승점 승률이 `PROMOTION_THRESHOLD=0.60` 이상이면 승격, 승격한 봇에게만 홀드아웃(시드 1001‥1050) 승률을 추가로 재서 함께 기록한다(승격 판정 자체엔 홀드아웃을 쓰지 않는다 — 판정 이후에 재는 참고 지표). 반려되면 패배 경기에서 손실이 큰 수 상위 3개를 `DiagnosisEntry`로 돌려준다. `Gen00Bot`(동작은 `StraightBot`과 동일, 관문 대상이 아님)을 챔피언 계보의 출발점으로 심었다.

**결정 1 — 도전자·챔피언 이름 충돌을 내부 예약 id로 원천 차단.** 이전 태스크(D52)에서 `Standing.seatOf`가 subjectId를 못 찾으면 `IllegalArgumentException`을 던지도록 고쳐졌다. `RegressionGate`는 신뢰할 수 없는 쪽이 한쪽(subject)뿐이라 `SUBJECT_ID="subject"` 하나로 풀렸지만, `Championship`은 도전자·챔피언 **둘 다** 제출된 봇이라 `bot.name()` 둘 다 신뢰할 수 없다. `challenger.name().equals(champion.name())`이면(악의적 제출이든, 우연이든, 심지어 같은 인스턴스를 양쪽에 넘기는 테스트든) 옛 방식대로 `bot.name()`을 그대로 `SeriesRunner`·`Standing`에 넘기면 챔피언전 전체가 예외로 죽는다.

**조치.** `Championship`에 서로 다른 게 보장된 내부 상수 `CHALLENGER_ID="__challenger__"`, `CHAMPION_ID="__champion__"`을 두고, `bot.name()`은 좌석 식별에 전혀 쓰지 않는다 — `SeriesRunner.run`·`Standing.of`·`Standing.seatOf`엔 전부 이 예약 id를 넘긴다. `bot.name()`은 `ChallengeReport.challenger()`/`champion()` 필드(표시용)에만 쓰인다. 이러면 도전자·챔피언 이름이 같아도, 심지어 같은 봇 인스턴스가 두 자리에 다 들어와도 판정은 정상적으로 끝난다 — 제출된 봇이 이름 하나로 하네스를 무너뜨릴 수 없다. `diagnose()`도 브리프 초안의 `r.bot0Id().equals(subjectId) ? 0 : 1`(D52가 정확히 이 패턴 때문에 `Standing.seatOf`를 만들게 된 그 취약 패턴이다) 대신 `Standing.seatOf(r, CHALLENGER_ID)`를 그대로 쓴다.

**테스트로 고정.** `도전자와_챔피언_이름이_같아도_판정이_죽지_않는다`(생성자로 이름을 지정할 수 있는 `NamedStraight`를 양쪽에 같은 이름으로 넘김), `같은_인스턴스로_붙어도_판정이_죽지_않는다`(같은 `Gen00Bot` 인스턴스를 도전자·챔피언 양쪽에 넘김). 둘 다 `assertDoesNotThrow`로 판정이 끝까지 완주하는지 확인한다.

**결정 2 — 60% 문턱의 포함 경계를 실제 대국이 아니라 직접 값으로 찌른다.** "정확히 0.60이면 승격해야 한다"(기준은 이상이지 초과가 아니다)를 실제 봇 대국으로 재현하려면 100경기 중 정확히 60승(또는 승·무 조합)이 나오게 봇을 조작해야 하는데, 그건 그 자체로 취약한 픽스처다. 대신 판정 로직을 `static boolean meetsThreshold(Standing standing)`로 뽑아 패키지 전용으로 열어 두고, `new Standing(60, 0, 40, 0.60)`을 직접 만들어 `assertTrue(meetsThreshold(...))`로, `new Standing(59, 1, 40, 0.595)`로 `assertFalse(...)`로 경계 양쪽을 각각 결정론적으로 고정했다.

**결정 3 — 브리프가 준 회귀 픽스처(`SlightlyDifferentStraight`) 자체가 실측과 어긋났다, 코드를 고쳐 의도를 살렸다.** 브리프 초안은 "직진봇과 거의 같지만 이름만 다르다, 승격 기준을 못 넘어야 한다"는 주석과 함께, "격자 밖으로 나갈 때만 한 번 꺾는다"는 로직을 준다. RED 확인 후 GREEN 단계에서 그대로 돌려 보니 이 테스트가 **실패**했다 — `Gen00Bot` 상대 승점 승률이 0.89로 나와 승격돼 버렸다. 실측해 보면 이유가 분명하다: 직진봇은 벽에 그대로 박아 죽지만, 이 픽스처는 첫 벽 충돌 직전 단 한 번만 피하고 그 뒤로도 죽지 않는 한 계속 산다 — "이름만 다르다"는 주석과 달리 실제로는 생존력이 크게 다른 별개의 전략이었다.

**조치.** 회피 로직을 빼 `move()`를 `return view.myDir();`(직진봇과 완전히 동일한 전략)로 바꿨다. 좌석 교대로 평균이 대칭에 수렴한다는 실측: `wins=50 draws=0 losses=50, scoreRate=0.5` — 60% 문턱을 넉넉히 밑도는 안정적인 회귀 픽스처가 됐다. `PROMOTION_THRESHOLD`나 관문 로직 쪽은 전혀 건드리지 않았다(BRIEF §11-4) — 문턱을 통과 못 시킨 건 픽스처였지, 문턱이 아니었다.

**결정 4 — 진단 필드 명명 매핑.** `MoveAnalysis.reachAfterBest()` → `DiagnosisEntry.reachIfBest`, `MoveAnalysis.reachAfterChosen()` → `DiagnosisEntry.reachChosen`(스펙 §7.1의 `reachBefore`/`reachAfter`보다 의미가 정확한 이름 — 계획 완료 후 스펙 문서를 이 이름으로 맞출 것). 패배 경기당 `LossAnalyzer.worstMoves(r, mySeat, 1)`로 최악의 수 1개씩 뽑아 모은 뒤, 손실 큰 순으로 정렬해 상위 3개만 리포트에 담는다.

**검증.** RED: `./gradlew :arena-tournament:test` — `Championship`·`ChallengeReport`·`DiagnosisEntry`가 없어 컴파일 실패(14개 컴파일 에러), 예상대로. GREEN(1차): 최소 구현 후 11개 중 3개 실패(`같은_봇끼리_붙으면_승격하지_못한다`, `반려되면_진단이_붙는다`, `반려되면_홀드아웃은_돌리지_않는다`) — 원인은 위 결정 3의 픽스처 오류. 픽스처 수정 후 GREEN(2차): `./gradlew :arena-tournament:test` 11개 전부 GREEN. `./gradlew test --rerun` 전체 125개(114+11) GREEN, 3분 내외, 빌드 출력 오류·경고 없음.

**손대지 않은 것.** `StraightBot`·`RandomBot`·`WallAvoidBot`(베이스라인 3종, 동결) 미수정. `PROMOTION_THRESHOLD`·심사/홀드아웃 시드 범위·격자 크기 등 어떤 상수도 브리프 값 그대로.

---

### D55. Task 16 리뷰 반려 — D54의 "예외를 던진다" 서술을 정정, 무승부 반려 진단 공백, 충돌 테스트 보강

**리뷰 결과.** 결정 1(예약 id로 이름 충돌 원천 차단)과 결정 3(`diagnose()`가 `Standing.seatOf`를 재사용해 D52가 잡았던 그 취약 패턴을 다시 베끼지 않은 것)은 승인됐다. 반려는 세 갈래: (0) D54 본문이 리뷰 지시 자체에 있던 오류를 그대로 옮겨 적었고, (1) 이름 충돌 테스트가 크래시하지 않는다만 확인해 정작 막아야 할 조용한 결과 왜곡은 놓칠 수 있었고, (2) 패배가 0인 반려에서 진단이 통째로 빌 수 있었다.

**0) D54의 "이름이 충돌하면 `Standing.seatOf`가 예외를 던진다" 서술이 틀렸다 — 되짚어 보니 리뷰 쪽 오류였고, 그 오류를 그대로 옮겨 적었다.** `Standing.seatOf`를 다시 읽으면: `bot0Id`를 먼저 검사해 맞으면 즉시 `0`을 반환한다. `bot0Id == bot1Id`(이름 충돌)이면 이 첫 검사에서 바로 걸려 조용히 `0`을 돌려주고, 예외를 던지는 두 번째 분기(`bot0Id`도 `bot1Id`도 안 맞을 때)까지 갈 일이 없다. 예외는 subjectId가 그 리플레이에 아예 없는 경우(오타 등)에만 나온다. `Championship.java`의 클래스 javadoc, `ChampionshipTest.java`의 두 충돌 테스트 javadoc, D54 본문(위) 세 곳 모두 "예외를 던진다"고 적었던 게 틀렸다 — Championship과 테스트 javadoc은 이번에 고쳤고(아래 1번 조치), D54 본문은 규칙대로 편집하지 않고 이 항목으로 정정한다. 진짜 위험은 크래시가 아니라 **조용한 증거 반전**이었다: 이름이 충돌하면 도전자가 매 경기 좌석 0으로 조용히 귀속돼, 좌석 교대(swapped) 절반에서 실제로 챔피언이 앉은 좌석 0의 결과까지 도전자 몫으로 잘못 집계될 수 있다.

**1) 충돌 테스트가 `assertDoesNotThrow`·이름 필드·경기 수만 봐서, 막으려던 왜곡이 나도 초록일 수 있었다.** `seatOf`가 예외 대신 조용히 `0`을 반환하므로, `bot.name()`을 다시 좌석 id로 쓰는 회귀가 나도 죽지 않고, 100경기 다 돌고, 이름 필드도 채워진다 — 옛 테스트의 단정 전부가 통과한다.

**조치.** 두 충돌 테스트(`도전자와_챔피언_이름이_같아도_판정이_죽지_않는다`, `같은_인스턴스로_붙어도_판정이_죽지_않는다`)에 `assertEquals(0.5, r.scoreRate(), 1e-12)`와 `assertFalse(r.promoted())`를 추가했다. 두 봇의 전략이 완전히 같으므로 0.5는 우연이 아니라 구조적으로 강제된다 — 같은 시드의 교대 경기는 같은 궤적을 좌석만 바꿔 재생하고, 좌석의 승패는 전략이 아니라 시작 배치가 정하므로 정방향·교대 각각에서 좌석 0이 항상 이겨 시드마다 정확히 1승 1패가 나온다.

**되돌려서 실제로 물리는지 확인.** `judge()` 내부에서 `CHALLENGER_ID`/`CHAMPION_ID` 대신 `challenger.name()`/`champion.name()`을 그대로 쓰도록 임시로 되돌려 같은 테스트를 돌렸다:
```
ChampionshipTest > 같은_인스턴스로_붙어도_판정이_죽지_않는다() FAILED
    expected: <0.5> but was: <0.48>
ChampionshipTest > 도전자와_챔피언_이름이_같아도_판정이_죽지_않는다() FAILED
    expected: <0.5> but was: <0.48>
```
두 테스트 모두 실패했고, 값은 정확히 0.5가 아닌 0.48로 나왔다(정확히 1.0에 가깝게 쏠릴 거라 예상했던 것과 달리, 시드별로 "정방향·교대가 같은 좌석을 이중으로 세는" 효과가 시드마다 어느 좌석이 이기느냐에 따라 양방향으로 갈려 평균은 0.5 근처로 몰리되 정확히 0.5는 아니게 나온다 — 그래서 이 단정은 완화된 근사 비교가 아니라 정확한 `assertEquals(..., 1e-12)`여야 실제로 문다). 확인 후 원래 코드(`CHALLENGER_ID`/`CHAMPION_ID`)로 복원했고, `diff`로 복원본이 리뷰 이전 코드와 완전히 같음을 확인했다.

**2) 패배가 0인 반려에서 진단이 통째로 빌 수 있었다.** `diagnose()`가 `lost`(패배)만 보고 후보를 모았다 — 900턴 상한이나 정면 충돌은 무승부로 끝나므로, "죽진 않지만 이기지도 못하는" 초기 세대(승 0~19, 패 0, 나머지 무승부)는 승점 승률이 0.60 밑인데 패배 경기가 하나도 없어 진단이 빈 리스트로 나갔다 — 다섯 번뿐인 재시도 하나를 단서 없이 태운다.

**조치.** `diagnose()`를 패배 → (없으면) 무승부 → (그래도 없으면) 전체 경기 순으로 후보를 넓혀가도록 고쳤다(`collect(replays, subjectId, outcome)` 보조 메서드, `ChallengeOutcome{LOSS,DRAW,ANY}`). 무승부에도 `MoveAnalysis.fatal`이 정면 충돌 턴을 그대로 표시하므로 `worstMoves`는 이 경우에도 결정적인 수를 앞세운다. `diagnose`는 내부 상수를 직접 참조하지 않고 `subjectId`를 인자로 받도록 바꾸고 패키지 전용으로 열었다 — 테스트가 손으로 만든 `Replay`로 무승부 전용 경로를 직접 찌를 수 있게 하려는 목적이다.

**테스트로 고정.** `패배없이_무승부만_있어도_진단이_비지_않는다` — 5×5 격자에 손으로 만든 1턴짜리 정면 충돌 `Replay`(시작 위치를 마주보게 배치해 둘 다 같은 칸을 동시에 노리게 함, `MatchResult(-1, 1, HEAD_ON_COLLISION)`)를 `Championship.diagnose(List.of(headOn), "subject")`로 직접 넘겨 진단이 비지 않음을 확인한다. 실제 대국으로 "패배 0·무승부 존재"를 100경기 전부 재현 가능하게 만들려면 두 봇을 정교하게 설계해야 하는데 그건 그 자체로 취약한 픽스처라 판단해, 대신 이미 `meetsThreshold` 테스트에 쓴 것과 같은 방식(로직을 패키지 전용으로 열어 손으로 만든 값으로 직접 찌름)을 재사용했다. 덧붙여 `같은_봇끼리_붙으면_승격하지_못한다`에도 `assertFalse(r.diagnosis().isEmpty())`를 추가해, 반려 테스트마다 진단 비어있지 않음을 최대한 함께 확인하도록 넓혔다.

**검증.** `./gradlew :arena-tournament:test` 12개(기존 11 + 신규 1) GREEN. `./gradlew test --rerun` 전체 126개(114+12) GREEN, 1분 44초, 빌드 출력 오류·경고 없음.

**손대지 않은 것(리뷰가 원장에 미루라고 지시한 항목).** 60% 경계가 `judge()`를 통하지 않고 `meetsThreshold`만으로 핀된 것, `holdoutScoreRate() > 0.0` 단정이 이름이 말하는 것보다 약한 것, 미사용 `arena-gate`/Jackson 의존, 시드 리스트에 대한 입력 검증 부재, `DiagnosisEntry`가 `fatal`을 안 실어 매치 간 재정렬이 fatal-우선 순서를 흐트러뜨리는 것, `log.md`와 리포트 사이 스위트 실행 시간 표기가 어긋난 것.

---

### D56. Task 17 — 기록 저장소, nextAttempt는 5회 한도를 강제하지 않고 보고만 한다

**작업.** `arena-tournament`에 `RecordStore(Path root)`를 구현했다. `saveGateReport`/`saveChallengeReport`/`saveReplays`로 세대·시도별 디렉터리(`gen-%02d/attempt-N/`)에 봇 소스와 관문·챔피언전 리포트를 JSON으로 남기고, `historyOf(generation)`로 시도 이력을 번호 순으로 되읽는다. `AttemptRecord`가 한 시도(세대·번호·verdict·stage·failedGate·detail)를 표현한다. D54~D55에서 이미 미사용 상태로 지적됐던 Jackson(`jackson-databind:2.18.2`)이 이 태스크에서 처음 실사용됐다.

**결정 1 — `nextAttempt`는 재시도 한도(5회, BRIEF §7)를 강제하지 않고 다음 번호를 보고만 한다.** 브리프 인터페이스는 `int nextAttempt(int generation)`으로, 예외를 던질지 캡을 걸지에 대한 언급이 없다. 두 선택지를 저울질했다: (a) 5회를 넘으면 예외를 던져 저장소 스스로 한도를 지킨다, (b) 그냥 다음 번호를 계속 보고하고 한도 판단은 호출자(다음 태스크의 세대 루프)에 맡긴다. (a)를 기각한 이유는 CLAUDE.md §2("반려 이력을 지우지 않는다")와 정면으로 부딪히기 때문이다 — 저장소가 6번째 저장 시도 자체를 예외로 막아버리면, "재시도가 다섯 번을 넘었다"는 사실을 기록할 방법이 없어진다. 한도를 넘겼는데도 무슨 일이 있었는지 알아야 CONVERGED 판정의 증거가 남는다. (b)를 택했다: `RecordStore`는 "무슨 일이 있었는지"의 진실만 담고, "다음에 무엇을 허용할지"는 세대 루프의 책임으로 명확히 분리했다. 경계를 `다섯_번째_시도를_넘겨도_nextAttempt는_예외_없이_다음_번호를_보고한다` 테스트로 고정했다 — 시도 5개를 저장한 뒤 `nextAttempt(9)`가 예외 없이 6을 돌려주고, `historyOf(9)`에 5개 전부가 남는지 확인한다.

**결정 2 — 바이트 단위 재현성은 record 필드 순서에 이미 내재돼 있다, 그래도 직접 증명한다.** Jackson은 record의 프로퍼티 순서를 바이트코드의 컴포넌트 선언 순서에서 얻으므로(2.12+ record 지원), HashMap 반복 순서 같은 비결정성이 끼어들 여지가 없다 — 직렬화 대상(`GateReport`, `ChallengeReport`, `DiagnosisEntry`, `Replay`, `AttemptRecord`) 어디에도 `Map`이 없다. 그래도 추론에 기대지 않고, 같은 `GateReport`/`ChallengeReport` 인스턴스를 서로 다른 두 세대(1, 2)에 저장해 `Files.readAllBytes`로 바이트 배열 자체를 `assertArrayEquals`로 비교하는 테스트 두 개를 추가했다. 타임스탬프 등 시간 의존 필드는 애초에 어떤 record에도 없다.

**결정 3 — `holdoutScoreRate`의 `NaN`은 별도 설정 없이 문자열 `"NaN"`으로 안전하게 왕복된다, 확인 후 그대로 둔다.** JSON 문법에 NaN 리터럴이 없어 처리를 정해야 했다. 별도 클래스파일로 Jackson 기본 `ObjectMapper` 동작을 직접 찔러봤다: `writeValueAsString`은 `{"x":"NaN"}`(따옴표 있는 문자열)을 내놓고, 같은 기본 매퍼로 `readValue`가 별도 `ALLOW_NON_NUMERIC_NUMBERS` 설정 없이 그대로 `Double.NaN`으로 되읽는다. 커스텀 직렬화기나 `JsonWriteFeature` 설정을 추가할 이유가 없었다 — 이미 유효한 JSON이고 왕복도 된다. `반려_리포트의_홀드아웃_승률_NaN이_문자열로_안전하게_직렬화된다` 테스트로 `"holdoutScoreRate" : "NaN"` 문자열 존재와 기본 `ObjectMapper`로의 왕복(`Double.isNaN`)을 함께 고정했다.

**결정 4 — 봇이 통제하는 문자열은 어디서도 경로 구성에 쓰지 않는다.** 이 하네스에서 봇 제출 문자열(이름, gate detail 등)이 공격 표면이 된 전례가 있다(D54~D55, 좌석 id로 오용된 사례). `RecordStore`의 경로는 `root/gen-%02d/attempt-%d/...`로만 구성되고, 두 자리 모두 하네스가 넘기는 `int`다 — `GateReport.botName`/`failedGate`/`detail`, `botSource` 문자열은 전부 JSON *값*이나 파일 *내용*으로만 들어가고 `Path.resolve`엔 한 번도 넘어가지 않는다. `봇_소스에_경로_탈출_문자열이_있어도_root_밖으로_새지_않는다` 테스트로, 봇 소스에 `../../../../etc/passwd`를 흉내낸 문자열과 gate id에 `../../G3`를 넣어도 지정된 `bot.java` 자리에 그대로 내용으로만 쓰이고 `root` 바깥에는 아무 것도 생기지 않음을 확인했다.

**추가로 고정한 것.** 브리프가 준 6개 테스트 외에, 인터페이스에는 있지만 브리프 예시엔 없던 `saveReplays`를 커버하는 테스트(`리플레이를_JSON으로_남긴다`)와, `historyOf`의 빈 세대 경계(`시도가_없는_세대의_이력은_빈_리스트다`, 예외 없이 빈 리스트)를 추가했다.

**바로잡은 것 1 — javadoc.** 브리프의 `AttemptRecord` javadoc은 "verdict는 PROMOTED 또는 REJECTED"라고 적었지만, 브리프 자신의 최소 구현 코드는 관문만 통과했을 때 `"PASSED"`를 쓴다(챔피언전까지 가야 PROMOTED다). javadoc이 실제 구현이 내는 세 번째 값을 누락한 것이므로, 코드 동작(PASSED/PROMOTED/REJECTED 세 값)을 정확히 반영하도록 javadoc을 고쳤다 — 동작 자체는 브리프 그대로다.

**바로잡은 것 2 — 자체 리뷰에서 찾은 부작용: `historyOf`(읽기)가 없는 시도 디렉터리를 만들 수 있었다.** 커밋 전 자체 리뷰에서 코드를 다시 읽다 발견했다. 브리프의 최소 구현은 `historyOf`가 각 시도를 순회할 때 `attemptDir(generation, attempt)`를 호출하는데, 이 메서드는 `save*`용으로 만들어진 것이라 호출할 때마다 `Files.createDirectories`로 디렉터리를 만든다. 시도 번호가 연속(1,2,3,...)일 때는 이미 존재하는 디렉터리라 `createDirectories`가 아무 일도 안 해 무해하지만, 시도 사이에 빈틈이 있으면(예: attempt-1, attempt-3만 있고 attempt-2가 없으면 `nextAttempt`가 4를 돌려주고 `historyOf`가 1~3을 순회) 조회만 했을 뿐인데 빈 `attempt-2/` 디렉터리가 디스크에 새로 생겨버린다 — 읽기 전용이어야 할 메서드가 파일시스템에 쓰기 부작용을 낸다.

**조치.** 디렉터리를 만들지 않고 경로만 계산하는 `attemptPath`를 따로 두고, `historyOf`는 이걸 쓰도록 바꿨다. `attemptDir`(만드는 쪽)은 `save*` 계열에만 남겼다. `이력_조회는_비어있는_시도_번호의_디렉터리를_만들지_않는다` 테스트로 고정했다 — attempt-1·attempt-3만 저장한 뒤 `historyOf(4)`를 호출하고 `attempt-2` 디렉터리가 생기지 않았음을 확인한다. 이 테스트는 수정 전 코드에 대고 돌리면 실패한다(직접 확인 후 되돌려 재확인).

**검증.** RED: `./gradlew :arena-tournament:test --tests '*RecordStoreTest*'` — `RecordStore`, `AttemptRecord` 없어 12개 컴파일 에러, 예상대로. GREEN(브리프 6개): 같은 명령, 6개 전부 PASS. 이후 4가지 요구사항(재현성·NaN·5회 한도·경로 탈출) 검증 테스트 7개와, 자체 리뷰로 찾은 부작용을 고정하는 테스트 1개를 더해 `RecordStoreTest` 14개 전부 GREEN. `./gradlew test --rerun` 전체 140개(126+14) GREEN, 2분 5초, 빌드 출력 오류·경고 없음. 매 테스트가 `@TempDir`을 쓰므로 저장소 루트의 `records/`(`.gitignore` 대상)는 어떤 테스트에서도 생성되지 않았다 — `git status`로 확인.

**손대지 않은 것.** `RecordStore`는 저장·조회만 하고 세대 루프(다음 태스크)가 할 CONVERGED 판정이나 5회 한도 강제는 구현하지 않았다. `saveReplays`가 저장하는 `replays.json`을 다시 읽어오는 메서드는 인터페이스에 없어 추가하지 않았다.

---

### D57. Task 17 리뷰 반려 — 성공 경로(PASSED/PROMOTED) 미검증, 개행 문자가 OS에 의존

**리뷰 결과.** D56의 경로 탈출 방어, 바이트 단위 재현성, NaN 처리, `nextAttempt` 보고-전용 결정, `historyOf`의 명시적 순회, 부작용 제거(2차 커밋)는 전부 실제로 검증했다며 승인. 반려는 두 갈래.

**1) 성공 verdict(PASSED, PROMOTED) 두 갈래가 14개 테스트 어디서도 실행되지 않았다.** `historyOf`의 `r.passed() ? "PASSED" : "REJECTED"`와 `r.promoted() ? "PROMOTED" : "REJECTED"` 두 삼항식에서, 테스트 픽스처가 전부 `rejected(...)`(관문)와 `challengeRejected()`(챔피언전, `promoted=false`)뿐이라 두 삼항식 모두 `false` 분기만 밟혔다. 특히 D56 본문이 "브리프 javadoc의 PROMOTED/REJECTED 이분법이 틀렸다, 실제로는 PASSED가 있는 세 값이다"라고 직접 정정해 놓고, 정작 그 세 번째 값(PASSED)과 승격 케이스(PROMOTED)를 검증하는 테스트가 하나도 없었다 — 이 태스크의 존재 이유("관문을 통과했는지, 승격했는지, 왜 아닌지")의 절반(통과·승격)이 미검증 상태로 커밋됐다.

**조치.** `passed()`(관문 통과 픽스처)와 `challengePromoted()`(승격 픽스처, `promoted=true`, `holdoutScoreRate=0.62`, 진단 빈 리스트)를 추가하고, `관문을_통과하면_이력에_PASSED로_남는다`·`챔피언전에서_승격하면_이력에_PROMOTED로_남는다` 두 테스트로 각각 `verdict`·`stage`·(관문 쪽은) `failedGate==null`을 고정했다. 실제로 물리는지 두 삼항식을 뒤집어(`r.promoted() ? "REJECTED" : "PROMOTED"`, `r.passed() ? "REJECTED" : "PASSED"`) 같은 스위트를 돌렸다:
```
RecordStoreTest > 챔피언전에서_승격하면_이력에_PROMOTED로_남는다(Path) FAILED
RecordStoreTest > 이력에_반려_사유가_순서대로_쌓인다(Path) FAILED
RecordStoreTest > 관문을_통과하면_이력에_PASSED로_남는다(Path) FAILED
17 tests completed, 3 failed
```
새로 추가한 2개 외에 브리프가 준 `이력에_반려_사유가_순서대로_쌓인다`도 같은 뒤집기에 걸렸다(그 테스트가 반려 케이스의 `verdict=="REJECTED"`를 이미 확인하고 있었으므로, 뒤집으면 반려도 잘못된 값을 받는다) — 우연한 이중 방어다. 확인 후 원래 코드로 복원, `diff`로 리뷰 이전 코드와 완전히 같음을 확인했다.

**2) `SerializationFeature.INDENT_OUTPUT`의 기본 pretty printer가 `System.lineSeparator()`를 쓴다.** 리눅스에서는 LF라 지금까지 아무 테스트도 이 차이를 드러내지 못했지만, 이 저장소를 만든 기계와 나중에 다시 읽고 검증하는 기계의 OS가 다르면(예: 윈도우에서 CRLF로 다시 씀) 같은 입력이 다른 바이트를 낸다 — "바이트 단위로 재현 가능하다"는 이 태스크의 핵심 주장이 딱 이 지점(산출물을 쓰는 유일한 곳)에서 OS에 종속돼 있었다. 리뷰가 지적한 대로, 같은 JVM 안에서 두 번 직렬화해 비교하는 기존 테스트로는 이 차이를 애초에 잡을 수 없다(OS가 하나뿐이므로).

**조치.** `DefaultPrettyPrinter`에 `DefaultIndenter("  ", "\n")`을 객체·배열 양쪽에 명시적으로 박아 `ObjectMapper.setDefaultPrettyPrinter`로 지정했다 — 개행이 더 이상 `System.lineSeparator()}`를 참조하지 않고 리터럴 `"\n"`으로 고정된다. 직접 확인용 별도 스크립트로 기본 pretty printer와 고정한 pretty printer의 출력 바이트를 비교했는데, 리눅스에서는 (예상대로) 바이트가 동일했다 — 이 환경에서 회귀를 잡는 테스트가 아니라, "설정을 OS에 맡기지 않는다"는 구조적 보장이라는 점을 리포트에 명시했다. 회귀 테스트 `JSON_출력에_CR이_섞이지_않는다`는 gate-report.json·championship.json 바이트에 `\r`(0x0D)이 하나도 없음을 확인한다 — 이 역시 리눅스에서는 수정 전 코드로도 통과하는 테스트지만(고정하지 않아도 우연히 LF), 명시적 인덴터가 실제로 무엇을 출력하는지 문서화하는 용도로 남겨둔다.

**검증.** `./gradlew :arena-tournament:test --tests '*RecordStoreTest*'` 17개(기존 14 + 신규 3: PASSED 1, PROMOTED 1, CR 부재 1) 전부 GREEN. `./gradlew test --rerun` 전체 143개(126+17) GREEN, 2분 6초, 빌드 출력 오류·경고 없음. `records/`는 이번에도 생성되지 않았다.

**손대지 않은 것(리뷰가 원장에 미루라고 지시한 항목).** `nextAttempt`가 손상된 `attempt-*` 디렉터리 이름(숫자가 아닌 접미사)을 만나면 `NumberFormatException`을 감싸지 않고 그대로 던지는 것, `historyOf`가 `attemptDir()`은 만들어졌지만 아직 어느 리포트 JSON도 쓰이지 않은(쓰기 도중 죽은) 시도 디렉터리를 조용히 건너뛰는 것.

---

### D58. Task 18 — 라운드로빈과 발표 번들: 브리프 예시 코드의 좌석 귀속 버그를 실제로 밟았다

**결정.** `RoundRobin.run`과 `BundleBuilder.buildStats` 둘 다 `bot.name()`을 SeriesRunner/Standing의 좌석 id로 쓰지 않는다. 각각 서로 다른 게 보장된 내부 예약 id 쌍(`__roundrobin_a__`/`__roundrobin_b__`, `__generation__`/`__champion__`)을 쓰고, 실제 좌석은 항상 `Standing.seatOf`로 판정한다. `Championship`(D54)이 이미 세운 패턴을 그대로 따랐다.

**왜 이게 브리프의 "최소 구현" 예시 코드를 그대로 옮기지 않은 지점인가.** 태스크 브리프 Step 3이 준 `RoundRobin`·`BundleBuilder` 초안은 각각 `a.name()`/`b.name()`, `bot.name()`/`champion.name()`을 SeriesRunner id로 그대로 넘기고, `BundleBuilder`는 좌석 판정도 `r.bot0Id().equals(bot.name()) ? 0 : 1`로 직접 문자열을 비교한다 — 이건 Task 16(D54, D55)이 이미 잡아냈던 바로 그 안티패턴이다. `Standing.seatOf`가 `bot0Id`를 먼저 검사하는 탓에, subjectId가 bot0Id와도 bot1Id와도 정확히 일치하지 않으면(둘 다 같은 문자열이라 사실상 항상 일치) 어느 쪽이 진짜 그 자리에 앉았는지와 무관하게 조용히 좌석 0으로 귀속된다. 좌석 교대 경기(전체의 절반)에서는 이게 통계를 통째로 뒤집는다.

그리고 이 버그는 가상의 걱정이 아니라 이 태스크 자신의 테스트 픽스처에서 실제로 발동한다: `BundleBuilderTest`의 픽스처는 `generations = [Gen00Bot, RandomBot, WallAvoidBot]`이고 `finalChampion = new WallAvoidBot()`(별도 인스턴스, 같은 이름) — 즉 마지막 세대와 최종 챔피언의 이름이 정확히 같다. 브리프 초안 코드로 이 픽스처를 돌리면, `WallAvoidBot` 세대의 좌석 교대 경기 10판(전체 20판의 절반)에서 `r.bot0Id()`가 항상 "WallAvoidBot"이 되어(id0==id1이므로 SeriesRunner의 스왑이 실제로 자리를 바꿔도 문자열은 똑같다) `mySeat`이 항상 0으로 잘못 판정된다 — occupancy·suicideRate 계산에 실제로는 자신이 앉지 않은 좌석의 배열 인덱스를 쓰게 된다.

**참여자 식별과 이름 충돌.** `RoundRobin.run(List<Bot>, ...)`이 짝짓는 두 봇의 이름이 같아도(우연이든, 이름이 겹치는 두 세대를 실수로 같이 넣었든) 행렬이 왜곡되지 않아야 한다는 요구를 `이름이_같은_두_봇이_섞여도_행렬이_왜곡되지_않는다` 테스트로 고정했다 — 강한 쪽(WallAvoidBot 위임)과 약한 쪽(Gen00Bot 위임)에 똑같이 "Dup"이라는 이름을 준 래퍼 두 개를 붙여서, 승률이 뭉개지지 않고 실제로 강한 쪽이 이긴 걸로 정확히 기록되는지까지 확인한다(`assertDoesNotThrow`만으로는 조용한 증거 반전을 못 잡는다 — D55와 같은 이유).

**행렬 방향과 대각선.** `[i][j]`는 `bots.get(i)`가 `bots.get(j)` 상대로 낸 승점 승률로 못박고 javadoc에 명시했다. `벽회피봇이_직진봇을_압도한다`(`m[2][0] > 0.8`) 테스트는 방향을 검증하는 역할도 겸한다 — 행과 열이 뒤집힌 구현(`matrix[j][i] = rateA` 식)이었다면 `m[2][0]`은 반대로 0.2 밑으로 나와 이 테스트가 잡아냈을 것이다. `마주보는_칸의_승률은_합이_1이다`은 `[i][j]+[j][i]==1.0`을 모든 쌍에 대해 검증하고, 대각선은 `Arrays.fill(row, NaN)`으로 미리 채운 뒤 자기 자신과는 절대 대전시키지 않는(`j = i+1`부터 시작) 루프 구조로 항상 NaN을 유지한다.

**바이트 동일성.** `BundleBuilder`도 `RecordStore`(D57)와 같은 장치를 쓴다 — `DefaultPrettyPrinter`의 객체·배열 인덴터 양쪽에 리터럴 `"\n"`을 박아 개행을 OS에 맡기지 않는다. `같은_입력은_두_번_만들어도_바이트_단위로_동일한_번들을_낸다` 테스트가 네 파일(gallery/generations/loop-history/roundrobin) 전부를 서로 다른 두 출력 디렉터리에 두 번 만들어 바이트 배열을 직접 비교한다.

**NaN 왕복.** `roundrobin.json`의 대각선은 Jackson 기본 동작대로 `"NaN"` 문자열로 직렬화된다(RecordStore의 `holdoutScoreRate`와 같은 처리, D56). `라운드로빈_대각선의_NaN이_왕복된다` 테스트는 이걸 가정하지 않고 실제로 plain `ObjectMapper`로 `record RoundRobinBundle(List<String> bots, double[][] matrix)`에 역직렬화해 `Double.isNaN`을 확인한다.

**라운드로빈 시드를 브리프 초안과 다르게 처리했다.** 브리프 Step 3 초안은 `roundrobin.json`의 시드를 `judgingSeeds.subList(0, Math.min(10, judgingSeeds.size()))`로 만든다 — 호출자가 넘기는 `judgingSeeds`가 마침 1부터 시작하는 오름차순일 때만 전역 제약("라운드로빈 시드 = 1‥10")과 우연히 일치하는 취약한 파생이다. 대신 `BundleBuilder`에 `ROUND_ROBIN_SEEDS = 1..10`을 상수로 못박았다 — 심사 시드 목록의 크기·정렬 순서가 바뀌어도 라운드로빈 시드는 전역 제약값 그대로 고정된다.

**기각한 대안**
| 대안 | 기각 사유 |
|---|---|
| 브리프 Step 3 초안을 그대로 구현 | 위에서 실측했듯 이 태스크 자신의 픽스처(마지막 세대=최종 챔피언과 이름 동일)에서 곧바로 좌석 오귀속이 발동한다 |
| `attempts` 필드를 브리프처럼 `Math.max(1, nextAttempt(gen)-1)`로 하한 고정 | Gen 0처럼 루프를 한 번도 거치지 않은 세대까지 "시도 1회"로 보고하게 되어 실제와 다른 값을 낸다. `nextAttempt(gen)-1`은 항상 0 이상이라 별도 하한이 필요 없다 |

**검증.** RED: `./gradlew :arena-tournament:test --tests '*RoundRobinTest*' --tests '*BundleBuilderTest*'` — 컴파일 실패(`RoundRobin`/`BundleBuilder`/`GenerationStat` 없음). GREEN: 같은 명령 12개 전부 PASS. 전체 `./gradlew test` 155개(기존 143 + 신규 12) 전부 GREEN, `records/`·`web/`은 이번에도 생성되지 않았다.

---

### D59. Task 18 리뷰 반려 — 죽은 갤러리 시드 테스트, `BundleBuilder` 충돌 시나리오 무방비, roundrobin.json의 NaN을 null로 교체, D58 메커니즘 서술 정정

**리뷰 결과.** `RoundRobin`·`BundleBuilder`의 예약 id 전환(브리프 초안의 `a.name()`/`bot.name()` 직접 사용을 걷어낸 것), 행렬 방향·대각선·자기대전 회피, 바이트 동일성 장치는 전부 독립적으로 재확인됐다며 승인 — 리뷰어가 `SeriesRunner.playOne`을 직접 확인해 "id0==id1일 때 좌석 교대 절반에서 `bot0Id`가 항상 같은 문자열로 찍힌다"는 D58의 핵심 주장을 실측으로 재검증했다. 반려는 증거 쪽 세 갈래 + 기록 정정 하나.

**1) `갤러리의_모든_경기는_같은_시드를_쓴다`가 항상 통과하는 죽은 테스트였다.** `assertFalse(json.contains("\"seed\":2"))`는 이 MAPPER가 `INDENT_OUTPUT`을 켠 pretty printer라 필드 구분자가 `" : "`인 것과 충돌한다 — 실제 출력은 `"seed" : 1`처럼 콜론 양옆에 공백이 들어가므로 `"seed":2`라는 부분 문자열 자체가 애초에 나올 수 없다(`RecordStoreTest`의 통과하는 테스트들이 이미 공백 있는 형태를 보여준다). 세대마다 다른 시드를 썼어도(테스트 이름이 정확히 막으려던 상황) 이 어서션은 계속 통과했을 것이다.

**조치.** 부분 문자열 매칭을 버리고 실제 `seed` 값을 확인하는 것으로 바꿨다. `Replay[]`로 완전히 역직렬화하려 했더니 `MatchResult.isDraw()`가 자바빈 getter 관례(`isXxx()`)를 따르는 탓에 Jackson이 직렬화할 때 레코드에 없는 파생 필드 `"draw"`를 추가로 써 넣고, 그 결과 `Replay[]`로 완전히 되읽으면 `UnrecognizedPropertyException`이 났다(실측 확인, 이 태스크의 범위 밖인 기존 quirk라 `MatchResult` 자체는 건드리지 않았다) — 그래서 `JsonNode`로 `seed`만 뽑는 방식으로 바꿨다. 겸사겸사 `gallery.size() == generations.size()`도 같이 고정했다(원래 이 검증이 어디에도 없어서 갤러리가 1개짜리든 6개짜리든 통과하던 공백을 메운다).

**2) `RoundRobin`은 이름 충돌 회귀 테스트로 무장했지만 `BundleBuilder.buildStats`는 무방비였다.** `이름이_같은_두_봇이_섞여도_행렬이_왜곡되지_않는다`(RoundRobin)는 비대칭 강도(WallAvoidBot vs Gen00Bot)에 `> 0.8` 어서션을 걸어 회귀에 민감하지만, `챔피언과_이름이_같은_세대도_지표가_유효_범위_안에_있다`(BundleBuilder)는 `occupancy`·`suicideRate`·`scoreRate`가 `[0,1]` 범위 안인지만 본다 — 오귀속이 나도 여전히 비율이고 여전히 범위 안이라(scoreRate는 좌석 0 승률 근사인 0.5 근처로 무너질 뿐) 이 테스트는 버그가 있어도 통과한다.

**조치.** `RoundRobin`이 쓰는 것과 같은 비대칭 강도 트릭을 `BundleBuilder`에도 적용했다: `generations = [new WallAvoidBot()]`(실제 강한 봇) 하나에, 이름은 "WallAvoidBot"이라고 보고하지만 실제로는 `Gen00Bot`(약함)에게 위임하는 챔피언을 붙여 `세대와_챔피언의_이름이_같아도_scoreRate가_뭉개지지_않는다`로 `scoreRate() > 0.8`을 고정했다. 실제로 물리는지 `BundleBuilder.buildStats`를 브리프 초안(`bot.name()`/`champion.name()`을 SeriesRunner id로 직접 넘기고 `r.bot0Id().equals(bot.name())`으로 좌석 판정)으로 일시적으로 되돌려 이 테스트만 돌렸더니:
```
BundleBuilderTest > 세대와_챔피언의_이름이_같아도_scoreRate가_뭉개지지_않는다(Path) FAILED
    org.opentest4j.AssertionFailedError: 이름이 같다는 이유로 WallAvoidBot의 scoreRate가
    뭉개졌다: 0.5 ==> expected: <true> but was: <false>
```
`scoreRate`가 정확히 `0.5`로 무너져 실패했다 — 좌석 0 오귀속이 승률을 동전 던지기 값으로 뭉갠다는 예측과 정확히 일치한다. 확인 후 원래 구현으로 즉시 복원했고, `git diff`로 `buildStats` 본문이 리뷰 이전 코드와 완전히 같음을 확인했다(변경분은 `buildRoundRobin`/`toWireMatrix`뿐).

**3) roundrobin.json의 대각선을 인용된 `"NaN"` 문자열이 아니라 JSON `null`로 내보내야 한다.** D58까지 따른 "Jackson 기본 NaN 처리"는 `records/`(자바가 다시 읽는 내부 기록)에는 맞지만, `roundrobin.json`은 자바스크립트 프론트엔드가 소비하는 파일이다. 숫자 배열 안에 섞인 문자열 `"NaN"`은 `cell.toFixed(2)`를 던지게 하고 `cell > 0.5`를 조용히 `false`로 만드는 타입 함정이다 — 히트맵 전용 파일에서 가장 나쁜 자리에서 터진다.

**조치.** `RoundRobin.run`의 Java API는 그대로 `Double.NaN`을 대각선에 둔다 — 값 자체의 의미(자기 자신과의 대전 없음)는 바뀌지 않는다. `BundleBuilder`가 JSON으로 내보낼 때만 `toWireMatrix`로 `double[][]`을 `Double[][]`(박싱)로 바꾸며 `Double.isNaN(v) ? null : v`로 대각선을 명시적 `null`로 치환한다 — 박싱된 `Double`에 NaN 값 자체를 담아도 Jackson은 여전히 `"NaN"` 문자열을 적으므로, 참조를 `null`로 바꾸는 것만이 유효한 우회다. `records/`에 쓰는 `ChallengeReport.holdoutScoreRate` 등은 손대지 않았다 — 그쪽은 자바가 다시 읽어야 하고, 인용된 `"NaN"`이 별도 설정 없이 정확히 왕복한다는 게 D56/RecordStoreTest로 이미 증명돼 있다. `RoundRobin.run`의 javadoc에 이 wire 변환("Java API는 NaN, `BundleBuilder`가 만드는 JSON은 null, 두 파일의 소비자가 다르므로 wire 표현도 다르다")을 명시했다. `라운드로빈_JSON의_대각선은_null이고_비대각선은_숫자다` 테스트가 `roundrobin.json`에 `"NaN"` 문자열이 전혀 없음과, `JsonNode`로 파싱했을 때 대각선은 `isNull()`, 비대각선은 `isNumber()`임을 확인한다 — 이 테스트가 리뷰 이전의 "NaN이 왕복된다" 테스트를 대체한다. NaN 왕복은 더 이상 이 파일의 계약이 아니다.

**4) D58의 메커니즘 서술이 틀렸다 — 정정.** D58은 버그를 "subjectId가 bot0Id와도 bot1Id와도 일치하지 않으면 조용히 좌석 0으로 귀속된다"고 적었는데, 이건 고쳐지기 전 코드의 동작이 아니라 **오늘의 `seatOf`가 예외를 던지는 경우**(subjectId가 어느 쪽과도 일치하지 않을 때)를 묘사한 것이다. 실제 메커니즘은 정반대다: id0==id1로 만들면 subjectId가 bot0Id와 bot1Id **둘 다와 일치**하고, `seatOf`가 bot0Id를 먼저 검사해 실제 좌석이 1이었어도 항상 0을 반환한다. `RoundRobin.java`의 javadoc(리뷰 전 버전 22~27행)은 이미 이렇게 정확히 적혀 있었고 D58 본문만 틀렸다. CLAUDE.md §2("뒤집힌 판단은 지우지 말고 다시 쓴다")에 따라 D58 본문은 그대로 두고 이 항목으로 정정한다.

**손대지 않은 것(리뷰가 원장에 미루라고 지시한 항목).** 번들 테스트가 항상 빈 `RecordStore`로 돌아 `attempts`·`loop-history` 내용물이 실제로는 검증되지 않는 것, 빈 `judgingSeeds`가 조용히 `NaN` 평균을 내는 것, 갤러리 원소에 세대 인덱스가 없어 프론트엔드가 위치로만 join해야 하는 것, `RoundRobinTest`가 메서드마다 전체 행렬을 다시 계산하는 것.

**검증.** `./gradlew :arena-tournament:test --tests '*BundleBuilderTest*' --tests '*RoundRobinTest*'` 13개 전부 GREEN(기존 12 + 신규 1: 이름 충돌 `buildStats` 회귀 테스트, "NaN 왕복" 테스트는 "null/숫자" 테스트로 교체). 전체 `./gradlew test` 156개(기존 155 + 신규 1) 전부 GREEN. `records/`·`web/`은 이번에도 생성되지 않았다.

---

## 다음 단계

- [x] 스펙 문서 작성 및 자체 검토
- [x] 구현 계획 1 (하네스) 작성
- [ ] 계획 1 실행
- [ ] 계획 2 (시각화) 작성 및 실행
- [ ] 계획 3 (세대 루프) 작성 및 실행
